---
phase: 05-dashboard
plan: "01"
type: tdd
wave: 1
depends_on: ["05-00"]
files_modified:
  - backend/src/parsers/suricata_parser.py
  - backend/src/api/models.py
autonomous: true
requirements:
  - FR-5S-1
  - FR-5S-2

must_haves:
  truths:
    - "parse_eve_line parses all 5 EVE event types without crashing"
    - "EVE dest_ip field is correctly mapped to dst_ip (not left as None)"
    - "Suricata severity 1 maps to critical, 4 maps to low (inverted scale)"
    - "Unknown EVE event types produce event_type=suricata_{type} without crashing"
    - "IngestSource.suricata enum value equals the string suricata"
    - "Alert model has threat_score defaulting to 0 and attack_tags defaulting to []"
    - "All 41 pre-existing tests still pass after model extension"
  artifacts:
    - path: "backend/src/parsers/suricata_parser.py"
      provides: "Full parse_eve_line implementation"
      exports: ["parse_eve_line", "_SEVERITY_MAP"]
      min_lines: 60
    - path: "backend/src/api/models.py"
      provides: "Extended IngestSource + Alert models"
      contains: "suricata"
  key_links:
    - from: "backend/src/parsers/suricata_parser.py"
      to: "backend/src/api/models.py"
      via: "compatible dict keys — same field names as normalize() expects"
      pattern: "dst_ip.*dest_ip"
    - from: "backend/src/api/models.py"
      to: "backend/src/api/routes.py"
      via: "IngestSource enum used in ingest_batch handler"
      pattern: "IngestSource\\.suricata"
---

<objective>
Wave 1, Plan A: Implement Suricata EVE parser (suricata_parser.py) and extend the Alert + IngestSource models.

Purpose: Make P5-T1 through P5-T9 pass. P5-T10 regression must also pass. Plans 01 and 02 run in parallel (no shared files).
Output: Full parse_eve_line implementation; extended models.py with IngestSource.suricata and Alert.threat_score/attack_tags.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-dashboard/05-CONTEXT.md
@.planning/phases/05-dashboard/05-RESEARCH.md
@.planning/phases/05-dashboard/05-00-SUMMARY.md

<interfaces>
<!-- Existing patterns to follow exactly. -->

From backend/src/parsers/normalizer.py (the normalize() function expects these keys):
```python
# normalize() accepts a raw dict and maps these keys:
# event, event_type, host, src_ip, dst_ip, query, port, protocol, severity, user, raw
# Unknown keys are preserved via raw=raw (full input dict)
```

From backend/src/ingestion/syslog_parser.py (canonical parser pattern):
```python
def parse_syslog_line(line: str) -> dict:
    """Returns dict with keys: event_type, host, severity, raw (and optional src_ip, dst_ip, query, port)"""
    ...
    return {
        "event_type": ...,
        "host": ...,
        "severity": ...,
        "src_ip": ...,
        "raw": {...},
    }
```

From backend/src/api/models.py (current Alert — adding fields with defaults is non-breaking):
```python
class Alert(BaseModel):
    id: str
    timestamp: str
    rule: str
    severity: str
    event_id: str
    description: str
    # DO NOT add required fields — only fields with defaults are safe

class IngestSource(str, Enum):
    fixture = "fixture"
    syslog = "syslog"
    vector = "vector"
    api = "api"
    # Add: suricata = "suricata"
```

EVE field mapping (LOCKED from CONTEXT.md):
| EVE field    | Normalized field |
|--------------|-----------------|
| dest_ip      | dst_ip          |  <-- TRAP: EVE uses dest_ip, schema uses dst_ip
| dest_port    | raw.dest_port   |
| src_port     | port            |
| proto        | protocol        |
| alert.severity | severity (1=critical,2=high,3=medium,4=low — INVERTED) |
| alert.signature | event_type (for alert events) |
| dns.rrname   | query           |
| tls.sni      | query           |
| flow_id      | raw.flow_id     |
| host         | host            |
</interfaces>
</context>

<feature>
  <name>Suricata EVE parser + model extension</name>
  <files>backend/src/parsers/suricata_parser.py, backend/src/api/models.py</files>
  <behavior>
    TDD cycles — RED then GREEN:

    RED (already failing from Wave 0 stubs):
    - P5-T1: parse_eve_line(alert_line)["severity"] == "critical" and ["dst_ip"] is not None
    - P5-T2: parse_eve_line(dns_line)["event_type"] == "dns_query" and ["query"] == "suspicious-domain.test" and ["dst_ip"] is not None
    - P5-T3: parse_eve_line(flow_line)["event_type"] == "connection" and ["dst_ip"] is not None
    - P5-T4: parse_eve_line(http_line)["event_type"] == "http_request"
    - P5-T5: parse_eve_line(tls_line)["event_type"] == "tls_session" and ["query"] == "c2.evil.test"
    - P5-T6: parse_eve_line(unknown_line) returns dict with event_type starting with "suricata_" — no exception
    - P5-T7: parse_eve_line with severity=1 → "critical"; severity=4 → "low"
    - P5-T8: IngestSource.suricata.value == "suricata"
    - P5-T9: Alert(id=...).threat_score == 0 and Alert(id=...).attack_tags == []
    - P5-T10: Full suite of 41 pre-existing tests still pass

    GREEN implementation requirements:
    - _SEVERITY_MAP = {1: "critical", 2: "high", 3: "medium", 4: "low"}
    - dst_ip = data.get("dest_ip") — NEVER data.get("dst_ip") (EVE uses dest_ip)
    - dest_port stored in raw dict, not as a top-level field
    - alert events: event_type = alert["signature"], not "alert"
    - dns events: query = dns.get("rrname")
    - tls events: query = tls.get("sni") or tls.get("subject")
    - flow events: event_type = "connection"
    - http events: event_type = "http_request"
    - unknown types: event_type = f"suricata_{event_type}" — no exception raised
    - Invalid JSON input: return safe fallback dict, not raise exception
    - Alert gets: threat_score: int = 0  and  attack_tags: list[dict] = Field(default_factory=list)
    - IngestSource gets: suricata = "suricata"
  </behavior>
  <implementation>
    Step 1 (RED): Run failing tests to confirm they fail with NotImplementedError:
      `uv run pytest backend/src/tests/test_phase5.py::TestSuricataParser backend/src/tests/test_phase5.py::TestModels -v --tb=short`

    Step 2 (GREEN — suricata_parser.py): Replace stub with full implementation.
    Use json.loads() for parsing. Implement _SEVERITY_MAP dict. Map all 5 event types.
    Implement graceful fallback for unknown types and invalid JSON.
    Critical traps to avoid (document in comments):
      - TRAP 1: EVE uses dest_ip not dst_ip — always `data.get("dest_ip")` for dst_ip
      - TRAP 2: Severity is INVERTED — 1=critical (highest), 4=low (lowest)

    Step 3 (GREEN — models.py): Add two lines:
      - suricata = "suricata" to IngestSource enum
      - threat_score: int = 0 and attack_tags: list[dict] = Field(default_factory=list) to Alert

    Step 4 (VERIFY): Run target tests then full suite:
      `uv run pytest backend/src/tests/test_phase5.py::TestSuricataParser backend/src/tests/test_phase5.py::TestModels -v`
      `uv run pytest backend/src/tests/ -v --tb=short`
  </implementation>
</feature>

<verification>
  <automated>uv run pytest backend/src/tests/test_phase5.py::TestSuricataParser backend/src/tests/test_phase5.py::TestModels -v --tb=short</automated>
  Full regression: `uv run pytest backend/src/tests/ -v --tb=no -q`
  P5-T1 through P5-T9 must PASS (not xfail). P5-T10 regression must PASS.
</verification>

<success_criteria>
- P5-T1 through P5-T9 all PASS
- P5-T10 regression: all 41 pre-existing tests still PASS
- parse_eve_line handles all 5 EVE event types + unknown + invalid JSON
- Alert.threat_score defaults to 0, Alert.attack_tags defaults to []
- IngestSource.suricata.value == "suricata"
</success_criteria>

<output>
After completion, create `.planning/phases/05-dashboard/05-01-SUMMARY.md`
</output>
