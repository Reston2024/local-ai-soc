---
phase: 37-analyst-report-templates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/test_report_templates.py
  - backend/models/report.py
  - backend/api/report_templates.py
  - backend/main.py
autonomous: true
requirements:
  - P37-T01
  - P37-T02
  - P37-T03
  - P37-T08

must_haves:
  truths:
    - "POST /api/reports/template/session-log returns 201 with report id and stores report with type='template_session_log'"
    - "POST /api/reports/template/incident/{case_id} returns 201 and stores report with type='template_incident'"
    - "POST /api/reports/template/playbook-log/{run_id} returns 201 and stores report with type='template_playbook_log'"
    - "GET /api/reports lists template reports alongside investigation/executive reports without ValidationError"
    - "GET /api/reports/template/meta returns counts for investigations, closed_cases, playbook_runs, actors, and actor_list"
  artifacts:
    - path: "tests/unit/test_report_templates.py"
      provides: "Wave 0 unit test stubs — all 6 HTML builders + Report.type widening + meta endpoint"
      exports: []
    - path: "backend/models/report.py"
      provides: "Report.type widened to include all 6 template type strings"
      contains: "template_session_log"
    - path: "backend/api/report_templates.py"
      provides: "6 HTML builder functions + 6 POST endpoints + template/meta GET endpoint"
      exports: ["router", "_session_log_html", "_incident_html", "_playbook_log_html", "_pir_html", "_ti_bulletin_html", "_severity_ref_html"]
    - path: "backend/main.py"
      provides: "report_templates router registered under /api/reports prefix"
      contains: "report_templates_router"
  key_links:
    - from: "backend/api/report_templates.py"
      to: "backend/api/reports._render_pdf"
      via: "from backend.api.reports import _render_pdf, _strip_pdf_b64"
      pattern: "_render_pdf"
    - from: "backend/api/report_templates.py"
      to: "stores.sqlite.insert_report"
      via: "asyncio.to_thread(stores.sqlite.insert_report, {...})"
      pattern: "insert_report"
    - from: "backend/models/report.py"
      to: "backend/api/reports list endpoint"
      via: "Report.type Literal deserialization"
      pattern: "template_session_log.*template_incident"
---

<objective>
Wave 0 test stubs + backend/models widening + Session Log / Incident Report / Playbook Log template endpoints.

Purpose: Establish the test scaffold and implement the first three report templates (Session Log, Incident Report, Playbook Log) in a new report_templates.py module. Also widen Report.type and register the router.

Output: tests/unit/test_report_templates.py (stubs for all 6 templates), backend/models/report.py (widened Literal), backend/api/report_templates.py (3 working templates + meta endpoint), backend/main.py (router registered).
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-analyst-report-templates/37-CONTEXT.md
@.planning/phases/37-analyst-report-templates/37-RESEARCH.md

<interfaces>
<!-- Key contracts the executor needs. -->

From backend/models/report.py (current):
```python
class Report(BaseModel):
    id: str
    type: Literal["investigation", "executive"]   # MUST widen to include 6 new types
    title: str
    subject_id: Optional[str] = None
    period_start: Optional[str] = None
    period_end: Optional[str] = None
    content_json: str
    created_at: str
```

From backend/api/reports.py (reuse these — do NOT modify reports.py):
```python
def _render_pdf(html_content: str) -> bytes:   # always call inside asyncio.to_thread()
def _strip_pdf_b64(report: dict) -> dict:
router = APIRouter(prefix="/api/reports", tags=["reports"])
```

SQLiteStore.insert_report(data: dict) where data keys:
  id, type, title, subject_id, period_start, period_end, content_json, created_at

SQLiteStore method inventory (confirmed available):
  get_latest_triage() -> dict | None
  list_investigation_cases(status=None) -> list[dict]
  get_investigation_case(case_id) -> dict | None
  list_detections() -> list[dict]
  _conn: sqlite3.Connection  (use directly for ad-hoc queries)

DuckDBStore:
  await fetch_all(sql, params) -> list[tuple]

From backend/main.py — router registration block for reports (line ~644):
  from backend.api.reports import router as reports_router
  app.include_router(reports_router, dependencies=[Depends(verify_token)])
  # Add report_templates router in the same try/except block immediately after
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — test stubs + Report.type widening</name>
  <files>tests/unit/test_report_templates.py, backend/models/report.py</files>
  <behavior>
    - test_report_type_widening: instantiate Report with type="template_session_log" — must not raise ValidationError
    - test_session_log_html_returns_string: call _session_log_html(title="T", generated_at="2026-01-01", event_count_24h=5, event_type_breakdown=[], source_types=[], detection_count_24h=2, latest_triage=None, git_hash="abc123") — returns str containing "INTERNAL USE ONLY" and "Session Log"
    - test_incident_html_returns_string: call _incident_html with minimal stub dicts — returns str containing "Incident Report"
    - test_playbook_log_html_returns_string: call _playbook_log_html with minimal stub dicts — returns str containing "Playbook"
    - test_pir_html_returns_string: call _pir_html with minimal stub dicts — returns str containing "Post-Incident"
    - test_ti_bulletin_html_returns_string: call _ti_bulletin_html with minimal stub dicts — returns str containing "Threat Intelligence"
    - test_severity_ref_html_returns_string: call _severity_ref_html() — returns str containing "Severity" and "INTERNAL USE ONLY"
    - All 6 html builder tests import from backend.api.report_templates (mark xfail initially as stubs, remove xfail when Plan 01 + 02 complete)
  </behavior>
  <action>
    Step 1 — Widen Report.type in backend/models/report.py:
    Change the Literal from `Literal["investigation", "executive"]` to:
    `Literal["investigation", "executive", "template_session_log", "template_incident", "template_playbook_log", "template_pir", "template_ti_bulletin", "template_severity_ref"]`

    Step 2 — Create tests/unit/test_report_templates.py:
    Write stub tests for all 6 HTML builder functions. Initially mark html-builder tests that import from report_templates.py as pytest.mark.xfail(strict=False, reason="report_templates.py not yet implemented") so the test FILE itself passes even before Plan 01 Task 2 creates the module.

    The test_report_type_widening test does NOT need xfail — it only tests the model Literal change.

    Import pattern in test file:
    ```python
    import pytest
    from backend.models.report import Report

    # These imports will fail until report_templates.py exists
    try:
        from backend.api.report_templates import (
            _session_log_html, _incident_html, _playbook_log_html,
            _pir_html, _ti_bulletin_html, _severity_ref_html,
        )
        TEMPLATES_AVAILABLE = True
    except ImportError:
        TEMPLATES_AVAILABLE = False

    skip_if_missing = pytest.mark.skipif(not TEMPLATES_AVAILABLE, reason="report_templates not yet created")
    ```

    Apply `@skip_if_missing` to all html-builder tests. Do NOT use xfail — use skipif on import success. This way tests run green as stubs.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_report_templates.py -q</automated>
  </verify>
  <done>test_report_type_widening passes. All 6 HTML builder tests are skipped (not failed). Report accepts all 6 template type strings.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: backend/api/report_templates.py — Session Log + Incident Report + Playbook Log + meta endpoint</name>
  <files>backend/api/report_templates.py</files>
  <behavior>
    - _session_log_html returns HTML string containing "INTERNAL USE ONLY", "Session Log", section headings "1.", "2.", "3.", signature line "Analyst Signature"
    - _incident_html returns HTML string containing "Security Incident Report", "INC-", "INTERNAL USE ONLY", "LLM Inference Audit Trail", signature line
    - _playbook_log_html returns HTML string containing "Playbook Execution Log", "INTERNAL USE ONLY", step results table, signature line
    - POST /api/reports/template/session-log (tested via test_session_log_html which already exists): endpoint exists, returns 201 JSON with "id" key
    - POST /api/reports/template/incident/{case_id}: returns 201 with "id", handles missing case_id gracefully (blank template)
    - POST /api/reports/template/playbook-log/{run_id}: returns 201, handles missing run_id (blank template)
    - GET /api/reports/template/meta: returns JSON with keys investigations, closed_cases, playbook_runs, actors, actor_list (list of {name, group_id})
  </behavior>
  <action>
    Create backend/api/report_templates.py. This module has its OWN router with prefix="/api/reports" (same prefix as reports.py but different module). FastAPI merges routes from both routers correctly since they use different paths.

    Module imports:
    ```python
    import asyncio, json, subprocess
    from datetime import datetime, timedelta, timezone
    from uuid import uuid4
    from fastapi import APIRouter, Request
    from fastapi.responses import JSONResponse
    from backend.api.reports import _render_pdf, _strip_pdf_b64
    from backend.core.logging import get_logger
    ```

    Router: `router = APIRouter(prefix="/api/reports", tags=["report-templates"])`

    **GET /api/reports/template/meta** (define FIRST to avoid path param capture):
    Fetch in a single asyncio.to_thread call using stores.sqlite._conn:
    - COUNT(*) from investigation_cases
    - COUNT(*) from investigation_cases WHERE case_status = 'closed'
    - COUNT(*) from playbook_runs (with try/except for missing table)
    - SELECT name, group_id FROM attack_groups ORDER BY name (with try/except)
    Return JSON: {investigations, closed_cases, playbook_runs, actors (count), actor_list: [{name, group_id}]}
    Also return case_list: [{case_id, title, case_status}] and run_list: [{run_id, playbook_id, status, started_at}] — this populates all dropdowns in a single call (per Pitfall 4 in RESEARCH.md).

    **_session_log_html(title, generated_at, event_count_24h, event_type_breakdown, source_types, detection_count_24h, latest_triage, git_hash) -> str:**
    Build HTML with:
    - Dark header: `<div style="background:#16213e;color:#fff;padding:16px 24px;margin-bottom:20px;">` with "AI-SOC-Brain" branding + classification badge "INTERNAL USE ONLY"
    - Section 1: Ingest Summary (event_count_24h, source type breakdown table)
    - Section 2: Detection Summary (detection_count_24h, last 24h)
    - Section 3: Event Type Breakdown (table of event_type, count)
    - Section 4: Latest Triage Result (pre-fill from latest_triage dict — severity_summary, result_text verbatim in `<pre style="white-space:pre-wrap;word-break:break-all;">`, model_name, created_at; if None show placeholder text)
    - Section 5: LLM Inference Audit Trail (REQUIRED per Rule 5.3 — table columns: Model, Prompt Version, Severity Summary, Result Text, Confidence [blank], Disposition [blank]; rows from latest_triage if available)
    - Section 6: Git Hash / Build Reference (git_hash)
    - Signature line: "Analyst Signature: _________________  Date: _______________"
    - CSS: same dark-header style as existing reports.py templates (body font Arial 12px, th background #16213e, tr:nth-child(even) #f2f2f2, word-break:break-all on pre elements)

    **POST /api/reports/template/session-log:**
    - Compute cutoff = (now - 24h).isoformat()
    - DuckDB queries (using await stores.duckdb.fetch_all):
      1. COUNT(*) WHERE timestamp >= cutoff AND timestamp <= now_iso
      2. GROUP BY event_type ORDER BY cnt DESC LIMIT 20
      3. GROUP BY source_type ORDER BY cnt DESC
    - SQLite: detection_count = query detections COUNT WHERE created_at >= cutoff (asyncio.to_thread)
    - SQLite: latest_triage = await asyncio.to_thread(stores.sqlite.get_latest_triage)
    - git hash via subprocess.check_output(["git","rev-parse","HEAD"], text=True, timeout=5).strip()[:12], except → "unknown"
    - Build HTML, render PDF in asyncio.to_thread(_render_pdf, html)
    - insert_report with type="template_session_log", subject_id=None
    - Return 201 JSON: {id, type, title, created_at}

    **_incident_html(title, case, detections, ioc_hits, triage, techniques) -> str:**
    Sections: 1. Case Summary, 2. Timeline of Detections, 3. IOC Matches, 4. ATT&CK Techniques Observed, 5. LLM Inference Audit Trail, 6. Analyst Assessment [blank placeholders], signature line.
    Blank template when case is None — all sections show "[No data available — analyst to complete]".

    **POST /api/reports/template/incident/{case_id}:**
    - Fetch case: await asyncio.to_thread(stores.sqlite.get_investigation_case, case_id)
    - If case is None, use blank template (never block)
    - Fetch detections via stores.sqlite._conn: SELECT * FROM detections WHERE investigation_id = ? OR case_id = ? LIMIT 50 (with try/except fallback to [])
    - Fetch ioc_hits: SELECT * FROM ioc_hits ORDER BY matched_at DESC LIMIT 30 (try/except)
    - Fetch triage: asyncio.to_thread(stores.sqlite.get_latest_triage)
    - Fetch techniques: direct _conn query on detection_techniques JOIN attack_techniques (try/except → [])
    - Build HTML, render PDF, insert_report type="template_incident" subject_id=case_id

    **_playbook_log_html(title, run, playbook, case, triage) -> str:**
    Sections: 1. Run Metadata, 2. Playbook Definition, 3. Step-by-Step Execution Log (verbatim steps_completed JSON rows as table), 4. LLM Inference Audit Trail, 5. Analyst Sign-off [blank], signature line.

    **POST /api/reports/template/playbook-log/{run_id}:**
    - Fetch run: direct _conn.execute("SELECT * FROM playbook_runs WHERE run_id = ?", [run_id]) (try/except → None for blank template)
    - Fetch playbook definition if run exists
    - Fetch case if run.investigation_id exists
    - Fetch triage: asyncio.to_thread(stores.sqlite.get_latest_triage)
    - Build HTML, render PDF, insert_report type="template_playbook_log" subject_id=run_id

    **Graceful fallback pattern** for all endpoints: if any data fetch raises, log warning and continue with None/[] — never raise HTTPException for missing data (blank template is always valid per CONTEXT.md).

    **Word-wrap for verbatim text blocks:** All `<td>` and `<pre>` cells containing triage result_text or playbook step outputs must use `style="white-space:pre-wrap;word-break:break-all;"` to prevent PDF overflow (RESEARCH.md Pitfall 5).
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_report_templates.py -q</automated>
  </verify>
  <done>All 3 HTML builder tests pass (skip_if_missing condition is now False since module exists). test_session_log_html, test_incident_html, test_playbook_log_html all pass. test_report_type_widening still passes.</done>
</task>

<task type="auto">
  <name>Task 3: Register report_templates router in main.py</name>
  <files>backend/main.py</files>
  <action>
    In main.py, find the try/except block that registers reports_router (around line 644):
    ```python
    try:
        from backend.api.reports import router as reports_router
        app.include_router(reports_router, dependencies=[Depends(verify_token)])
    ```

    Add the report_templates router registration INSIDE the same try/except block, immediately after the reports_router line:
    ```python
        from backend.api.report_templates import router as report_templates_router
        app.include_router(report_templates_router, dependencies=[Depends(verify_token)])
    ```

    The router prefix is already "/api/reports" inside the module — do NOT add an additional prefix= here (would double-prefix). Match the pattern used for reports_router exactly.

    After adding, verify the app still imports cleanly.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/ -q --tb=short 2>&1 | tail -5</automated>
  </verify>
  <done>uv run pytest tests/unit/ -q exits 0. All existing 989 tests still pass. report_templates router registered without import error.</done>
</task>

</tasks>

<verification>
uv run pytest tests/unit/test_report_templates.py -v
uv run pytest tests/unit/ -q
</verification>

<success_criteria>
- tests/unit/test_report_templates.py exists and passes (7 tests: 1 type-widening pass + 3 html-builder pass + 3 still-skipped for Plan 02 builders)
- backend/models/report.py Report.type Literal includes all 6 template type strings
- backend/api/report_templates.py exists with _session_log_html, _incident_html, _playbook_log_html, GET /template/meta, POST /template/session-log, POST /template/incident/{case_id}, POST /template/playbook-log/{run_id}
- backend/main.py registers report_templates_router
- All 989+ existing unit tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/37-analyst-report-templates/37-01-SUMMARY.md`
</output>
