---
phase: 27-malcolm-nsm-integration-and-live-feed-collector
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/unit/test_malcolm_collector.py
  - tests/unit/test_malcolm_normalizer.py
  - tests/unit/test_dispatch_endpoint.py
autonomous: true
requirements:
  - P27-T02
  - P27-T04

must_haves:
  truths:
    - "All three stub test files exist and are collected by pytest"
    - "All stubs report as SKIPPED (not ERROR, not FAILED)"
    - "Full unit suite still passes with 0 failures after stubs are added"
  artifacts:
    - path: "tests/unit/test_malcolm_collector.py"
      provides: "Wave-0 skipped stubs for MalcolmCollector lifecycle (P27-T02)"
    - path: "tests/unit/test_malcolm_normalizer.py"
      provides: "Wave-0 skipped stubs for field normalization (P27-T02)"
    - path: "tests/unit/test_dispatch_endpoint.py"
      provides: "Wave-0 skipped stubs for dispatch endpoint (P27-T04)"
  key_links:
    - from: "tests/unit/test_malcolm_collector.py"
      to: "ingestion/jobs/malcolm_collector.py"
      via: "try/except ImportError guard + pytestmark skip"
      pattern: "pytestmark.*skip"
    - from: "tests/unit/test_dispatch_endpoint.py"
      to: "backend/api/recommendations.py"
      via: "FastAPI TestClient — activated in plan 27-04"
      pattern: "pytestmark.*skip"
---

<objective>
Create Wave-0 test stubs for Phase 27.

Purpose: Establish Nyquist-compliant test scaffolding before any implementation starts. All stubs are pre-skipped so the full test suite continues to pass. Executor plans 27-02, 27-03, and 27-04 will activate these stubs once the corresponding implementation is complete.

Output: Three new test files under tests/unit/, all reporting SKIPPED when run.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-malcolm-nsm-integration-and-live-feed-collector/27-CONTEXT.md
@.planning/phases/27-malcolm-nsm-integration-and-live-feed-collector/27-VALIDATION.md

<interfaces>
<!-- Wave-0 stub pattern established in Phase 26 — use exactly this pattern. -->

From tests/unit/test_graph_schema.py (module-level skip — simplest form):
```python
import pytest
pytestmark = pytest.mark.skip(reason="wave-0 stub — activate in plan 26-05")

def test_something():
    pass
```

From tests/unit/test_firewall_collector.py (try/except ImportError guard — use this for
files where the implementation module does not yet exist):
```python
import pytest

try:
    from ingestion.jobs.firewall_collector import FirewallCollector
    _IMPORT_OK = True
except ImportError:
    _IMPORT_OK = False

pytestmark = pytest.mark.skipif(not _IMPORT_OK, reason="FirewallCollector not implemented — Wave 2")
```

Decision from STATE.md (26-00):
  "26-00: pytestmark module-level skip used for Wave-0 stubs — single decorator per file,
   activated in plan 26-05"
  "26-00: extract_perimeter_entities import wrapped in try/except inside test body —
   avoids module-level ImportError breaking skip mechanism"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test_malcolm_collector.py stubs</name>
  <files>tests/unit/test_malcolm_collector.py</files>
  <action>
Create tests/unit/test_malcolm_collector.py with module-level pytestmark skip.

Use the try/except ImportError guard pattern (from test_firewall_collector.py) because
ingestion/jobs/malcolm_collector.py does NOT exist yet — without the guard, a bare import
would raise ImportError and break pytest collection even with pytestmark.

Stubs to create (all skipped):

1. test_malcolm_collector_init_sets_defaults — MalcolmCollector() sets _running=False,
   _alerts_ingested=0, _syslog_ingested=0, _consecutive_failures=0

2. test_malcolm_collector_status_shape — status() returns dict with keys:
   running, alerts_ingested, syslog_ingested, consecutive_failures

3. test_malcolm_collector_run_cancels_cleanly — asyncio.CancelledError propagates out of
   run() and sets _running=False

4. test_malcolm_collector_backoff_on_failure — after a failed poll cycle, backoff doubles
   (up to 300s cap)

5. test_malcolm_collector_heartbeat_updates_kv — _poll_and_ingest() calls
   sqlite.set_kv("malcolm.last_heartbeat", ...) each cycle

File header docstring: "Stub tests for Phase 27 MalcolmCollector (P27-T02). Activated in plan 27-02."
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_malcolm_collector.py -v 2>&1 | grep -E "(SKIPPED|PASSED|ERROR|FAILED|test_)"</automated>
  </verify>
  <done>All 5 stubs collected and reported as SKIPPED, 0 errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create test_malcolm_normalizer.py stubs</name>
  <files>tests/unit/test_malcolm_normalizer.py</files>
  <action>
Create tests/unit/test_malcolm_normalizer.py with module-level pytestmark skip.

Use the same try/except ImportError guard as Task 1 (malcolm_collector.py does not exist yet).
Import target: `from ingestion.jobs.malcolm_collector import MalcolmCollector`.

Stubs to create (all skipped):

1. test_normalize_alert_extracts_src_ip — _normalize_alert() extracts source.ip (ECS) with
   fallback to src_ip (flat) from doc, returns NormalizedEvent with source_type="suricata_eve"

2. test_normalize_alert_extracts_dst_ip — _normalize_alert() extracts destination.ip with
   fallback to dst_ip

3. test_normalize_alert_severity_mapping — event.severity or alert.severity becomes NormalizedEvent.severity

4. test_normalize_alert_detection_source — rule.name or alert.signature becomes
   NormalizedEvent.detection_source

5. test_normalize_syslog_source_type — _normalize_syslog() returns NormalizedEvent with
   source_type="ipfire_syslog"

6. test_normalize_alert_raw_event_truncated — raw_event in NormalizedEvent is truncated to
   8192 characters when doc JSON exceeds 8KB

7. test_normalize_alert_returns_none_on_missing_ip — _normalize_alert() returns None when
   both source.ip and src_ip/srcip are absent (drop incomplete events)

File header docstring: "Stub tests for Phase 27 Malcolm field normalization (P27-T02). Activated in plan 27-03."
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_malcolm_normalizer.py -v 2>&1 | grep -E "(SKIPPED|PASSED|ERROR|FAILED|test_)"</automated>
  </verify>
  <done>All 7 stubs collected and reported as SKIPPED, 0 errors.</done>
</task>

<task type="auto">
  <name>Task 3: Create test_dispatch_endpoint.py stubs + verify full suite</name>
  <files>tests/unit/test_dispatch_endpoint.py</files>
  <action>
Create tests/unit/test_dispatch_endpoint.py with module-level pytestmark skip.

The dispatch endpoint lives in backend/api/recommendations.py which already exists.
Use the simple module-level skip (not try/except) because the import path exists:
  pytestmark = pytest.mark.skip(reason="wave-0 stub — activate in plan 27-04")

Stubs to create (all skipped):

1. test_dispatch_approved_recommendation_returns_200 — POST /api/recommendations/{id}/dispatch
   with an approved recommendation returns 200 with {"dispatched": true, "recommendation_id": id}

2. test_dispatch_non_approved_returns_409 — POST /api/recommendations/{id}/dispatch
   when status != "approved" returns 409 Conflict

3. test_dispatch_not_found_returns_404 — POST /api/recommendations/{id}/dispatch
   for unknown id returns 404

4. test_dispatch_schema_validation_failure_returns_422 — POST /api/recommendations/{id}/dispatch
   when RecommendationArtifact validation fails returns 422 with
   {"error": "schema_validation_failed", "detail": [...]}

5. test_dispatch_does_not_make_http_call — dispatch endpoint does NOT make any outbound
   HTTP calls (future phase — no firewall integration yet)

File header docstring: "Stub tests for Phase 27 recommendation dispatch endpoint (P27-T04). Activated in plan 27-04."

After creating all three stub files, run the full unit suite to confirm 0 failures:
  uv run pytest tests/unit/ -x -q
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_malcolm_collector.py tests/unit/test_malcolm_normalizer.py tests/unit/test_dispatch_endpoint.py -v 2>&1 | tail -5 && uv run pytest tests/unit/ -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>All 17 stubs (5 + 7 + 5) report SKIPPED. Full unit suite passes with 0 failures.</done>
</task>

</tasks>

<verification>
uv run pytest tests/unit/test_malcolm_collector.py tests/unit/test_malcolm_normalizer.py tests/unit/test_dispatch_endpoint.py -v
Expected: all stubs SKIPPED, 0 FAILED, 0 ERROR

uv run pytest tests/unit/ -x -q
Expected: no regressions in full suite
</verification>

<success_criteria>
- tests/unit/test_malcolm_collector.py exists with 5 SKIP stubs
- tests/unit/test_malcolm_normalizer.py exists with 7 SKIP stubs
- tests/unit/test_dispatch_endpoint.py exists with 5 SKIP stubs
- uv run pytest tests/unit/ -x -q reports 0 failures
</success_criteria>

<output>
After completion, create .planning/phases/27-malcolm-nsm-integration-and-live-feed-collector/27-00-SUMMARY.md
</output>
