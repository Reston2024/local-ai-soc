---
phase: 27-malcolm-nsm-integration-and-live-feed-collector
plan: "06"
type: execute
wave: 4
depends_on: ["27-02", "27-03", "27-04", "27-05"]
files_modified:
  - scripts/e2e-malcolm-verify.ps1
autonomous: false
requirements:
  - P27-T06

must_haves:
  truths:
    - "scripts/e2e-malcolm-verify.ps1 exists and parses without PowerShell errors"
    - "Script triggers a Suricata alert via SSH to root@192.168.1.1 (IPFire)"
    - "Script polls GET /api/events until alert appears or 3min timeout"
    - "Script reports PASS/FAIL with elapsed time and timestamp"
    - "Live alert appears in local-ai-soc within 2 x MALCOLM_POLL_INTERVAL after reaching OpenSearch"
    - "Alert is visible in the Svelte dashboard Detections/Events view"
  artifacts:
    - path: "scripts/e2e-malcolm-verify.ps1"
      provides: "End-to-end alert pipeline verification script"
  key_links:
    - from: "scripts/e2e-malcolm-verify.ps1"
      to: "GET http://localhost:8000/api/events"
      via: "Invoke-RestMethod polling loop"
      pattern: "Invoke-RestMethod.*api/events"
    - from: "IPFire (192.168.1.1)"
      to: "Malcolm OpenSearch (192.168.1.22:9200)"
      via: "Suricata → EVE → SCP → Malcolm → OpenSearch"
      pattern: "testmynids\\.org"
---

<objective>
Create scripts/e2e-malcolm-verify.ps1 and run the live end-to-end alert pipeline test.

Purpose: With MalcolmCollector running and OpenSearch exposed, verify that a live Suricata alert
triggered on IPFire (192.168.1.1) travels through the full pipeline: IPFire → Suricata EVE →
Malcolm SCP → Malcolm OpenSearch → MalcolmCollector poll → local-ai-soc DuckDB → GET /api/events.

This is the final integration gate for Phase 27. The script automates the trigger and polling;
the human checkpoint confirms the alert appears in the Svelte dashboard.

Output: scripts/e2e-malcolm-verify.ps1 + human confirmation that alert appears end-to-end.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-malcolm-nsm-integration-and-live-feed-collector/27-CONTEXT.md
@.planning/phases/27-malcolm-nsm-integration-and-live-feed-collector/27-02-SUMMARY.md
@.planning/phases/27-malcolm-nsm-integration-and-live-feed-collector/27-03-SUMMARY.md

<interfaces>
<!-- Alert trigger from CONTEXT.md (locked) -->
SSH target:       root@192.168.1.1  (IPFire firewall)
Alert trigger:    curl -s http://testmynids.org/uid/index.html > /dev/null
Pipeline wait:    90 seconds (Suricata → EVE → SCP → Malcolm → OpenSearch)
Poll endpoint:    GET http://localhost:8000/api/events?source_type=suricata_eve&limit=10
Poll interval:    15 seconds
Timeout:          180 seconds (3 minutes)

Verification gate: Alert timestamp > trigger timestamp and source_type == "suricata_eve"

<!-- API events endpoint (existing) -->
GET /api/events?source_type=suricata_eve&limit=10
Returns: {"items": [...], "total": N}
Each item has: timestamp, source_type, event_type, severity, detection_source

<!-- Health check endpoint -->
GET http://localhost:8000/health
Returns: {"status": "healthy", ...}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write scripts/e2e-malcolm-verify.ps1</name>
  <files>scripts/e2e-malcolm-verify.ps1</files>
  <action>
Create scripts/e2e-malcolm-verify.ps1 with the following implementation:

```powershell
#Requires -Version 5.1
<#
.SYNOPSIS
    End-to-end verification of the Malcolm NSM → local-ai-soc alert pipeline.

.DESCRIPTION
    Triggers a known-bad Suricata alert by curling testmynids.org from IPFire,
    waits for the alert to propagate through Malcolm → OpenSearch → MalcolmCollector,
    then polls the local-ai-soc events API until the alert appears or a 3-minute
    timeout is reached.

    Requires:
    - local-ai-soc running on localhost:8000 with MALCOLM_ENABLED=True
    - Malcolm running on 192.168.1.22 with port 9200 exposed
    - SSH access to root@192.168.1.1 (IPFire)
    - SSH access to opsadmin@192.168.1.22 (Malcolm)

.PARAMETER ApiBase
    Base URL of local-ai-soc API. Default: http://localhost:8000

.PARAMETER PollIntervalSeconds
    Seconds between event API polls. Default: 15

.PARAMETER TimeoutSeconds
    Max seconds to wait for alert to appear. Default: 180

.EXAMPLE
    .\e2e-malcolm-verify.ps1
    .\e2e-malcolm-verify.ps1 -TimeoutSeconds 300

.NOTES
    Phase 27 P27-T06 — end-to-end pipeline verification.
#>

[CmdletBinding()]
param(
    [string]$ApiBase          = "http://localhost:8000",
    [int]$PollIntervalSeconds = 15,
    [int]$TimeoutSeconds      = 180
)

Set-StrictMode -Version Latest
$ErrorActionPreference = "Stop"

$IPFireHost    = "root@192.168.1.1"
$MalcolmHost   = "opsadmin@192.168.1.22"
$TestUrl       = "http://testmynids.org/uid/index.html"

Write-Host "=== Malcolm NSM End-to-End Verification ===" -ForegroundColor Cyan
Write-Host "API base:        $ApiBase"
Write-Host "Poll interval:   $PollIntervalSeconds s"
Write-Host "Timeout:         $TimeoutSeconds s"
Write-Host ""

# ---------------------------------------------------------------------------
# Step 1: Verify local-ai-soc is healthy
# ---------------------------------------------------------------------------
Write-Host "[1/5] Checking local-ai-soc health..." -ForegroundColor Cyan
try {
    $health = Invoke-RestMethod -Uri "$ApiBase/health" -Method GET -TimeoutSec 10
    Write-Host "  Status: $($health.status)" -ForegroundColor Green
} catch {
    Write-Host "ERROR: local-ai-soc is not reachable at $ApiBase/health" -ForegroundColor Red
    Write-Host "  Start with: uv run uvicorn backend.main:create_app --factory"
    exit 1
}

# ---------------------------------------------------------------------------
# Step 2: Verify MalcolmCollector is running (check collector status)
# ---------------------------------------------------------------------------
Write-Host ""
Write-Host "[2/5] Checking MalcolmCollector status..." -ForegroundColor Cyan
try {
    $collectorStatus = Invoke-RestMethod -Uri "$ApiBase/api/telemetry/malcolm/status" -Method GET -TimeoutSec 10 -ErrorAction SilentlyContinue
    if ($collectorStatus.running) {
        Write-Host "  MalcolmCollector: running" -ForegroundColor Green
        Write-Host "  Alerts ingested so far: $($collectorStatus.alerts_ingested)"
    } else {
        Write-Host "  WARNING: MalcolmCollector status endpoint unavailable or not running." -ForegroundColor Yellow
        Write-Host "  Continuing — will verify by checking events after trigger."
    }
} catch {
    Write-Host "  (No collector status endpoint — continuing)" -ForegroundColor Yellow
}

# ---------------------------------------------------------------------------
# Step 3: Trigger Suricata alert from IPFire
# ---------------------------------------------------------------------------
Write-Host ""
Write-Host "[3/5] Triggering Suricata alert via IPFire ($IPFireHost)..." -ForegroundColor Cyan
$TriggerTimestamp = (Get-Date).ToUniversalTime()
Write-Host "  Trigger time (UTC): $($TriggerTimestamp.ToString('o'))"

$sshTrigger = & ssh.exe $IPFireHost "curl -s $TestUrl > /dev/null 2>&1 && echo TRIGGERED"
if ($LASTEXITCODE -ne 0) {
    Write-Host "ERROR: SSH to IPFire failed. Cannot trigger alert." -ForegroundColor Red
    Write-Host "  Check SSH access: ssh $IPFireHost"
    exit 1
}
Write-Host "  Alert trigger: $TestUrl" -ForegroundColor Green
Write-Host "  SSH result: $sshTrigger"

# ---------------------------------------------------------------------------
# Step 4: Wait for pipeline propagation
# ---------------------------------------------------------------------------
$PipelineWaitSeconds = 90
Write-Host ""
Write-Host "[4/5] Waiting $PipelineWaitSeconds s for pipeline propagation..." -ForegroundColor Cyan
Write-Host "  (Suricata → EVE → SCP → Malcolm → OpenSearch → MalcolmCollector)"
for ($i = $PipelineWaitSeconds; $i -gt 0; $i -= 10) {
    Write-Host "  $i seconds remaining..." -ForegroundColor DarkGray
    Start-Sleep -Seconds ([Math]::Min(10, $i))
}

# ---------------------------------------------------------------------------
# Step 5: Poll events API until alert appears
# ---------------------------------------------------------------------------
Write-Host ""
Write-Host "[5/5] Polling $ApiBase/api/events for new suricata_eve alerts..." -ForegroundColor Cyan
$StartPoll    = Get-Date
$AlertFound   = $false
$Elapsed      = 0

while ($Elapsed -lt $TimeoutSeconds) {
    try {
        $eventsResp = Invoke-RestMethod `
            -Uri "$ApiBase/api/events?source_type=suricata_eve&limit=20" `
            -Method GET `
            -TimeoutSec 15

        $newAlerts = $eventsResp.items | Where-Object {
            $itemTs = [datetime]::Parse($_.timestamp)
            $itemTs.ToUniversalTime() -gt $TriggerTimestamp
        }

        if ($newAlerts -and $newAlerts.Count -gt 0) {
            $AlertFound = $true
            Write-Host ""
            Write-Host "  ALERT FOUND: $($newAlerts.Count) new suricata_eve event(s)" -ForegroundColor Green
            $newAlerts | Select-Object -First 3 | ForEach-Object {
                Write-Host "    timestamp=$($_.timestamp) severity=$($_.severity) source=$($_.detection_source)"
            }
            break
        }
    } catch {
        Write-Host "  Poll error: $_" -ForegroundColor Yellow
    }

    $Elapsed = [int]((Get-Date) - $StartPoll).TotalSeconds
    Write-Host "  No new alerts yet. Elapsed: $Elapsed s / $TimeoutSeconds s"
    if ($Elapsed -lt $TimeoutSeconds) {
        Start-Sleep -Seconds $PollIntervalSeconds
    }
}

$TotalElapsed = [int]((Get-Date) - $TriggerTimestamp).TotalSeconds

# ---------------------------------------------------------------------------
# Result
# ---------------------------------------------------------------------------
Write-Host ""
Write-Host "=== RESULT ===" -ForegroundColor Cyan
if ($AlertFound) {
    Write-Host "PASS: Alert appeared in local-ai-soc within $TotalElapsed seconds." -ForegroundColor Green
    Write-Host ""
    Write-Host "Next step: Open Svelte dashboard and navigate to Events/Detections view."
    Write-Host "Confirm the suricata_eve alert is visible with correct source and timestamp."
    exit 0
} else {
    Write-Host "FAIL: No new suricata_eve alerts appeared within $TimeoutSeconds seconds (plus $PipelineWaitSeconds s wait)." -ForegroundColor Red
    Write-Host ""
    Write-Host "Troubleshooting checklist:"
    Write-Host "  1. Is MALCOLM_ENABLED=True in .env and local-ai-soc restarted?"
    Write-Host "  2. Is port 9200 accessible: curl -sk -u admin:Adam1000! https://192.168.1.22:9200/_cluster/health"
    Write-Host "  3. Did Suricata actually fire? SSH to $MalcolmHost and check:"
    Write-Host "     curl -sk -u admin:Adam1000! https://localhost:9200/arkime_sessions3-*/_count"
    Write-Host "  4. Check MalcolmCollector logs: search for 'MalcolmCollector' in application logs"
    exit 1
}
```
  </action>
  <verify>
    <automated>powershell -Command "& 'C:\Users\Admin\AI-SOC-Brain\scripts\e2e-malcolm-verify.ps1' -WhatIf" 2>&1 | head -5 ; powershell -NoProfile -Command "Get-Content 'C:\Users\Admin\AI-SOC-Brain\scripts\e2e-malcolm-verify.ps1' | Out-Null; Write-Host 'SYNTAX OK'"</automated>
  </verify>
  <done>scripts/e2e-malcolm-verify.ps1 exists and PowerShell parses it without syntax errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Created scripts/e2e-malcolm-verify.ps1 that:
    1. Checks local-ai-soc health
    2. Triggers a Suricata IDS alert via SSH to IPFire (testmynids.org curl)
    3. Waits 90 seconds for pipeline propagation
    4. Polls GET /api/events?source_type=suricata_eve every 15 seconds for 3 minutes
    5. Reports PASS/FAIL with elapsed time
  </what-built>
  <how-to-verify>
    Prerequisites:
    - local-ai-soc is running with MALCOLM_ENABLED=True in .env
    - MALCOLM_OPENSEARCH_PASS is set to "Adam1000!" in .env
    - local-ai-soc has been restarted after adding MALCOLM_* settings

    Run the verification script from the project root:
      powershell -File scripts/e2e-malcolm-verify.ps1

    Expected outcome (PASS):
      - "[3/5] Triggering Suricata alert" step completes (TRIGGERED)
      - "[5/5] Polling..." step finds at least 1 new suricata_eve event
      - Script prints "PASS: Alert appeared in local-ai-soc within N seconds"

    Then open the Svelte dashboard at http://localhost:5173 (or wherever it's running):
      - Navigate to the Events or Detections view
      - Confirm you can see a suricata_eve alert with source = "testmynids.org" or similar
      - Confirm the timestamp is recent (matches the trigger time)

    If the script reports FAIL, use the troubleshooting checklist it prints.
    Common fix: restart local-ai-soc with MALCOLM_ENABLED=True, wait 30 seconds, re-run.
  </how-to-verify>
  <resume-signal>Type "approved" when the alert appears end-to-end (script PASS + visible in dashboard), or describe what failed.</resume-signal>
</task>

</tasks>

<verification>
powershell -NoProfile -Command "Get-Content 'scripts/e2e-malcolm-verify.ps1' | Out-Null; Write-Host 'SYNTAX OK'"
Expected: "SYNTAX OK" (no parse errors)

Manual (live): Run scripts/e2e-malcolm-verify.ps1 — expect PASS output within 4 minutes.

Dashboard: Navigate to Events view — confirm suricata_eve alert with timestamp > trigger time.
</verification>

<success_criteria>
- scripts/e2e-malcolm-verify.ps1 exists and has no PowerShell syntax errors
- Script correctly triggers alert via IPFire SSH and polls the events API
- At least 1 suricata_eve event with timestamp after trigger time appears in the API
- Alert is visible in the Svelte dashboard Events/Detections view
- Human reviewer types "approved" confirming end-to-end pipeline works
</success_criteria>

<output>
After completion, create .planning/phases/27-malcolm-nsm-integration-and-live-feed-collector/27-06-SUMMARY.md
</output>
