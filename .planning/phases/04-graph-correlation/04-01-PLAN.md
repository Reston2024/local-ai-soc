---
phase: 04-graph-correlation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/tests/test_phase4.py
autonomous: true
requirements:
  - FR-4.1
  - FR-4.2
  - FR-4.3
  - FR-4.4
  - FR-4.5
must_haves:
  truths:
    - "test_phase4.py exists and all 8 test classes are importable"
    - "All stubs are marked xfail with descriptive reasons so the file runs without error"
    - "All 41 existing tests still pass after stub file is added"
  artifacts:
    - path: "backend/src/tests/test_phase4.py"
      provides: "Phase 4 test stubs — 8 classes, all xfail until implementation"
      min_lines: 80
  key_links:
    - from: "backend/src/tests/test_phase4.py"
      to: "backend/src/api/main.py"
      via: "TestClient(app) import"
      pattern: "from backend.src.api.main import app"
---

<objective>
Wave 0: Create test_phase4.py with all 8 xfail stub classes before any implementation begins.

Purpose: Establishes the red baseline. Every subsequent plan flips stubs from xfail to passing. Prevents executor from implementing untested code paths.
Output: backend/src/tests/test_phase4.py with 8 test classes, all xfail, full suite still green.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-graph-correlation/04-CONTEXT.md
@.planning/phases/04-graph-correlation/04-RESEARCH.md
</context>

<interfaces>
<!-- Current test patterns — match exactly -->

From backend/src/tests/test_phase3.py:
```python
from fastapi.testclient import TestClient
from backend.src.api.main import app
client = TestClient(app)
# Per-class structure: class TestXxx:  def test_yyy(self): ...
# pytest.importorskip used inside methods for optional imports
```

From backend/src/api/models.py (current — will change in Plan 02):
```python
class GraphNode(BaseModel):
    id: str; type: str; label: str
class GraphEdge(BaseModel):
    id: str; source: str; target: str; type: str
class GraphResponse(BaseModel):
    nodes: list[GraphNode]; edges: list[GraphEdge]
```

_alerts shape (from routes.py line 64):
```python
_alerts.extend(a.model_dump() for a in new_alerts)
# Keys: id, timestamp, rule, severity, event_id, description
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create test_phase4.py with 8 xfail stub classes</name>
  <files>backend/src/tests/test_phase4.py</files>
  <action>
Create backend/src/tests/test_phase4.py. Do NOT import from builder.py or models.py at module level — only inside test methods, to avoid import errors before Phase 02 changes.

File structure:
1. Imports: pytest, TestClient, app (module-level — these exist already)
2. client = TestClient(app) — module-level
3. Eight test classes, each containing one test method marked @pytest.mark.xfail(strict=False, reason="..."):

Class TestGraphModels:
  test_graphnode_has_new_fields — import GraphNode from models; assert it has attributes, first_seen, last_seen, evidence fields

Class TestNodeExtraction:
  test_extract_nodes_emits_host_node — import build_graph from builder; call build_graph([{"host":"testhost","event_type":"dns","id":"e1","timestamp":"2026-01-01T00:00:00Z"}], []); assert any n.id == "host:testhost"

Class TestEdgeExtraction:
  test_extract_edges_emits_connection_edge — call build_graph with a connection event (src_ip+dst_ip); assert len(response.edges) >= 1 and edges[0].src is not None (tests src field exists)

Class TestCorrelation:
  test_correlate_dns_chain — build_graph with dns_query event at T=0 + connection event at T+30s same host same domain→IP; assert any e.type == "related_event" in edges

Class TestAttackPaths:
  test_group_attack_paths_returns_list — call build_graph with 2 connected events; assert isinstance(response.attack_paths, list)

Class TestGraphAPI:
  test_get_graph_has_attack_paths_and_stats — client.get("/graph"); r.status_code == 200; "attack_paths" in r.json() and "stats" in r.json()

Class TestAlertGraph:
  test_alert_trigger_edge_created — POST an event that fires an alert (severity=critical), then GET /graph; assert any edge with type "alert_trigger" in response.edges (parsed from JSON)

Class TestCorrelateRoute:
  test_get_graph_correlate_returns_200 — POST an event, capture its id, GET /graph/correlate?event_id={id}; assert r.status_code == 200
  test_get_graph_correlate_unknown_returns_404 — GET /graph/correlate?event_id=nonexistent-id; assert r.status_code == 404

All test methods must use @pytest.mark.xfail(strict=False, reason="Phase 4 Plan 02/03 not yet implemented") decorator.

The file must import successfully with the current (unmodified) codebase. Use pytest.importorskip inside test methods if importing from builder inside the test could fail. Keep module-level imports to: pytest, TestClient, app only.
  </action>
  <verify>
    <automated>uv run pytest backend/src/tests/test_phase4.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>
- test_phase4.py exists and pytest collects it without import errors
- All test methods show as xfail (x) or xpass — none ERROR or FAILED
- uv run pytest backend/src/tests/ -x -q shows all prior 41 tests still pass (the new xfail tests add to the count but don't fail the suite)
  </done>
</task>

</tasks>

<verification>
uv run pytest backend/src/tests/ -q 2>&1 | tail -10
Expected: previously-passing tests still pass; new stubs show as xfail (counted but not failures).
</verification>

<success_criteria>
- backend/src/tests/test_phase4.py created with 8 test classes
- All stubs xfail (strict=False) — file runs clean
- Full test suite: uv run pytest backend/src/tests/ -x -q passes (41 prior tests green, new stubs xfail not counted as failures)
</success_criteria>

<output>
After completion, create .planning/phases/04-graph-correlation/04-01-SUMMARY.md
</output>
