---
phase: 48-hayabusa-evtx-threat-hunting-integration
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/unit/test_hayabusa_scanner.py
  - tests/integration/test_hayabusa_e2e.py
autonomous: true
requirements:
  - HAY-01
  - HAY-02
  - HAY-03
  - HAY-04
  - HAY-05
  - HAY-06
  - HAY-08

must_haves:
  truths:
    - "6 unit test stubs for HAY-01 through HAY-06 exist and SKIP cleanly (importorskip pattern)"
    - "1 integration test stub for HAY-08 exists and is skipped when hayabusa binary absent"
    - "Full unit suite remains green with zero regressions"
  artifacts:
    - path: "tests/unit/test_hayabusa_scanner.py"
      provides: "Unit test stubs covering record mapping, level normalization, MITRE tag filtering, no-binary skip, dedup skip, migration idempotence"
      min_lines: 60
    - path: "tests/integration/test_hayabusa_e2e.py"
      provides: "Integration test stub gated on shutil.which('hayabusa')"
      min_lines: 20
  key_links:
    - from: "tests/unit/test_hayabusa_scanner.py"
      to: "ingestion.hayabusa_scanner"
      via: "pytest.importorskip"
      pattern: "importorskip.*hayabusa"
    - from: "tests/integration/test_hayabusa_e2e.py"
      to: "shutil.which"
      via: "pytest.mark.skipif"
      pattern: "shutil.which.*hayabusa"
---

<objective>
Wave 0: Write failing test stubs for all HAY-01 through HAY-06 requirements and the HAY-08 integration test. All stubs SKIP cleanly because the implementation modules do not yet exist. This defines the contracts that Plan 02 must satisfy.

Purpose: Establish the RED phase before writing any implementation. Guarantees Plan 02 has clear pass criteria and prevents scope drift.
Output: 7 test stubs (6 unit + 1 integration) that SKIP cleanly; full unit suite still green.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-hayabusa-evtx-threat-hunting-integration/48-RESEARCH.md
@.planning/phases/48-hayabusa-evtx-threat-hunting-integration/48-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write unit test stubs — HAY-01 through HAY-06</name>
  <files>tests/unit/test_hayabusa_scanner.py</files>
  <behavior>
    - test_record_mapping: hayabusa_record_to_detection(sample_record) returns a DetectionRecord with rule_id starting with "hayabusa-", correct rule_name, correct severity, and attack_technique matching a T#### entry (HAY-01)
    - test_level_normalization: _LEVEL_MAP maps "crit"→"critical", "high"→"high", "med"→"medium", "medium"→"medium", "low"→"low", "info"→"informational"; unknown values default to "medium" (HAY-02)
    - test_mitre_tag_filter: MitreTags ["T1059.001", "G0016", "S0002", "T1078"] → attack_technique == "T1059.001"; G#### and S#### entries are excluded (HAY-03)
    - test_no_binary: when HAYABUSA_BIN is None/empty, scan_evtx("any.evtx") yields zero records and does not raise (HAY-04)
    - test_dedup_skip: HayabusaScanner.scan() with a SHA-256 already in hayabusa_scanned_files returns 0 findings without calling subprocess (HAY-05)
    - test_migration_idempotent: calling SQLiteStore._run_migrations() twice on the same in-memory SQLite db does not raise; detection_source column exists after both calls (HAY-06)
  </behavior>
  <action>
    Create tests/unit/test_hayabusa_scanner.py with pytestmark = pytest.mark.unit.

    Use pytest.importorskip at the TOP of the file (module level, not inside functions) to skip the entire file if ingestion.hayabusa_scanner does not exist yet:

    ```python
    pytest.importorskip("ingestion.hayabusa_scanner")
    ```

    This is the same importorskip pattern used in Phase 44 (test_feedback_classifier.py) and Phase 45 (test_agent_tools.py). It lets the stubs be defined without import errors, and all tests SKIP if the module is absent.

    Write all 6 test functions using `pytest.mark.skip(reason="Wave 0 stub — implementation pending")` decorator on each individual test (NOT module-level pytestmark skip — the importorskip at module level handles the absent-module case; the per-test skip ensures they don't accidentally run before Plan 02).

    Each test body should contain only `pytest.skip("Wave 0 stub")` or a `pass` after the skip decorator.

    Test function names must match the VALIDATION.md exactly:
    - test_record_mapping
    - test_level_normalization
    - test_mitre_tag_filter
    - test_no_binary
    - test_dedup_skip
    - test_migration_idempotent

    The behavior docstrings (from the `<behavior>` block above) should appear as the test function's docstring so Plan 02's executor knows what the test is asserting.

    Do NOT import from backend.stores.sqlite_store in this file — keep it focused on ingestion.hayabusa_scanner only. The migration test (test_migration_idempotent) can import sqlite_store separately inside the test body.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_hayabusa_scanner.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>All 6 tests in test_hayabusa_scanner.py are collected and show as SKIP (not ERROR, not FAIL). `uv run pytest tests/unit/ -x` passes with no regressions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write integration test stub — HAY-08</name>
  <files>tests/integration/test_hayabusa_e2e.py</files>
  <behavior>
    - test_hayabusa_e2e_scan: with a real hayabusa binary on PATH and a sample EVTX file, HayabusaScanner.scan() returns >= 0 findings (no crash); if binary absent, test is skipped automatically (HAY-08)
  </behavior>
  <action>
    Create tests/integration/test_hayabusa_e2e.py.

    Gate the entire test on binary availability using pytest.mark.skipif at module level:

    ```python
    import shutil
    import pytest

    pytestmark = pytest.mark.hayabusa

    HAYABUSA_AVAILABLE = shutil.which("hayabusa") or shutil.which("hayabusa.exe")

    @pytest.mark.skipif(not HAYABUSA_AVAILABLE, reason="hayabusa binary not on PATH")
    def test_hayabusa_e2e_scan():
        pytest.skip("Wave 0 stub — implement after Plan 02")
    ```

    The `pytestmark = pytest.mark.hayabusa` marker ensures the test is only collected when explicitly requested via `-m hayabusa` or during full integration runs.

    Also add `hayabusa` to pyproject.toml markers section if it is not already present (check `[tool.pytest.ini_options]` markers list first to avoid duplicates).

    The test body is a stub — just `pytest.skip(...)`. Plan 02 will fill in the real assertion: call HayabusaScanner with a fixture EVTX, verify findings >= 0, verify no exception.
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_hayabusa_e2e.py -v 2>&1 | tail -10</automated>
  </verify>
  <done>test_hayabusa_e2e.py is collected; test shows as SKIP (binary absent) or SKIP (Wave 0 stub). No ERROR. Full unit suite `uv run pytest tests/unit/ -x` still green.</done>
</task>

</tasks>

<verification>
After both tasks:
- `uv run pytest tests/unit/test_hayabusa_scanner.py -v` — all 6 tests SKIP, 0 errors
- `uv run pytest tests/integration/test_hayabusa_e2e.py -v` — 1 test SKIP, 0 errors
- `uv run pytest tests/unit/ -x` — all existing tests green, no regressions
</verification>

<success_criteria>
- tests/unit/test_hayabusa_scanner.py exists with 6 named stubs matching VALIDATION.md task IDs
- tests/integration/test_hayabusa_e2e.py exists with hayabusa marker and binary-check skipif
- Zero test regressions in the full unit suite
- importorskip pattern used (not try/except import) — consistent with Phase 44/45 pattern
</success_criteria>

<output>
After completion, create `.planning/phases/48-hayabusa-evtx-threat-hunting-integration/48-01-SUMMARY.md`
</output>
