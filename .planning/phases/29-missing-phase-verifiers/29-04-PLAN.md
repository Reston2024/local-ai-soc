---
phase: 29-missing-phase-verifiers
plan: 04
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [P29-T04]

must_haves:
  truths:
    - "VERIFICATION.md exists for Phase 18"
    - "VERIFICATION.md documents PDF report generation capability"
    - "VERIFICATION.md documents MITRE ATT&CK heatmap generation"
    - "VERIFICATION.md documents TheHive export endpoint"
    - "Status is passed or human_needed"
  artifacts:
    - path: ".planning/phases/18-reporting-compliance/18-VERIFICATION.md"
      provides: "Authoritative verification record for Phase 18"
  key_links:
    - from: "backend/api/export.py"
      to: "backend/services/report_generator.py"
      via: "Export routes call report service"
      pattern: "export|report|pdf|heatmap"
    - from: "backend/api/export.py"
      to: "TheHive export endpoint"
      via: "POST /api/export/thehive"
      pattern: "thehive|TheHive"
---

<objective>
Run the GSD verifier for Phase 18 (Reporting & Compliance) to produce an authoritative VERIFICATION.md.

Purpose: Phase 18 was GSD-executed but the verifier was never run. The phase delivered PDF report generation, MITRE ATT&CK heatmap output, and TheHive export. All confirmed working via integration check in the milestone audit.

Output: `.planning/phases/18-reporting-compliance/18-VERIFICATION.md`
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-reporting-compliance/18-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run GSD verifier for Phase 18 — Reporting & Compliance</name>
  <files>.planning/phases/18-reporting-compliance/18-VERIFICATION.md</files>
  <action>
    Spawn a gsd-verifier agent for Phase 18 (18-reporting-compliance).

    The verifier must:
    1. Read all PLAN.md and SUMMARY.md files in `.planning/phases/18-reporting-compliance/`
    2. Read the Phase 18 goal from ROADMAP.md
    3. Check the codebase for the following Phase 18 deliverables:
       - `backend/api/export.py` — Export routes (PDF report, MITRE heatmap, TheHive export)
       - Report generation service or utility (check `backend/services/` for report_generator or similar)
       - MITRE ATT&CK heatmap generation (check for navigator layer JSON output)
       - TheHive export endpoint (`POST /api/export/thehive` or similar)
       - Any PDF generation library usage (weasyprint, reportlab, fpdf, or similar)
    4. Run automated checks:
       - `uv run pytest tests/ -k "report or export or heatmap or thehive" -x -q 2>/dev/null || echo "no report tests"`
       - Import check: `.venv/Scripts/python.exe -c "from backend.api.export import router; print('export router OK')" 2>/dev/null || echo "export import check failed"`
       - Check route registration: grep for export router in `backend/main.py`
    5. Milestone audit confirmed reports and export working via integration check. Status should be `passed` if artifacts exist, `human_needed` if a live PDF render or TheHive connection is required for full validation.
    6. Write `18-VERIFICATION.md` using the standard GSD verifier output format.
  </action>
  <verify>
    <automated>test -f .planning/phases/18-reporting-compliance/18-VERIFICATION.md && echo "VERIFICATION.md created" || echo "MISSING"</automated>
  </verify>
  <done>18-VERIFICATION.md exists with status: passed or human_needed, documenting PDF report, MITRE heatmap, and TheHive export artifacts</done>
</task>

</tasks>

<verification>
- `18-VERIFICATION.md` exists in `.planning/phases/18-reporting-compliance/`
- File contains `status: passed` or `status: human_needed`
- File documents the export route artifacts and report generation capability
- TheHive and MITRE heatmap deliverables explicitly noted
</verification>

<success_criteria>
VERIFICATION.md created for Phase 18 with authoritative status. Gap in milestone audit closed for P29-T04.
</success_criteria>

<output>
After completion, create `.planning/phases/29-missing-phase-verifiers/29-04-SUMMARY.md`
</output>
