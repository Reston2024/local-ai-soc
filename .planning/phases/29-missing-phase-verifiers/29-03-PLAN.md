---
phase: 29-missing-phase-verifiers
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [P29-T03]

must_haves:
  truths:
    - "VERIFICATION.md exists for Phase 23"
    - "VERIFICATION.md documents IPFire syslog parser and Suricata EVE JSON parser as implemented"
    - "VERIFICATION.md documents FirewallCollector or equivalent collector for scheduled ingestion"
    - "Status is passed or human_needed"
  artifacts:
    - path: ".planning/phases/23-firewall-telemetry-ingestion/23-VERIFICATION.md"
      provides: "Authoritative verification record for Phase 23"
  key_links:
    - from: "ingestion/parsers/ipfire_syslog.py"
      to: "ingestion/normalizer.py"
      via: "IpfireSyslogParser normalizes raw syslog lines to NormalizedEvent"
      pattern: "IpfireSyslogParser|ipfire"
    - from: "ingestion/parsers/suricata_eve.py"
      to: "ingestion/normalizer.py"
      via: "SuricataEveParser normalizes Suricata EVE JSON to NormalizedEvent"
      pattern: "SuricataEveParser|suricata"
---

<objective>
Run the GSD verifier for Phase 23 (Firewall Telemetry Ingestion) to produce an authoritative VERIFICATION.md.

Purpose: Phase 23 was GSD-executed but the verifier was never run. The phase delivered IPFire syslog and Suricata EVE JSON parsers, plus a firewall collector for scheduled ingestion.

Output: `.planning/phases/23-firewall-telemetry-ingestion/23-VERIFICATION.md`
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-firewall-telemetry-ingestion/23-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run GSD verifier for Phase 23 — Firewall Telemetry Ingestion</name>
  <files>.planning/phases/23-firewall-telemetry-ingestion/23-VERIFICATION.md</files>
  <action>
    Spawn a gsd-verifier agent for Phase 23 (23-firewall-telemetry-ingestion).

    The verifier must:
    1. Read all PLAN.md and SUMMARY.md files in `.planning/phases/23-firewall-telemetry-ingestion/`
    2. Read the Phase 23 goal from ROADMAP.md
    3. Check the codebase for the following Phase 23 deliverables:
       - `ingestion/parsers/ipfire_syslog.py` — IpfireSyslogParser class
       - `ingestion/parsers/suricata_eve.py` — SuricataEveParser class (may be combined with JSON parser)
       - Firewall collector script (check `scripts/collectors/` for ipfire or firewall collector)
       - Test fixtures for IPFire syslog and Suricata EVE in `fixtures/`
       - Integration of parsers into the collector scheduling system
    4. Run automated checks:
       - `uv run pytest tests/ -k "ipfire or suricata or firewall" -x -q 2>/dev/null || echo "no firewall tests"`
       - Import checks for parser classes:
         ```
         .venv/Scripts/python.exe -c "from ingestion.parsers.ipfire_syslog import IpfireSyslogParser; print('ipfire OK')" 2>/dev/null || echo "ipfire import failed"
         .venv/Scripts/python.exe -c "from ingestion.parsers.suricata_eve import SuricataEveParser; print('suricata OK')" 2>/dev/null || echo "suricata import failed — check json_ndjson.py"
         ```
       - Note from milestone audit: IpfireSyslogParser and SuricataEveParser are not registered in the file-upload parser registry (only available via collector jobs) — document this as a known tech debt item, not a blocker.
    5. Set status to `passed` if parser implementations exist and tests pass. Set `human_needed` if live IPFire/Suricata hardware is required for full validation.
    6. Write `23-VERIFICATION.md` using the standard GSD verifier output format.
  </action>
  <verify>
    <automated>test -f .planning/phases/23-firewall-telemetry-ingestion/23-VERIFICATION.md && echo "VERIFICATION.md created" || echo "MISSING"</automated>
  </verify>
  <done>23-VERIFICATION.md exists with status: passed or human_needed, documenting IPFire syslog and Suricata EVE parser implementation and any known parser registry limitation</done>
</task>

</tasks>

<verification>
- `23-VERIFICATION.md` exists in `.planning/phases/23-firewall-telemetry-ingestion/`
- File contains `status: passed` or `status: human_needed`
- File documents IpfireSyslogParser and SuricataEveParser artifact existence
- Known tech debt (parser registry exclusion) noted if applicable
</verification>

<success_criteria>
VERIFICATION.md created for Phase 23 with authoritative status. Gap in milestone audit closed for P29-T03.
</success_criteria>

<output>
After completion, create `.planning/phases/29-missing-phase-verifiers/29-03-SUMMARY.md`
</output>
