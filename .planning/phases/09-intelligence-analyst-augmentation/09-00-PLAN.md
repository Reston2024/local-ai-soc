---
phase: 09-intelligence-analyst-augmentation
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/unit/test_risk_scorer.py
  - tests/unit/test_anomaly_rules.py
  - tests/unit/test_explain_engine.py
  - tests/unit/test_score_api.py
  - tests/unit/test_explain_api.py
  - tests/unit/test_top_threats_api.py
  - tests/unit/test_sqlite_store.py
autonomous: true
requirements: [P9-T01, P9-T02, P9-T03, P9-T04, P9-T05, P9-T06, P9-T07, P9-T08, P9-T09, P9-T10]

must_haves:
  truths:
    - "All 7 test stub files exist and are importable"
    - "pytest collects all stubs without ImportError"
    - "Every stub test is marked xfail so it fails expectedly without blocking the suite"
    - "Existing 66+ unit tests continue to pass (no regressions)"
  artifacts:
    - path: "tests/unit/test_risk_scorer.py"
      provides: "xfail stubs for P9-T01, P9-T02, P9-T08 (score_entity, MITRE weights, node data)"
    - path: "tests/unit/test_anomaly_rules.py"
      provides: "xfail stubs for P9-T03 (office_spawns_shell, masquerading, unusual_port)"
    - path: "tests/unit/test_explain_engine.py"
      provides: "xfail stubs for P9-T07 (build_evidence_context, generate_explanation)"
    - path: "tests/unit/test_score_api.py"
      provides: "xfail stubs for P9-T04 (POST /api/score 200)"
    - path: "tests/unit/test_explain_api.py"
      provides: "xfail stubs for P9-T05 (POST /api/explain 200)"
    - path: "tests/unit/test_top_threats_api.py"
      provides: "xfail stubs for P9-T06 (GET /api/top-threats 200)"
    - path: "tests/unit/test_sqlite_store.py"
      provides: "TestSavedInvestigations class for P9-T09/T10 (save/retrieve snapshots)"
  key_links:
    - from: "tests/unit/test_risk_scorer.py"
      to: "backend/intelligence/risk_scorer.py"
      via: "from backend.intelligence.risk_scorer import score_entity"
      pattern: "xfail — module will not exist until Plan 01"
    - from: "tests/unit/test_score_api.py"
      to: "backend/api/score.py"
      via: "FastAPI TestClient with mocked intelligence module"
      pattern: "xfail — route will not exist until Plan 03"
---

<objective>
Create all Wave 0 test stub files for Phase 9 using pytest xfail markers. These stubs define the test contracts that implementation plans (01–06) must satisfy.

Purpose: Establish the TDD baseline. Every subsequent plan must turn xfail stubs green. No plan may remove or weaken a stub.
Output: 7 test files — all collected by pytest, all xfail, suite remains green.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-intelligence-analyst-augmentation/09-CONTEXT.md
@.planning/phases/09-intelligence-analyst-augmentation/09-VALIDATION.md

Existing test pattern reference — tests/unit/test_osquery_collector.py uses:
  import pytest
  pytestmark = pytest.mark.asyncio
  class TestOsqueryCollector:
      @pytest.mark.xfail(reason="P8-T01: ...", strict=True)
      def test_something(self): ...

pytest-asyncio mode=auto is set in pyproject.toml — no explicit asyncio_mode needed on classes.
xfail stubs must use strict=True so they fail expectedly (not silently pass).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create risk scorer + anomaly rules test stubs</name>
  <files>tests/unit/test_risk_scorer.py, tests/unit/test_anomaly_rules.py</files>
  <action>
Create tests/unit/test_risk_scorer.py with the following structure:

```python
import pytest

pytestmark = pytest.mark.unit


class TestScoreEntity:
    @pytest.mark.xfail(reason="P9-T01: backend/intelligence/risk_scorer.py not yet implemented", strict=True)
    def test_score_entity_returns_int_0_to_100(self):
        from backend.intelligence.risk_scorer import score_entity
        result = score_entity(
            entity_id="svchosts.exe",
            events=[{"severity": "critical", "attack_technique": "T1003.001"}],
            detections=[{"severity": "critical"}],
            anomaly_flags=["ANO-001"],
        )
        assert isinstance(result, int)
        assert 0 <= result <= 100

    @pytest.mark.xfail(reason="P9-T01: score_detection not yet implemented", strict=True)
    def test_score_detection_high_mitre_gives_high_score(self):
        from backend.intelligence.risk_scorer import score_detection
        result = score_detection(severity="critical", technique_id="T1003.001", anomaly_count=2)
        assert result >= 70

    @pytest.mark.xfail(reason="P9-T01: score_entity with no anomalies and low severity gives low score", strict=True)
    def test_score_entity_low_severity_no_anomalies(self):
        from backend.intelligence.risk_scorer import score_entity
        result = score_entity(
            entity_id="notepad.exe",
            events=[{"severity": "low", "attack_technique": None}],
            detections=[],
            anomaly_flags=[],
        )
        assert result < 40


class TestMitreWeights:
    @pytest.mark.xfail(reason="P9-T02: MITRE_WEIGHTS lookup not yet implemented", strict=True)
    def test_critical_technique_weight_is_40_or_more(self):
        from backend.intelligence.risk_scorer import MITRE_WEIGHTS
        assert MITRE_WEIGHTS.get("T1003.001", 0) >= 40

    @pytest.mark.xfail(reason="P9-T02: high technique weight", strict=True)
    def test_high_technique_weight_is_30(self):
        from backend.intelligence.risk_scorer import MITRE_WEIGHTS
        assert MITRE_WEIGHTS.get("T1059.001", 0) >= 25

    @pytest.mark.xfail(reason="P9-T02: unknown technique defaults to 0", strict=True)
    def test_unknown_technique_defaults_to_zero(self):
        from backend.intelligence.risk_scorer import MITRE_WEIGHTS
        assert MITRE_WEIGHTS.get("T9999.999", 0) == 0


class TestNodeData:
    @pytest.mark.xfail(reason="P9-T08: enrich_nodes_with_risk_score not yet implemented", strict=True)
    def test_enrich_nodes_adds_risk_score_field(self):
        from backend.intelligence.risk_scorer import enrich_nodes_with_risk_score
        nodes = [
            {"data": {"id": "svchosts.exe", "entity_type": "process", "severity": "critical"}}
        ]
        enriched = enrich_nodes_with_risk_score(nodes, scored_entities={"svchosts.exe": 85})
        assert enriched[0]["data"]["risk_score"] == 85

    @pytest.mark.xfail(reason="P9-T08: nodes without a score default to 0", strict=True)
    def test_enrich_nodes_defaults_missing_to_zero(self):
        from backend.intelligence.risk_scorer import enrich_nodes_with_risk_score
        nodes = [{"data": {"id": "explorer.exe", "entity_type": "process"}}]
        enriched = enrich_nodes_with_risk_score(nodes, scored_entities={})
        assert enriched[0]["data"]["risk_score"] == 0
```

Create tests/unit/test_anomaly_rules.py with the following structure:

```python
import pytest

pytestmark = pytest.mark.unit


class TestAnomalyRules:
    @pytest.mark.xfail(reason="P9-T03: backend/intelligence/anomaly_rules.py not yet implemented", strict=True)
    def test_office_spawns_shell_fires(self):
        from backend.intelligence.anomaly_rules import check_event_anomalies
        event = {"parent_process_name": "winword.exe", "process_name": "powershell.exe"}
        flags = check_event_anomalies(event)
        assert "ANO-001" in flags

    @pytest.mark.xfail(reason="P9-T03: normal process pair does not fire office_spawns_shell", strict=True)
    def test_normal_process_pair_no_flag(self):
        from backend.intelligence.anomaly_rules import check_event_anomalies
        event = {"parent_process_name": "explorer.exe", "process_name": "notepad.exe"}
        flags = check_event_anomalies(event)
        assert "ANO-001" not in flags

    @pytest.mark.xfail(reason="P9-T03: process masquerading in temp path flags ANO-003", strict=True)
    def test_process_masquerading_flags(self):
        from backend.intelligence.anomaly_rules import check_event_anomalies
        event = {
            "parent_process_name": "explorer.exe",
            "process_name": "svchost.exe",
            "process_path": "C:\\Users\\user\\AppData\\Local\\Temp\\svchost.exe",
        }
        flags = check_event_anomalies(event)
        assert "ANO-003" in flags

    @pytest.mark.xfail(reason="P9-T03: unusual external port flags ANO-004", strict=True)
    def test_unusual_external_port_flags(self):
        from backend.intelligence.anomaly_rules import check_event_anomalies
        event = {
            "dest_ip": "185.220.101.45",
            "dest_port": 4444,
        }
        flags = check_event_anomalies(event)
        assert "ANO-004" in flags

    @pytest.mark.xfail(reason="P9-T03: ANOMALY_RULES list is importable and non-empty", strict=True)
    def test_anomaly_rules_list_non_empty(self):
        from backend.intelligence.anomaly_rules import ANOMALY_RULES
        assert len(ANOMALY_RULES) >= 4
```
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_risk_scorer.py tests/unit/test_anomaly_rules.py -v --no-header 2>&amp;1 | tail -20</automated>
  </verify>
  <done>All tests in both files are collected and reported as xfail (x) — no ERROR, no unexpected PASS, no FAILED. Exit code 0.</done>
</task>

<task type="auto">
  <name>Task 2: Create explain engine + API endpoint test stubs</name>
  <files>tests/unit/test_explain_engine.py, tests/unit/test_score_api.py, tests/unit/test_explain_api.py, tests/unit/test_top_threats_api.py, tests/unit/test_sqlite_store.py</files>
  <action>
Create tests/unit/test_explain_engine.py:

```python
import pytest
from unittest.mock import AsyncMock, patch

pytestmark = pytest.mark.unit


class TestBuildEvidenceContext:
    @pytest.mark.xfail(reason="P9-T07: backend/intelligence/explain_engine.py not yet implemented", strict=True)
    def test_build_evidence_context_includes_detection_info(self):
        from backend.intelligence.explain_engine import build_evidence_context
        investigation = {
            "detection": {"rule_name": "Mimikatz", "severity": "critical", "attack_technique": "T1003.001"},
            "events": [],
            "techniques": [],
            "graph": {"elements": {"nodes": []}},
            "timeline": [],
        }
        ctx = build_evidence_context(investigation)
        assert "Mimikatz" in ctx
        assert "T1003.001" in ctx

    @pytest.mark.xfail(reason="P9-T07: build_evidence_context includes top N events", strict=True)
    def test_build_evidence_context_limits_events(self):
        from backend.intelligence.explain_engine import build_evidence_context
        investigation = {
            "detection": None,
            "events": [{"timestamp": f"2024-01-01T00:{i:02d}:00Z", "event_type": "process_create",
                         "process_name": f"proc{i}.exe", "hostname": "host1",
                         "severity": "low", "attack_technique": None} for i in range(20)],
            "techniques": [],
            "graph": {"elements": {"nodes": []}},
            "timeline": [],
        }
        ctx = build_evidence_context(investigation, max_events=5)
        # at most 5 EVENT: lines
        assert ctx.count("EVENT:") <= 5


class TestGenerateExplanation:
    @pytest.mark.xfail(reason="P9-T07: generate_explanation async not yet implemented", strict=True)
    async def test_generate_explanation_returns_three_sections(self):
        from backend.intelligence.explain_engine import generate_explanation
        mock_client = AsyncMock()
        mock_client.generate = AsyncMock(return_value=(
            "## What Happened\nFoo\n## Why It Matters\nBar\n## Recommended Next Steps\nBaz"
        ))
        investigation = {
            "detection": {"rule_name": "Test", "severity": "high", "attack_technique": "T1059.001"},
            "events": [], "techniques": [], "graph": {"elements": {"nodes": []}}, "timeline": [],
        }
        result = await generate_explanation(investigation, mock_client)
        assert "what_happened" in result
        assert "why_it_matters" in result
        assert "recommended_next_steps" in result
```

Create tests/unit/test_score_api.py:

```python
import pytest
from unittest.mock import patch

pytestmark = pytest.mark.unit


class TestScoreEndpoint:
    @pytest.mark.xfail(reason="P9-T04: POST /api/score endpoint not yet implemented", strict=True)
    def test_post_score_returns_200(self):
        from fastapi.testclient import TestClient
        from backend.main import create_app
        app = create_app()
        client = TestClient(app)
        with patch("backend.api.score.score_entity", return_value=75):
            resp = client.post("/api/score", json={"detection_id": "det-001"})
        assert resp.status_code == 200

    @pytest.mark.xfail(reason="P9-T04: POST /api/score returns structured JSON with scored_entities", strict=True)
    def test_post_score_returns_scored_entities(self):
        from fastapi.testclient import TestClient
        from backend.main import create_app
        app = create_app()
        client = TestClient(app)
        resp = client.post("/api/score", json={"event_ids": []})
        assert resp.status_code == 200
        body = resp.json()
        assert "scored_entities" in body

    @pytest.mark.xfail(reason="P9-T04: POST /api/score returns 200 not 404/500 on missing data", strict=True)
    def test_post_score_graceful_empty(self):
        from fastapi.testclient import TestClient
        from backend.main import create_app
        app = create_app()
        client = TestClient(app)
        resp = client.post("/api/score", json={})
        assert resp.status_code == 200
```

Create tests/unit/test_explain_api.py:

```python
import pytest
from unittest.mock import AsyncMock, patch

pytestmark = pytest.mark.unit


class TestExplainEndpoint:
    @pytest.mark.xfail(reason="P9-T05: POST /api/explain endpoint not yet implemented", strict=True)
    def test_post_explain_returns_200(self):
        from fastapi.testclient import TestClient
        from backend.main import create_app
        app = create_app()
        client = TestClient(app)
        resp = client.post("/api/explain", json={"detection_id": "det-001"})
        assert resp.status_code == 200

    @pytest.mark.xfail(reason="P9-T05: POST /api/explain returns three structured sections", strict=True)
    def test_post_explain_returns_sections(self):
        from fastapi.testclient import TestClient
        from backend.main import create_app
        app = create_app()
        client = TestClient(app)
        resp = client.post("/api/explain", json={"detection_id": "det-001"})
        assert resp.status_code == 200
        body = resp.json()
        assert "what_happened" in body or "explanation" in body

    @pytest.mark.xfail(reason="P9-T05: POST /api/explain returns 200 not 500 when Ollama unavailable", strict=True)
    def test_post_explain_graceful_ollama_error(self):
        from fastapi.testclient import TestClient
        from backend.main import create_app
        app = create_app()
        client = TestClient(app)
        with patch("backend.intelligence.explain_engine.generate_explanation",
                   side_effect=Exception("Ollama offline")):
            resp = client.post("/api/explain", json={"detection_id": "det-001"})
        assert resp.status_code == 200
```

Create tests/unit/test_top_threats_api.py:

```python
import pytest

pytestmark = pytest.mark.unit


class TestTopThreatsEndpoint:
    @pytest.mark.xfail(reason="P9-T06: GET /api/top-threats endpoint not yet implemented", strict=True)
    def test_get_top_threats_returns_200(self):
        from fastapi.testclient import TestClient
        from backend.main import create_app
        app = create_app()
        client = TestClient(app)
        resp = client.get("/api/top-threats")
        assert resp.status_code == 200

    @pytest.mark.xfail(reason="P9-T06: GET /api/top-threats returns list", strict=True)
    def test_get_top_threats_returns_list(self):
        from fastapi.testclient import TestClient
        from backend.main import create_app
        app = create_app()
        client = TestClient(app)
        resp = client.get("/api/top-threats")
        body = resp.json()
        assert "threats" in body
        assert isinstance(body["threats"], list)

    @pytest.mark.xfail(reason="P9-T06: GET /api/top-threats respects ?limit= query param", strict=True)
    def test_get_top_threats_limit_param(self):
        from fastapi.testclient import TestClient
        from backend.main import create_app
        app = create_app()
        client = TestClient(app)
        resp = client.get("/api/top-threats?limit=5")
        assert resp.status_code == 200
        body = resp.json()
        assert len(body.get("threats", [])) <= 5
```

Append a TestSavedInvestigations class to tests/unit/test_sqlite_store.py. First read the existing file to find where to append, then add at the end:

```python
class TestSavedInvestigations:
    @pytest.mark.xfail(reason="P9-T09: saved_investigations table not yet implemented in SQLiteStore", strict=True)
    def test_save_investigation_returns_id(self, tmp_path):
        from backend.stores.sqlite_store import SQLiteStore
        store = SQLiteStore(str(tmp_path / "test.sqlite3"))
        inv_id = store.save_investigation(
            detection_id="det-001",
            graph_snapshot={"nodes": [], "edges": []},
            metadata={"label": "APT test"},
        )
        assert inv_id is not None
        assert isinstance(inv_id, str)

    @pytest.mark.xfail(reason="P9-T09: list_saved_investigations not yet implemented", strict=True)
    def test_list_investigations_returns_saved(self, tmp_path):
        from backend.stores.sqlite_store import SQLiteStore
        store = SQLiteStore(str(tmp_path / "test.sqlite3"))
        store.save_investigation("det-001", {}, {})
        store.save_investigation("det-002", {}, {})
        results = store.list_saved_investigations()
        assert len(results) >= 2

    @pytest.mark.xfail(reason="P9-T09: get_saved_investigation not yet implemented", strict=True)
    def test_get_investigation_by_id(self, tmp_path):
        from backend.stores.sqlite_store import SQLiteStore
        store = SQLiteStore(str(tmp_path / "test.sqlite3"))
        inv_id = store.save_investigation("det-001", {"nodes": [1, 2]}, {"label": "test"})
        result = store.get_saved_investigation(inv_id)
        assert result is not None
        assert result["detection_id"] == "det-001"
        snapshot = result["graph_snapshot"]
        assert snapshot["nodes"] == [1, 2]
```

Read tests/unit/test_sqlite_store.py first to verify the file exists and confirm the structure before appending the class. If the file does not exist, create it with the necessary imports plus the class above.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_explain_engine.py tests/unit/test_score_api.py tests/unit/test_explain_api.py tests/unit/test_top_threats_api.py tests/unit/test_sqlite_store.py -v --no-header 2>&amp;1 | tail -30</automated>
  </verify>
  <done>All stub tests collected and reported xfail (x). No ERROR or FAILED. Full suite still passes: `uv run pytest tests/unit/ -q` exits 0.</done>
</task>

</tasks>

<verification>
`uv run pytest tests/unit/ -q --tb=short` exits 0.
Every new stub test shows as `x` (xfail), not `F` (failed) or `E` (error).
Existing test count (66+) unchanged — no regressions.
</verification>

<success_criteria>
- 7 test stub files exist (6 new + test_sqlite_store.py extended)
- `uv run pytest tests/unit/ -q` exits 0
- All 9 stub test classes visible with `pytest --collect-only`
- No existing test broken
</success_criteria>

<output>
After completion, create `.planning/phases/09-intelligence-analyst-augmentation/09-00-SUMMARY.md`
</output>
