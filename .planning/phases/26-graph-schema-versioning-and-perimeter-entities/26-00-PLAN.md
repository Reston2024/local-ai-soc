---
phase: 26-graph-schema-versioning-and-perimeter-entities
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/unit/test_graph_schema.py
  - tests/unit/test_graph_versioning.py
autonomous: true
requirements:
  - P26-T01
  - P26-T02
  - P26-T03
  - P26-T04

must_haves:
  truths:
    - "Running the phase-26 test files produces SKIP, not ERROR or PASS"
    - "test_graph_schema.py stubs cover P26-T02, P26-T03, P26-T04 constant assertions"
    - "test_graph_versioning.py stubs cover P26-T01 versioning migration and endpoint assertions"
  artifacts:
    - path: "tests/unit/test_graph_schema.py"
      provides: "Stubbed tests for schema constant additions and additive-only guard"
      exports: ["test_firewall_zone_in_entity_types", "test_network_segment_in_entity_types",
                "test_new_edge_types_present", "test_pre_existing_entity_types_preserved",
                "test_pre_existing_edge_types_preserved"]
    - path: "tests/unit/test_graph_versioning.py"
      provides: "Stubbed tests for system_kv seeding and schema-version endpoint"
      exports: ["test_fresh_install_version", "test_preexisting_install_version",
                "test_schema_version_endpoint"]
  key_links:
    - from: "tests/unit/test_graph_schema.py"
      to: "graph/schema.py"
      via: "import ENTITY_TYPES, EDGE_TYPES, is_valid_entity_type, is_valid_edge_type"
      pattern: "from graph\\.schema import"
    - from: "tests/unit/test_graph_versioning.py"
      to: "backend/stores/sqlite_store.py"
      via: "SQLiteStore(tmp_path) initialisation"
      pattern: "SQLiteStore"
---

<objective>
Create two pre-skipped test stub files that define the complete test surface for phase 26. All tests use `pytestmark = pytest.mark.skip(reason="wave-0 stub")` and will be activated in the final plan. Stub bodies assert the key behaviors without implementing any production code.

Purpose: Nyquist compliance — every requirement gets automated verification before implementation.
Output: tests/unit/test_graph_schema.py and tests/unit/test_graph_versioning.py, both skipped.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/26-graph-schema-versioning-and-perimeter-entities/26-VALIDATION.md

Existing test harness pattern (from test_graph_api.py) — use `_build_app(tmp_path)` with real
SQLiteStore for integration-style unit tests. pytest-asyncio mode: auto (pyproject.toml).
Module-level skip pattern from phase 25: `pytestmark = pytest.mark.skip(reason="wave-0 stub")`.

Schema constants location: graph/schema.py
  - ENTITY_TYPES: list[str] — 11 items (host, user, process, file, network_connection, domain, ip,
    detection, artifact, incident, attack_technique)
  - EDGE_TYPES: list[str] — 12 items (executed_by, ran_on, accessed, connected_to, resolved_to,
    triggered, maps_to, part_of, spawned, wrote, logged_into, related_to)
  - is_valid_entity_type(s: str) -> bool
  - is_valid_edge_type(s: str) -> bool
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create test_graph_schema.py stubs (P26-T02, P26-T03, P26-T04)</name>
  <files>tests/unit/test_graph_schema.py</files>
  <action>
Create tests/unit/test_graph_schema.py with module-level skip:

```python
pytestmark = pytest.mark.skip(reason="wave-0 stub — activate in plan 26-05")
```

Include these test functions (bodies use `assert False, "stub"` or `pytest.fail("stub")`):

1. test_firewall_zone_in_entity_types — asserts "firewall_zone" in ENTITY_TYPES and
   is_valid_entity_type("firewall_zone") is True (P26-T02)
2. test_network_segment_in_entity_types — asserts "network_segment" in ENTITY_TYPES and
   is_valid_entity_type("network_segment") is True (P26-T02)
3. test_new_edge_types_present — asserts all three of "blocks", "permits", "traverses" in
   EDGE_TYPES and is_valid_edge_type returns True for each (P26-T03)
4. test_pre_existing_entity_types_preserved — asserts all 11 original entity types
   (host, user, process, file, network_connection, domain, ip, detection, artifact,
   incident, attack_technique) still present in ENTITY_TYPES (P26-T04)
5. test_pre_existing_edge_types_preserved — asserts all 12 original edge types
   (executed_by, ran_on, accessed, connected_to, resolved_to, triggered, maps_to,
   part_of, spawned, wrote, logged_into, related_to) still present in EDGE_TYPES (P26-T04)
6. test_extract_perimeter_entities_blocks — given a mock NormalizedEvent with source_type
   "ipfire_syslog" and event_outcome "failure" (DROP_ prefix), asserts
   extract_perimeter_entities() returns at least one edge with edge_type "blocks" (P26-T03).
   Import path: `from ingestion.entity_extractor import extract_perimeter_entities`
   (this function does not exist yet — stub just needs the import + assert shape)
7. test_extract_perimeter_entities_permits — same but event_outcome "success" with
   FORWARDFW prefix, expects "permits" edge (P26-T03)

Imports needed:
  import pytest
  from graph.schema import ENTITY_TYPES, EDGE_TYPES, is_valid_entity_type, is_valid_edge_type
  from ingestion.entity_extractor import extract_perimeter_entities  (forward-declared — will fail
    at import time; wrap in try/except ImportError inside the test body, not at module level,
    to avoid breaking the skip mechanism)
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_graph_schema.py -v 2>&1 | head -30</automated>
  </verify>
  <done>All tests in test_graph_schema.py collect and show as SKIPPED (not ERROR). No ImportError at module level.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create test_graph_versioning.py stubs (P26-T01, P26-T04)</name>
  <files>tests/unit/test_graph_versioning.py</files>
  <action>
Create tests/unit/test_graph_versioning.py with module-level skip:

```python
pytestmark = pytest.mark.skip(reason="wave-0 stub — activate in plan 26-05")
```

Include these test functions:

1. test_fresh_install_gets_version_2 — creates SQLiteStore(tmp_path) with an empty entities
   table; asserts store.get_graph_schema_version() returns "2.0.0" (P26-T01).
   Note: get_graph_schema_version() does not exist yet; the stub body calls it and will
   fail at activation if not implemented.
2. test_preexisting_install_gets_version_1 — creates SQLiteStore(tmp_path), inserts one row
   into entities table directly via sqlite3, then creates a second SQLiteStore(tmp_path)
   instance pointing at same DB; asserts get_graph_schema_version() returns "1.0.0" (P26-T01).
3. test_schema_version_endpoint — uses TestClient with the FastAPI app (via `_build_app`
   helper from test_graph_api.py pattern); calls GET /api/graph/schema-version; asserts
   response.status_code == 200 and response.json()["graph_schema_version"] is a non-empty
   string (P26-T01).
4. test_system_kv_not_clobbered — creates SQLiteStore(tmp_path), manually sets
   graph_schema_version to "1.0.0" in system_kv via direct sqlite3 execute; creates new
   SQLiteStore instance; asserts version is still "1.0.0" (INSERT OR IGNORE idempotency,
   P26-T04).
5. test_no_columns_removed — checks that all expected SQLite columns in the entities and
   edges tables are still present after SQLiteStore init. Use PRAGMA table_info() to get
   column names; assert a known set of column names is a subset (P26-T04).

Imports needed:
  import pytest
  import sqlite3
  from pathlib import Path
  from backend.stores.sqlite_store import SQLiteStore

Note: `_build_app` helper — copy the pattern from tests/unit/test_graph_api.py. Read that
file briefly to confirm the exact helper signature before writing.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_graph_versioning.py -v 2>&1 | head -30</automated>
  </verify>
  <done>All tests in test_graph_versioning.py collect and show as SKIPPED (not ERROR). No module-level ImportError.</done>
</task>

</tasks>

<verification>
Both test files exist, all tests SKIP cleanly:
  uv run pytest tests/unit/test_graph_schema.py tests/unit/test_graph_versioning.py -v
Expected: all items shown as SKIPPED, exit code 0 (pytest treats all-skip as success).
Full suite must still pass:
  uv run pytest --ignore=tests/integration -x -q
</verification>

<success_criteria>
- tests/unit/test_graph_schema.py exists with 7 stub functions, all SKIPPED
- tests/unit/test_graph_versioning.py exists with 5 stub functions, all SKIPPED
- uv run pytest (full suite) exits 0 — no regressions introduced
- Requirement IDs P26-T01, P26-T02, P26-T03, P26-T04 each have at least one named stub
</success_criteria>

<output>
After completion, create .planning/phases/26-graph-schema-versioning-and-perimeter-entities/26-00-SUMMARY.md
</output>
