---
phase: 26-graph-schema-versioning-and-perimeter-entities
plan: "02"
type: execute
wave: 2
depends_on:
  - "26-01"
files_modified:
  - ingestion/entity_extractor.py
  - ingestion/loader.py
autonomous: true
requirements:
  - P26-T03
  - P26-T04

must_haves:
  truths:
    - "IPFire DROP_* events produce a blocks edge between the src firewall_zone and dst ip entity"
    - "IPFire FORWARDFW events produce a permits or traverses edge"
    - "Perimeter graph emission is a pure function — no SQLiteStore import in the parser"
    - "All existing extract_entities_and_edges() behavior is unchanged (NormalizedEvent field names preserved)"
    - "firewall_zone entity attributes include zone_name, zone_color (uppercase), interface"
    - "IPFire events processed via loader produce blocks/permits/traverses edges in SQLite"
  artifacts:
    - path: "ingestion/entity_extractor.py"
      provides: "extract_perimeter_entities() function for IPFire syslog events"
      exports: ["extract_perimeter_entities"]
    - path: "ingestion/loader.py"
      provides: "Wiring: calls extract_perimeter_entities() for source_type==ipfire_syslog in _write_graph()"
      contains: "extract_perimeter_entities"
  key_links:
    - from: "ingestion/entity_extractor.py"
      to: "graph/schema.py"
      via: "Uses EDGE_TYPES constants as string literals (blocks, permits, traverses)"
      pattern: "\"blocks\"|\"permits\"|\"traverses\""
    - from: "ingestion/entity_extractor.py"
      to: "backend/models/event.py"
      via: "NormalizedEvent.source_type, .event_outcome, .src_ip, .dst_ip, .tags"
      pattern: "NormalizedEvent"
    - from: "ingestion/loader.py"
      to: "ingestion/entity_extractor.py"
      via: "_write_graph() calls extract_perimeter_entities(event) for ipfire_syslog events"
      pattern: "extract_perimeter_entities"
---

<objective>
Add extract_perimeter_entities() to ingestion/entity_extractor.py, then wire it into
ingestion/loader.py so IPFire syslog events produce firewall_zone and ip entities with
blocks/permits/traverses edges in SQLite during actual ingest.

Task 1 adds the pure extraction function (no I/O). Task 2 wires the call site inside
_write_graph() in loader.py, where extract_entities_and_edges() is already called per event.

Purpose: P26-T03 (perimeter edges). P26-T04 (no removal of existing fields/behavior).
Output: extract_perimeter_entities() in entity_extractor.py, called from loader._write_graph().
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-graph-schema-versioning-and-perimeter-entities/26-RESEARCH.md
@.planning/phases/26-graph-schema-versioning-and-perimeter-entities/26-01-SUMMARY.md

<interfaces>
<!-- Current entity_extractor.py contract — DO NOT change the existing function signature -->

```python
def extract_entities_and_edges(
    event: NormalizedEvent,
) -> tuple[list[dict[str, Any]], list[dict[str, Any]]]:
    """Returns (entities, edges) — pure function, no I/O."""
```

NormalizedEvent fields used by the new function:
  event.source_type: str       — "ipfire_syslog" for IPFire events
  event.event_outcome: str     — "success" (FORWARDFW/INPUTFW) or "failure" (DROP_*/REJECT_*)
  event.src_ip: str | None     — source IP address
  event.dst_ip: str | None     — destination IP address
  event.tags: list[str]        — may contain zone labels like "zone:green", "zone:red"
  event.event_id: str          — for edge properties
  event.timestamp: datetime    — for edge properties
  event.case_id: str | None    — for entity case_id field

From IPFire syslog parser (_ZONE_MAP in ingestion/parsers/ipfire_syslog_parser.py):
  "green0" -> "green", "red0" -> "red", "blue0" -> "blue", "orange0" -> "orange"
  Zone uppercase mapping for firewall_zone.zone_color: green->GREEN, red->RED, etc.

Edge type selection (from RESEARCH.md Pattern 4):
  event_outcome == "failure" (DROP_*/REJECT_*) -> "blocks"
  event_outcome == "success" and src_ip and dst_ip both present -> "traverses"
  event_outcome == "success" (FORWARDFW/INPUTFW) and no traversal detected -> "permits"

firewall_zone entity shape:
  id: "firewall_zone:{zone_name}"
  type: "firewall_zone"
  name: zone_name (e.g. "red", "green")
  attributes: {zone_name: str, zone_color: str (uppercase), interface: str | None}
  case_id: event.case_id

ip entity shape (destination IP):
  id: "ip:{dst_ip}"
  type: "ip"
  name: dst_ip
  attributes: {ip_address: dst_ip}
  case_id: event.case_id

loader.py _write_graph() inner loop — existing call site (lines 509-538 of ingestion/loader.py):

```python
for event in events:
    try:
        entity_list, edge_list = extract_entities_and_edges(event)

        for entity in entity_list:
            attrs = entity.get("attributes") or {}
            sqlite.upsert_entity(
                entity_id=entity["id"],
                entity_type=entity["type"],
                name=entity["name"],
                attributes=attrs,
                case_id=entity.get("case_id"),
            )

        for edge in edge_list:
            result = sqlite.insert_edge(
                source_type=edge["source_type"],
                source_id=edge["source_id"],
                edge_type=edge["edge_type"],
                target_type=edge["target_type"],
                target_id=edge["target_id"],
                properties=edge.get("properties"),
            )
            if result is not None:
                local_edges += 1

    except Exception as exc:
        msg = f"Graph write error for event {event.event_id}: {exc}"
        local_errors.append(msg)
        log.warning(msg, event_id=event.event_id, error=str(exc))
```

The new call goes AFTER the existing extract_entities_and_edges block, still inside the
per-event try/except. Reuse the same sqlite.upsert_entity / sqlite.insert_edge calls.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add extract_perimeter_entities() to entity_extractor.py (P26-T03)</name>
  <files>ingestion/entity_extractor.py</files>
  <behavior>
    - IPFire DROP event (event_outcome="failure", src_ip="10.0.0.1", dst_ip="8.8.8.8",
      tags=["zone:red"]) -> entities include firewall_zone:red node; edges include one edge
      with edge_type="blocks", source_type="firewall_zone", target_type="ip"
    - IPFire FORWARDFW event (event_outcome="success", src_ip="10.0.0.1", dst_ip="1.2.3.4",
      tags=["zone:green"]) -> edge_type="traverses" (both src and dst present)
    - IPFire INPUTFW event (event_outcome="success", src_ip=None, dst_ip="1.2.3.4",
      tags=["zone:green"]) -> edge_type="permits" (only dst present, no traversal)
    - Non-IPFire event (source_type="windows_evtx") -> returns ([], [])
    - Missing dst_ip -> returns ([], []) — no entities or edges without a destination
    - firewall_zone entity attributes contain zone_color in uppercase (RED, GREEN, ORANGE, BLUE)
    - existing extract_entities_and_edges() is not modified; all its tests still pass
  </behavior>
  <action>
Append to ingestion/entity_extractor.py (after the existing extract_entities_and_edges function).
Do NOT modify any existing code in the file.

Add the following:

```python
# ---------------------------------------------------------------------------
# IPFire perimeter entity / edge extraction (Phase 26 — P26-T03)
# ---------------------------------------------------------------------------

_ZONE_COLOR_UPPER: dict[str, str] = {
    "green": "GREEN",
    "red": "RED",
    "blue": "BLUE",
    "orange": "ORANGE",
}


def extract_perimeter_entities(
    event: NormalizedEvent,
) -> tuple[list[dict[str, Any]], list[dict[str, Any]]]:
    """
    Derive perimeter graph entities and edges from an IPFire syslog NormalizedEvent.

    Only processes events where source_type == "ipfire_syslog" and dst_ip is populated.
    Returns an empty ([], []) for all other event types (safe to call unconditionally).

    Returns the same (entities, edges) shape as extract_entities_and_edges().
    """
    if event.source_type != "ipfire_syslog" or not event.dst_ip:
        return [], []

    entities: list[dict[str, Any]] = []
    edges: list[dict[str, Any]] = []

    # Extract zone name from tags (e.g. "zone:red" -> "red")
    zone_name: str | None = None
    for tag in (event.tags or []):
        if tag.startswith("zone:"):
            zone_name = tag[5:].lower()
            break

    # Destination IP entity
    ip_id = f"ip:{event.dst_ip}"
    entities.append({
        "id": ip_id,
        "type": "ip",
        "name": event.dst_ip,
        "attributes": {"ip_address": event.dst_ip},
        "case_id": event.case_id,
    })

    # firewall_zone entity (only when zone is known from tags)
    zone_id: str | None = None
    if zone_name:
        zone_id = f"firewall_zone:{zone_name}"
        entities.append({
            "id": zone_id,
            "type": "firewall_zone",
            "name": zone_name,
            "attributes": {
                "zone_name": zone_name,
                "zone_color": _ZONE_COLOR_UPPER.get(zone_name, zone_name.upper()),
                "interface": None,  # interface info not present in NormalizedEvent
            },
            "case_id": event.case_id,
        })

    edge_props: dict[str, Any] = {
        "event_id": event.event_id,
        "timestamp": event.timestamp.isoformat() if event.timestamp else None,
        "src_ip": event.src_ip,
    }

    # Determine edge type: blocks (failure), traverses (success + src+dst), permits (success)
    if event.event_outcome == "failure":
        edge_type = "blocks"
    elif event.src_ip and event.dst_ip:
        edge_type = "traverses"
    else:
        edge_type = "permits"

    # Emit edge: firewall_zone -> ip (if zone known), else skip edge
    if zone_id:
        edges.append({
            "source_type": "firewall_zone",
            "source_id": zone_id,
            "edge_type": edge_type,
            "target_type": "ip",
            "target_id": ip_id,
            "properties": edge_props,
        })

    return entities, edges
```

After writing, verify the existing extract_entities_and_edges function is untouched
(no lines removed or reformatted above it).
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_entity_extractor.py tests/unit/test_entity_extractor_ecs.py -x -q</automated>
  </verify>
  <done>
    - extract_perimeter_entities is importable from ingestion.entity_extractor
    - Existing entity extractor tests all pass (zero regressions)
    - IPFire failure event produces edge_type="blocks"
    - IPFire success event with both IPs produces edge_type="traverses"
    - Non-IPFire events return ([], [])
    - firewall_zone attributes include zone_color in uppercase
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire extract_perimeter_entities() into loader._write_graph() (P26-T03)</name>
  <files>ingestion/loader.py</files>
  <action>
Read ingestion/loader.py. Find the import line:

```python
from ingestion.entity_extractor import extract_entities_and_edges
```

Change it to:

```python
from ingestion.entity_extractor import extract_entities_and_edges, extract_perimeter_entities
```

Then find the inner `_sync_write_graph` function inside `_write_graph`. The existing per-event
loop ends with the `for edge in edge_list` block that calls `sqlite.insert_edge(...)`. After
that block (still inside the same `try:` and still inside the `for event in events:` loop),
add a conditional call for IPFire events:

```python
                    # Perimeter entities/edges for IPFire syslog events (Phase 26 — P26-T03)
                    if event.source_type == "ipfire_syslog":
                        perim_entities, perim_edges = extract_perimeter_entities(event)

                        for entity in perim_entities:
                            attrs = entity.get("attributes") or {}
                            sqlite.upsert_entity(
                                entity_id=entity["id"],
                                entity_type=entity["type"],
                                name=entity["name"],
                                attributes=attrs,
                                case_id=entity.get("case_id"),
                            )

                        for edge in perim_edges:
                            result = sqlite.insert_edge(
                                source_type=edge["source_type"],
                                source_id=edge["source_id"],
                                edge_type=edge["edge_type"],
                                target_type=edge["target_type"],
                                target_id=edge["target_id"],
                                properties=edge.get("properties"),
                            )
                            if result is not None:
                                local_edges += 1
```

Preserve all indentation (the new block is at the same indentation level as the existing
`for entity in entity_list:` and `for edge in edge_list:` blocks). Do not alter any other
lines in the file.

After writing, verify the import is updated and the new block appears inside the try: in the
per-event loop.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_loader_ipfire_pipeline.py -x -v</automated>
  </verify>
  <done>
    - loader.py imports extract_perimeter_entities alongside extract_entities_and_edges
    - _write_graph calls extract_perimeter_entities for source_type=="ipfire_syslog" events
    - test_loader_ipfire_pipeline.py passes: loader path produces blocks/permits/traverses edges in SQLite
    - No other tests regress
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write pipeline integration test for loader + IPFire perimeter edges (P26-T03)</name>
  <files>tests/unit/test_loader_ipfire_pipeline.py</files>
  <behavior>
    - ingest_events() called with an IPFire DROP NormalizedEvent -> SQLite contains at least one
      edge with edge_type="blocks" and source_type="firewall_zone"
    - ingest_events() called with an IPFire FORWARDFW NormalizedEvent (src_ip + dst_ip) ->
      SQLite contains edge with edge_type="traverses"
    - ingest_events() called with a Windows EVTX NormalizedEvent -> no firewall_zone entity
      or blocks/permits/traverses edge created (existing extractor unaffected)
    - IngestionResult.edges_created is >= 1 for IPFire events that produce edges
  </behavior>
  <action>
Create tests/unit/test_loader_ipfire_pipeline.py.

This test exercises IngestionLoader.ingest_events() end-to-end with mock stores. Use
unittest.mock to stub DuckDB and Chroma so the test is unit-scoped (no real I/O). Use a
real in-memory SQLiteStore (tmp_path) for graph writes.

```python
"""
Pipeline test: IngestionLoader._write_graph() calls extract_perimeter_entities()
for IPFire syslog events and persists the resulting edges to SQLiteStore.

Exercises the loader pipeline path — not just the pure function.
"""
from __future__ import annotations

import asyncio
from datetime import datetime, timezone
from pathlib import Path
from unittest.mock import AsyncMock, MagicMock

import pytest

from backend.models.event import NormalizedEvent
from backend.stores.sqlite_store import SQLiteStore
from ingestion.loader import IngestionLoader


def _make_ipfire_event(
    event_id: str,
    event_outcome: str,
    src_ip: str | None,
    dst_ip: str,
    tags: list[str],
) -> NormalizedEvent:
    return NormalizedEvent(
        event_id=event_id,
        source_type="ipfire_syslog",
        event_type="network_firewall",
        event_outcome=event_outcome,
        severity="medium",
        src_ip=src_ip,
        dst_ip=dst_ip,
        tags=tags,
        timestamp=datetime(2026, 1, 1, tzinfo=timezone.utc),
    )


def _make_stores(tmp_path: Path):
    """Build a Stores-like mock with a real SQLiteStore for graph writes."""
    sqlite = SQLiteStore(str(tmp_path / "test.db"))

    # DuckDB mock — deduplication returns empty (all events are "new"),
    # execute_write is a no-op.
    duckdb = MagicMock()
    duckdb.fetch_all = AsyncMock(return_value=[])   # no duplicates
    duckdb.execute_write = AsyncMock(return_value=None)

    # Chroma mock — embed_batch returns empty embeddings (non-fatal path)
    chroma = MagicMock()

    stores = MagicMock()
    stores.sqlite = sqlite
    stores.duckdb = duckdb
    stores.chroma = chroma
    return stores, sqlite


def _make_ollama():
    ollama = MagicMock()
    ollama.embed_batch = AsyncMock(return_value=[[]])
    return ollama


@pytest.mark.asyncio
async def test_loader_ipfire_drop_produces_blocks_edge(tmp_path):
    """IPFire DROP event via loader -> blocks edge in SQLite."""
    stores, sqlite = _make_stores(tmp_path)
    loader = IngestionLoader(stores, _make_ollama())

    event = _make_ipfire_event(
        event_id="ipfire-drop-001",
        event_outcome="failure",
        src_ip="10.0.0.1",
        dst_ip="8.8.8.8",
        tags=["zone:red"],
    )

    result = await loader.ingest_events([event])

    # At least one edge written
    assert result.edges_created >= 1, f"Expected >=1 edges, got {result.edges_created}"

    # SQLite contains a blocks edge from a firewall_zone
    edges = sqlite.get_edges_for_entity("firewall_zone:red")
    assert edges, "No edges found for firewall_zone:red in SQLite"
    edge_types = [e["edge_type"] for e in edges]
    assert "blocks" in edge_types, f"Expected blocks edge, got: {edge_types}"


@pytest.mark.asyncio
async def test_loader_ipfire_forward_produces_traverses_edge(tmp_path):
    """IPFire FORWARDFW event (src + dst) via loader -> traverses edge in SQLite."""
    stores, sqlite = _make_stores(tmp_path)
    loader = IngestionLoader(stores, _make_ollama())

    event = _make_ipfire_event(
        event_id="ipfire-fwd-001",
        event_outcome="success",
        src_ip="192.168.1.10",
        dst_ip="1.2.3.4",
        tags=["zone:green"],
    )

    result = await loader.ingest_events([event])

    assert result.edges_created >= 1
    edges = sqlite.get_edges_for_entity("firewall_zone:green")
    assert edges, "No edges found for firewall_zone:green in SQLite"
    edge_types = [e["edge_type"] for e in edges]
    assert "traverses" in edge_types, f"Expected traverses edge, got: {edge_types}"


@pytest.mark.asyncio
async def test_loader_non_ipfire_no_firewall_zone_entity(tmp_path):
    """Non-IPFire event via loader -> no firewall_zone entity written."""
    stores, sqlite = _make_stores(tmp_path)
    loader = IngestionLoader(stores, _make_ollama())

    event = NormalizedEvent(
        event_id="win-001",
        source_type="windows_evtx",
        event_type="logon_success",
        severity="low",
        hostname="WORKSTATION1",
        username="alice",
        timestamp=datetime(2026, 1, 1, tzinfo=timezone.utc),
    )

    await loader.ingest_events([event])

    # No firewall_zone entity should exist
    entity = sqlite.get_entity("firewall_zone:red")
    assert entity is None, "firewall_zone entity created for non-IPFire event"
```

Check that SQLiteStore has `get_edges_for_entity(entity_id)` and `get_entity(entity_id)`
methods before writing. If they don't exist, read backend/stores/sqlite_store.py briefly to
find the correct query method names and adapt the assertions accordingly (e.g., query via
`sqlite._conn.execute(...)` with a context manager if needed). Do NOT add methods to
SQLiteStore — adapt the test to use existing APIs.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_loader_ipfire_pipeline.py -x -v</automated>
  </verify>
  <done>
    - All 3 pipeline tests pass
    - Tests exercise IngestionLoader.ingest_events() (not the pure function directly)
    - IPFire DROP -> blocks edge confirmed in SQLite
    - IPFire FORWARDFW -> traverses edge confirmed in SQLite
    - Non-IPFire event -> no firewall_zone entity
  </done>
</task>

</tasks>

<verification>
Import check:
  uv run python -c "from ingestion.entity_extractor import extract_perimeter_entities; print('import OK')"

Loader import check:
  uv run python -c "from ingestion.loader import IngestionLoader; print('loader import OK')"

Pure function tests (must not regress):
  uv run pytest tests/unit/test_entity_extractor.py tests/unit/test_entity_extractor_ecs.py -x -q

Pipeline integration tests (new):
  uv run pytest tests/unit/test_loader_ipfire_pipeline.py -v

Full unit suite:
  uv run pytest tests/unit/ -x -q
</verification>

<success_criteria>
- extract_perimeter_entities() appended to entity_extractor.py with no changes to existing code
- loader.py imports and calls extract_perimeter_entities() for source_type=="ipfire_syslog" events
- test_loader_ipfire_pipeline.py: 3 tests PASSED exercising the full loader pipeline path
- Returns ([], []) for non-IPFire events and events without dst_ip
- Returns firewall_zone entity + blocks/permits/traverses edge for IPFire events
- zone_color stored as uppercase string (RED/GREEN/ORANGE/BLUE)
- All existing entity extractor unit tests still pass
</success_criteria>

<output>
After completion, create .planning/phases/26-graph-schema-versioning-and-perimeter-entities/26-02-SUMMARY.md
</output>
