---
phase: 16-security-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/core/config.py
  - backend/core/auth.py
  - tests/unit/test_auth.py
autonomous: true
requirements:
  - P16-SEC-01

must_haves:
  truths:
    - "AUTH_TOKEN defaults to 'changeme' so auth is ON in all environments without explicit config"
    - "An empty string AUTH_TOKEN causes verify_token to return 401 for all requests (not bypass)"
    - "A request with the correct token returns 200; missing or wrong token returns 401"
  artifacts:
    - path: "backend/core/config.py"
      provides: "AUTH_TOKEN default changed to 'changeme'"
      contains: "AUTH_TOKEN: str = \"changeme\""
    - path: "backend/core/auth.py"
      provides: "verify_token rejects empty-string token as misconfiguration"
      contains: "settings.AUTH_TOKEN.strip()"
    - path: "tests/unit/test_auth.py"
      provides: "Tests for new empty-token-as-misconfiguration behaviour"
      exports: ["test_empty_token_returns_401", "test_changeme_default_enforces_auth"]
  key_links:
    - from: "backend/core/config.py"
      to: "backend/core/auth.py"
      via: "settings.AUTH_TOKEN import"
      pattern: "from backend.core.config import settings"
---

<objective>
Harden the backend auth defaults so auth is secure-by-default.

Purpose: The external critique found AUTH_TOKEN defaults to "" causing open-mode bypass in all environments where the env var is unset. This plan sets a non-empty default and makes empty-string equivalent to misconfiguration that rejects all requests.
Output: config.py with AUTH_TOKEN="changeme" default, auth.py rejecting empty tokens, updated test suite.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-security-hardening/16-CONTEXT.md

Key files:
- backend/core/config.py — Settings class, AUTH_TOKEN field at line 50
- backend/core/auth.py — verify_token dependency (30 lines total)
- tests/unit/test_auth.py — existing auth unit tests (4 tests)

Current state:
- config.py line 50: `AUTH_TOKEN: str = ""`
- auth.py line 26: `if not settings.AUTH_TOKEN: return  # open mode bypass`
- test_auth.py has test_open_mode_bypass that asserts empty string = bypass (MUST be updated)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Change AUTH_TOKEN default and reject empty-string token</name>
  <files>backend/core/config.py, backend/core/auth.py, tests/unit/test_auth.py</files>
  <behavior>
    - Test: AUTH_TOKEN="" → verify_token(None) raises HTTPException(401) — no bypass
    - Test: AUTH_TOKEN="changeme" → verify_token(correct creds) passes
    - Test: AUTH_TOKEN="changeme" → verify_token(None) raises 401
    - Test: Settings() with no env var → AUTH_TOKEN == "changeme"
  </behavior>
  <action>
    1. In `backend/core/config.py`, change line 50:
       FROM: `AUTH_TOKEN: str = ""`
       TO:   `AUTH_TOKEN: str = "changeme"`
       Comment: "Default is non-empty so auth is ON by default. Set AUTH_TOKEN= in .env to override."

    2. In `backend/core/auth.py`, replace the open-mode bypass logic.
       Current lines 26-27:
         `if not settings.AUTH_TOKEN:`
         `    return  # AUTH_TOKEN not set → open mode (dev/localhost)`

       Replace with:
         ```python
         configured = settings.AUTH_TOKEN.strip()
         if not configured:
             # Empty string is misconfiguration — reject all requests to prevent
             # accidental open-access deployment. Set AUTH_TOKEN in .env.
             raise HTTPException(status_code=401, detail="Auth misconfigured: AUTH_TOKEN is empty")
         if credentials is None or credentials.credentials != configured:
             raise HTTPException(status_code=401, detail="Unauthorized")
         ```

       Also update the module docstring to reflect the new behaviour:
       "When AUTH_TOKEN is empty string, ALL requests are rejected (misconfiguration guard).
        To disable auth for local dev: set AUTH_TOKEN=dev-only-bypass in .env explicitly."

    3. In `tests/unit/test_auth.py`:
       - Update `test_open_mode_bypass`: change assertion — empty AUTH_TOKEN must NOW raise 401, not pass.
         Rename to `test_empty_token_raises_401` and assert HTTPException(401).
       - Add `test_changeme_default_enforces_auth`: with AUTH_TOKEN="changeme" and no credentials → 401.
       - Add `test_whitespace_only_token_raises_401`: AUTH_TOKEN="   " → 401 (covers strip() edge case).
       - Keep existing `test_valid_token_passes`, `test_missing_token_returns_401`, `test_wrong_token_returns_401` — they remain correct.
  </action>
  <verify>
    <automated>cd C:/Users/Admin/AI-SOC-Brain && uv run pytest tests/unit/test_auth.py -v 2>&1</automated>
  </verify>
  <done>
    All auth tests pass. AUTH_TOKEN default is "changeme" in config.py. verify_token raises 401 when AUTH_TOKEN is empty or whitespace. Correct token still passes. Six tests total pass.
  </done>
</task>

</tasks>

<verification>
uv run pytest tests/unit/test_auth.py -v
uv run pytest tests/unit/test_config.py -v
</verification>

<success_criteria>
- AUTH_TOKEN default in config.py is "changeme" (non-empty)
- verify_token raises HTTP 401 when AUTH_TOKEN="" (empty string)
- verify_token raises HTTP 401 when AUTH_TOKEN="   " (whitespace only)
- All existing tests that depended on open-mode pass still updated and green
- uv run pytest tests/unit/test_auth.py shows 6+ passing tests
</success_criteria>

<output>
After completion, create `.planning/phases/16-security-hardening/16-01-SUMMARY.md`
</output>
