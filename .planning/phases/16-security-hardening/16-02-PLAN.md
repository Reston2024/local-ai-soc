---
phase: 16-security-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - .gitignore
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - P16-DEP-05

must_haves:
  truths:
    - "pytest, pytest-asyncio, ruff, httpx are in a [dependency-groups] dev group, not in main [dependencies]"
    - "uv run pytest still executes correctly after the move"
    - "CI test job passes with uv sync --group dev"
    - ".gitignore has no duplicate entries"
  artifacts:
    - path: "pyproject.toml"
      provides: "Dev deps in [dependency-groups] dev section"
      contains: "[dependency-groups]"
    - path: ".github/workflows/ci.yml"
      provides: "CI sync commands updated to include dev group"
      contains: "--group dev"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "pyproject.toml"
      via: "uv sync --group dev"
      pattern: "uv sync.*group dev"
---

<objective>
Separate runtime and development dependencies in pyproject.toml so production installs do not include test/lint tooling.

Purpose: The critique found pytest, pytest-asyncio, and ruff in main dependencies. This inflates production images and violates separation of concerns. This plan moves them to a dev group and updates CI to install the dev group.
Output: pyproject.toml with proper dev group, CI updated, .gitignore deduplicated.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-security-hardening/16-CONTEXT.md

Current pyproject.toml state:
- Main [dependencies] contains: pytest==9.0.2, pytest-asyncio==1.3.0, ruff==0.15.6, httpx==0.28.1
- [project.optional-dependencies] dev already has: pytest-cov>=6.0.0
- CI test job already does: uv sync --extra dev (for pytest-cov)
- CI lint job does: uv sync (no extras)

Note: httpx is BOTH used at runtime (for Ollama client) AND in tests. Keep httpx in main [dependencies] — it is a runtime dep (backend/services/ollama_client.py imports it). Only pure dev tools move.

.gitignore duplicate: `.claude/settings.local.json` appears on lines 89 and 91.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move dev-only deps to dependency-groups and fix .gitignore</name>
  <files>pyproject.toml, .gitignore</files>
  <action>
    1. In `pyproject.toml`:

       Remove from [dependencies]:
         - "pytest==9.0.2"
         - "pytest-asyncio==1.3.0"
         - "ruff==0.15.6"
       Keep in [dependencies]: httpx==0.28.1 (runtime dependency for OllamaClient)

       Replace the existing [project.optional-dependencies] section with a [dependency-groups] section:
       ```toml
       [dependency-groups]
       dev = [
           "pytest==9.0.2",
           "pytest-asyncio==1.3.0",
           "pytest-cov>=6.0.0",
           "ruff==0.15.6",
       ]
       ```

       Remove the old [project.optional-dependencies] block entirely (it only contained pytest-cov).

    2. In `.gitignore`:
       Line 89 and line 91 both contain `.claude/settings.local.json`. Remove the duplicate (line 91).
       The file currently ends:
         `.claude/settings.local.json`
         (blank line)
         `.claude/settings.local.json`
         (blank line)
       Should end with a single entry:
         `.claude/settings.local.json`
  </action>
  <verify>
    <automated>cd C:/Users/Admin/AI-SOC-Brain && uv sync --group dev && uv run pytest tests/unit/test_auth.py -q 2>&1</automated>
  </verify>
  <done>
    pyproject.toml has [dependency-groups] dev containing pytest, pytest-asyncio, pytest-cov, ruff. Main [dependencies] no longer contains those packages. uv sync --group dev succeeds. uv run pytest runs. .gitignore has no duplicate entries.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update CI to install dev group</name>
  <files>.github/workflows/ci.yml</files>
  <action>
    In `.github/workflows/ci.yml`, update three jobs:

    1. lint job — change the install step:
       FROM: `run: uv sync`
       TO:   `run: uv sync --group dev`
       (ruff is now in dev group, lint job needs it)

    2. test job — change the install step:
       FROM: `run: uv sync --extra dev`
       TO:   `run: uv sync --group dev`
       (pytest-cov moved from optional-extras to dependency-groups)

    3. dependency-audit job — change the install step:
       FROM: `run: uv sync`
       TO:   `run: uv sync --group dev`
       (consistent; dev group packages should be audited too)

    Also update the comment on the test job install step:
       FROM: `# Install dependencies (with dev extras for pytest-cov)`
       TO:   `# Install dependencies including dev group (pytest, pytest-cov, ruff)`
  </action>
  <verify>
    <automated>cd C:/Users/Admin/AI-SOC-Brain && grep -n "group dev" .github/workflows/ci.yml 2>&1</automated>
  </verify>
  <done>
    All three CI jobs (lint, test, dependency-audit) use `uv sync --group dev`. No remaining references to `--extra dev` or bare `uv sync` in the affected install steps. Grep confirms `--group dev` appears 3+ times in ci.yml.
  </done>
</task>

</tasks>

<verification>
cd C:/Users/Admin/AI-SOC-Brain && uv sync --group dev && uv run pytest tests/unit/ -q --tb=no 2>&1 | tail -5
grep "dependency-groups" pyproject.toml
grep "group dev" .github/workflows/ci.yml | wc -l
</verification>

<success_criteria>
- pyproject.toml [dependency-groups] dev contains pytest, pytest-asyncio, pytest-cov, ruff
- pytest and ruff NOT in main [dependencies]
- httpx REMAINS in main [dependencies]
- uv sync --group dev succeeds
- uv run pytest tests/unit/ passes (same results as before)
- CI ci.yml has --group dev in lint, test, and dependency-audit install steps
- .gitignore has exactly one entry for .claude/settings.local.json
</success_criteria>

<output>
After completion, create `.planning/phases/16-security-hardening/16-02-SUMMARY.md`
</output>
