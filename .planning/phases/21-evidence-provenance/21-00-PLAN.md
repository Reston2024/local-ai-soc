---
phase: 21-evidence-provenance
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/unit/test_ingest_provenance.py
  - tests/unit/test_detection_provenance.py
  - tests/unit/test_llm_provenance.py
  - tests/unit/test_playbook_provenance.py
  - tests/unit/test_provenance_api.py
  - backend/models/provenance.py
  - detections/field_map.py
autonomous: true
requirements:
  - P21-T01
  - P21-T02
  - P21-T03
  - P21-T04
  - P21-T05

must_haves:
  truths:
    - "All 5 test files exist and FAIL (not error) when run with pytest"
    - "Pydantic provenance response models are defined and importable"
    - "FIELD_MAP_VERSION constant exists in detections/field_map.py"
  artifacts:
    - path: "tests/unit/test_ingest_provenance.py"
      provides: "Failing stubs for P21-T01 ingest provenance behaviors"
    - path: "tests/unit/test_detection_provenance.py"
      provides: "Failing stubs for P21-T02 detection provenance behaviors"
    - path: "tests/unit/test_llm_provenance.py"
      provides: "Failing stubs for P21-T03 LLM audit provenance behaviors"
    - path: "tests/unit/test_playbook_provenance.py"
      provides: "Failing stubs for P21-T04 playbook run provenance behaviors"
    - path: "tests/unit/test_provenance_api.py"
      provides: "Failing stubs for P21-T05 provenance API endpoints"
    - path: "backend/models/provenance.py"
      provides: "Pydantic BaseModel classes for all 4 provenance record types"
    - path: "detections/field_map.py"
      provides: "FIELD_MAP_VERSION constant for detection provenance"
  key_links:
    - from: "tests/unit/test_ingest_provenance.py"
      to: "backend/stores/sqlite_store.py"
      via: "SQLiteStore import — tests stub the store directly"
      pattern: "from backend.stores.sqlite_store import SQLiteStore"
    - from: "backend/models/provenance.py"
      to: "tests/unit/test_provenance_api.py"
      via: "Pydantic models imported by both API and tests"
      pattern: "from backend.models.provenance import"
---

<objective>
Create all failing test stubs (RED state) for Phase 21, plus the Pydantic response models and FIELD_MAP_VERSION constant that all downstream plans depend on.

Purpose: Nyquist compliance — every subsequent plan has concrete FAIL tests to turn GREEN. The Pydantic models serve as interface contracts so Wave 1 and Wave 2 executor plans have types to implement against without exploring the codebase.

Output: 5 test files (all failing), backend/models/provenance.py (importable), detections/field_map.py FIELD_MAP_VERSION added.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/21-evidence-provenance/21-RESEARCH.md
@.planning/phases/21-evidence-provenance/21-VALIDATION.md

<interfaces>
<!-- Existing patterns the executor must match. Extracted from codebase. -->

From tests/unit/ (existing stub pattern — Phase 19 reference):
```python
import pytest
pytest.fail("NOT IMPLEMENTED")
```

From backend/models/event.py (Pydantic v2 pattern):
```python
from pydantic import BaseModel, Field
class SomeRecord(BaseModel):
    id: str
    created_at: str
```

From detections/field_map.py (module-level constant pattern):
```python
SIGMA_FIELD_MAP: dict[str, str] = { ... }
INTEGER_COLUMNS: frozenset[str] = frozenset({ ... })
# Add: FIELD_MAP_VERSION: str = "20"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Pydantic provenance models and FIELD_MAP_VERSION constant</name>
  <files>backend/models/provenance.py, detections/field_map.py</files>
  <behavior>
    - IngestProvenanceRecord has: prov_id, event_id, raw_sha256 (64-char hex), source_file, parser_name, parser_version (optional), operator_id (optional), ingested_at
    - DetectionProvenanceRecord has: prov_id, detection_id, rule_id, rule_title, rule_sha256, pysigma_version, field_map_version, operator_id (optional), detected_at
    - LlmProvenanceRecord has: audit_id, model_id, prompt_template_name, prompt_template_sha256, response_sha256, operator_id (optional), grounding_event_ids (list[str]), created_at
    - PlaybookProvenanceRecord has: prov_id, run_id, playbook_id, playbook_file_sha256, playbook_version, trigger_event_ids (list[str]), operator_id_who_approved (optional), created_at
    - All fields use str or Optional[str] or list[str] — no complex types
    - FIELD_MAP_VERSION = "20" added as module-level constant to detections/field_map.py (place after existing constants, before SIGMA_FIELD_MAP dict)
  </behavior>
  <action>
    Create backend/models/provenance.py with four Pydantic BaseModel classes:
    IngestProvenanceRecord, DetectionProvenanceRecord, LlmProvenanceRecord, PlaybookProvenanceRecord.
    Each matches the SQLite table schema defined in 21-RESEARCH.md.
    Use Optional[str] from typing for nullable fields.
    grounding_event_ids and trigger_event_ids are list[str] (deserialized from JSON storage).

    Then open detections/field_map.py and add the line:
      FIELD_MAP_VERSION: str = "20"
    immediately after the module docstring / existing imports, before SIGMA_FIELD_MAP is defined.
    This value "20" matches the phase that last updated the field map (Phase 20 — ECS extension).
  </action>
  <verify>
    <automated>uv run python -c "from backend.models.provenance import IngestProvenanceRecord, DetectionProvenanceRecord, LlmProvenanceRecord, PlaybookProvenanceRecord; from detections.field_map import FIELD_MAP_VERSION; print('OK', FIELD_MAP_VERSION)"</automated>
  </verify>
  <done>All 4 Pydantic models import cleanly; FIELD_MAP_VERSION = "20" prints; no existing tests broken</done>
</task>

<task type="auto">
  <name>Task 2: Create 5 failing test stub files (RED state for all P21 requirements)</name>
  <files>
    tests/unit/test_ingest_provenance.py,
    tests/unit/test_detection_provenance.py,
    tests/unit/test_llm_provenance.py,
    tests/unit/test_playbook_provenance.py,
    tests/unit/test_provenance_api.py
  </files>
  <action>
    Create each test file using pytest.fail("NOT IMPLEMENTED") as the stub body.
    Every stub must FAIL (not error) — use a function body that calls pytest.fail(), not an empty body or import error.

    test_ingest_provenance.py — 5 test functions:
      test_ingest_provenance_table_exists: verify SQLiteStore creates ingest_provenance + ingest_provenance_events tables
      test_sha256_file_hash: verify _sha256_file() returns 64-char hex string
      test_ingest_provenance_written: verify ingest_provenance row written after ingest_file()
      test_ingest_provenance_api: verify GET /api/provenance/ingest/{event_id} returns 200 with sha256, parser_name
      test_ingest_provenance_failure_nonfatal: verify SQLite INSERT failure does not abort ingestion

    test_detection_provenance.py — 3 test functions:
      test_detection_provenance_table_exists: verify SQLiteStore creates detection_provenance table
      test_detection_provenance_fields: verify row has pySigma version, rule_sha256, field_map_version
      test_detection_provenance_api: verify GET /api/provenance/detection/{id} returns 200 with rule_sha256, pysigma_version

    test_llm_provenance.py — 4 test functions:
      test_llm_provenance_table_exists: verify SQLiteStore creates llm_audit_provenance table
      test_llm_provenance_written: verify generate() writes row with audit_id, prompt_template_sha256, grounding_event_ids
      test_llm_provenance_no_duplicate_rows: verify only 1 row per logical LLM call (not per streaming chunk)
      test_llm_provenance_api: verify GET /api/provenance/llm/{audit_id} returns 200 with model_id, response_sha256

    test_playbook_provenance.py — 3 test functions:
      test_playbook_provenance_table_exists: verify SQLiteStore creates playbook_run_provenance table
      test_playbook_provenance_fields: verify row has playbook_file_sha256, operator_id_who_approved
      test_playbook_provenance_api: verify GET /api/provenance/playbook/{run_id} returns 200 with trigger_event_ids

    test_provenance_api.py — 1 test function:
      test_provenance_endpoints_require_auth: verify all 4 GET endpoints return 401 without auth token

    Each test file starts with:
      import pytest
      from backend.models.provenance import (the relevant model class)

    This ensures import errors surface as collection errors, not FAIL — only add the import that is relevant to each file.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_ingest_provenance.py tests/unit/test_detection_provenance.py tests/unit/test_llm_provenance.py tests/unit/test_playbook_provenance.py tests/unit/test_provenance_api.py -q 2>&1 | tail -10</automated>
  </verify>
  <done>All 16 test functions FAIL (not error) — pytest output shows "16 failed" with "NOT IMPLEMENTED" messages, zero errors</done>
</task>

</tasks>

<verification>
Run: uv run pytest tests/unit/test_ingest_provenance.py tests/unit/test_detection_provenance.py tests/unit/test_llm_provenance.py tests/unit/test_playbook_provenance.py tests/unit/test_provenance_api.py -q
Expected: 16 failed, 0 errors — all stubs are RED

Run: uv run python -c "from backend.models.provenance import IngestProvenanceRecord, DetectionProvenanceRecord, LlmProvenanceRecord, PlaybookProvenanceRecord; print('models OK')"
Expected: "models OK"

Run: uv run python -c "from detections.field_map import FIELD_MAP_VERSION; assert FIELD_MAP_VERSION == '20'"
Expected: no assertion error

Run: uv run pytest tests/unit/ -q --tb=short -x (excluding new stub files) to confirm no regressions
</verification>

<success_criteria>
- 5 test files exist under tests/unit/
- 16 total test functions, all in FAIL state (pytest.fail("NOT IMPLEMENTED"))
- backend/models/provenance.py exists with 4 Pydantic BaseModel classes
- detections/field_map.py has FIELD_MAP_VERSION = "20"
- Existing test suite still passes (no regressions)
</success_criteria>

<output>
After completion, create .planning/phases/21-evidence-provenance/21-00-SUMMARY.md
</output>
