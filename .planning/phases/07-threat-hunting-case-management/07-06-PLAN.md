---
phase: 07-threat-hunting-case-management
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/App.svelte
autonomous: true
gap_closure: true
requirements: [P7-UI-01, P7-UI-02]

must_haves:
  truths:
    - "Dashboard nav bar shows tabs: Alerts, Cases, Hunt, Investigation, Attack Chain"
    - "Clicking Cases tab renders CasePanel with case list and create form"
    - "Clicking Hunt tab renders HuntPanel with template dropdown and Run button"
    - "Clicking Investigation tab renders InvestigationPanel"
    - "Clicking Attack Chain tab renders AttackChain"
    - "Default tab (Alerts) shows original layout — ThreatGraph + EventTimeline + EvidencePanel"
    - "npm run build exits 0 after change"
  artifacts:
    - path: "frontend/src/App.svelte"
      provides: "Tab nav + conditional panel rendering for all 6 panels"
      contains: "CasePanel, HuntPanel, InvestigationPanel, AttackChain"
  key_links:
    - from: "frontend/src/App.svelte"
      to: "frontend/src/components/panels/CasePanel.svelte"
      via: "import + {#if activeTab === 'cases'}"
    - from: "frontend/src/App.svelte"
      to: "frontend/src/components/panels/HuntPanel.svelte"
      via: "import + {#if activeTab === 'hunt'}"
    - from: "frontend/src/App.svelte"
      to: "frontend/src/components/panels/InvestigationPanel.svelte"
      via: "import + {#if activeTab === 'investigation'}"
    - from: "frontend/src/App.svelte"
      to: "frontend/src/components/graph/AttackChain.svelte"
      via: "import + {#if activeTab === 'attackchain'}"
---

<objective>
Wire CasePanel, HuntPanel, InvestigationPanel, and AttackChain into the App.svelte
navigation so all four Phase 6/7 components are reachable from the dashboard.

Purpose: UAT Tests 10–12 failed because the panels were never imported or rendered.
App.svelte uses a fixed 3-panel layout with no tab switching.

Output: Updated App.svelte with a tab nav bar and conditional rendering. Existing
Alerts view (ThreatGraph + EventTimeline + EvidencePanel) becomes the default tab.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Existing components that remain in the Alerts tab -->
<!-- frontend/src/components/graph/ThreatGraph.svelte — props: onNodeSelect: (node: any) => void -->
<!-- frontend/src/components/timeline/EventTimeline.svelte — no props -->
<!-- frontend/src/components/panels/EvidencePanel.svelte — props: selected: any -->

<!-- New panels to add -->
<!-- frontend/src/components/panels/CasePanel.svelte — no props required -->
<!-- frontend/src/components/panels/HuntPanel.svelte — no props required -->
<!-- frontend/src/components/panels/InvestigationPanel.svelte — props: alertId, score, techniques, firstEvent, lastEvent, onFilterApplied (all optional/have defaults per 06-05 decision) -->
<!-- frontend/src/components/graph/AttackChain.svelte — no required props per build -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tab navigation and conditional panel rendering to App.svelte</name>
  <files>frontend/src/App.svelte</files>
  <action>
    Rewrite App.svelte to add a tab navigation bar and render panels conditionally.
    Use Svelte 5 runes only ($state, $derived, $effect — NOT writable() or stores).

    Tab list (in nav order):
    - 'alerts'       — default tab, existing 3-panel layout (ThreatGraph + EvidencePanel + EventTimeline footer)
    - 'cases'        — renders CasePanel full-width in main content area
    - 'hunt'         — renders HuntPanel full-width in main content area
    - 'investigation' — renders InvestigationPanel full-width in main content area
    - 'attackchain'  — renders AttackChain full-width in main content area

    Add state:
      let activeTab = $state<'alerts' | 'cases' | 'hunt' | 'investigation' | 'attackchain'>('alerts')

    Add imports at top of script:
      import CasePanel from './components/panels/CasePanel.svelte'
      import HuntPanel from './components/panels/HuntPanel.svelte'
      import InvestigationPanel from './components/panels/InvestigationPanel.svelte'
      import AttackChain from './components/graph/AttackChain.svelte'

    In the HTML, add a tab bar inside .soc-layout, between the .status-bar header and
    .main-content div. Render it as a flex row of buttons:
      <nav class="tab-bar">
        <button class:active={activeTab === 'alerts'} onclick={() => activeTab = 'alerts'}>Alerts</button>
        <button class:active={activeTab === 'cases'} onclick={() => activeTab = 'cases'}>Cases</button>
        <button class:active={activeTab === 'hunt'} onclick={() => activeTab = 'hunt'}>Hunt</button>
        <button class:active={activeTab === 'investigation'} onclick={() => activeTab = 'investigation'}>Investigation</button>
        <button class:active={activeTab === 'attackchain'} onclick={() => activeTab = 'attackchain'}>Attack Chain</button>
      </nav>

    Wrap the existing .main-content div and .timeline-bar footer in:
      {#if activeTab === 'alerts'}
        [existing .main-content + .timeline-bar]
      {/if}

    Add below it (still inside .soc-layout):
      {#if activeTab === 'cases'}
        <div class="tab-content"><CasePanel /></div>
      {/if}
      {#if activeTab === 'hunt'}
        <div class="tab-content"><HuntPanel /></div>
      {/if}
      {#if activeTab === 'investigation'}
        <div class="tab-content"><InvestigationPanel /></div>
      {/if}
      {#if activeTab === 'attackchain'}
        <div class="tab-content"><AttackChain /></div>
      {/if}

    Add CSS for .tab-bar and .tab-content:
      .tab-bar {
        display: flex; gap: 2px; padding: 0 14px;
        background: var(--surface); border-bottom: 1px solid var(--border);
        flex-shrink: 0;
      }
      .tab-bar button {
        background: none; border: none; border-bottom: 2px solid transparent;
        color: var(--muted); padding: 6px 14px; cursor: pointer; font-size: 12px;
        font-weight: 500; transition: color 0.15s;
      }
      .tab-bar button:hover { color: var(--fg); }
      .tab-bar button.active { color: var(--accent); border-bottom-color: var(--accent); }
      .tab-content { flex: 1; overflow-y: auto; padding: 16px; }

    Keep ALL existing state, polling, handleLoadFixtures, and the status-bar header
    unchanged. The Load Fixtures button and health/alert indicators stay in the header.

    Update the .title text from "AI SOC Brain — Phase 2" to "AI SOC Brain — v1.0"
    to reflect completion.
  </action>
  <verify>
    <automated>cd /c/Users/Admin/AI-SOC-Brain/frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    npm run build exits 0.
    App.svelte imports CasePanel, HuntPanel, InvestigationPanel, AttackChain.
    Tab nav renders 5 tabs; activeTab state drives conditional rendering.
    Alerts tab (default) shows original ThreatGraph + EventTimeline + EvidencePanel layout.
  </done>
</task>

</tasks>

<verification>
uv run pytest tests/ -x -q 2>&1 | tail -10   (no regressions)
cd /c/Users/Admin/AI-SOC-Brain/frontend && npm run build   (exits 0)
</verification>

<success_criteria>
- frontend/src/App.svelte imports all 4 new panel components
- Tab bar with 5 tabs present in rendered HTML
- npm run build exits 0
- No Python test regressions
</success_criteria>

<output>
After completion, create `.planning/phases/07-threat-hunting-case-management/07-06-SUMMARY.md`
</output>
