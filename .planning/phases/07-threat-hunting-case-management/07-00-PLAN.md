---
phase: 07-threat-hunting-case-management
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/src/tests/test_phase7.py
  - backend/investigation/__init__.py
  - backend/investigation/case_manager.py
  - backend/investigation/hunt_engine.py
  - backend/investigation/timeline_builder.py
  - backend/investigation/artifact_store.py
  - backend/investigation/tagging.py
  - backend/investigation/investigation_routes.py
  - backend/stores/sqlite_store.py
autonomous: true
requirements:
  - P7-T01
  - P7-T02
  - P7-T03
  - P7-T04
  - P7-T05
  - P7-T06
  - P7-T07
  - P7-T08
  - P7-T09
  - P7-T10
  - P7-T11
  - P7-T12
  - P7-T13
  - P7-T14
  - P7-T15
  - P7-T16

must_haves:
  truths:
    - "16 xfail test stubs exist in test_phase7.py covering P7-T01 through P7-T16"
    - "All 6 backend/investigation/ stub modules exist and import without error"
    - "SQLiteStore _DDL contains investigation_cases, case_artifacts, and case_tags tables"
    - "investigation_routes.py defines an investigation_router APIRouter (even if empty)"
    - "Full regression suite passes (no new errors from stubs)"
  artifacts:
    - path: "backend/src/tests/test_phase7.py"
      provides: "16 xfail test stubs"
      min_lines: 100
    - path: "backend/investigation/__init__.py"
      provides: "Package marker"
    - path: "backend/investigation/case_manager.py"
      provides: "Stub class CaseManager"
    - path: "backend/investigation/hunt_engine.py"
      provides: "Stub HUNT_TEMPLATES dict + execute_hunt stub"
    - path: "backend/investigation/timeline_builder.py"
      provides: "Stub build_timeline function"
    - path: "backend/investigation/artifact_store.py"
      provides: "Stub save_artifact function"
    - path: "backend/investigation/tagging.py"
      provides: "Stub tag management functions"
    - path: "backend/investigation/investigation_routes.py"
      provides: "Empty investigation_router APIRouter"
    - path: "backend/stores/sqlite_store.py"
      provides: "Extended _DDL with investigation_cases, case_artifacts, case_tags tables"
  key_links:
    - from: "backend/src/tests/test_phase7.py"
      to: "backend/investigation/case_manager.py"
      via: "deferred import inside test methods"
      pattern: "from backend.investigation.case_manager import"
    - from: "backend/stores/sqlite_store.py"
      to: "investigation_cases table"
      via: "_DDL string extension"
      pattern: "investigation_cases"
---

<objective>
Wave 0: Establish the TDD red baseline for Phase 7 — write all 16 xfail test stubs, create the backend/investigation/ package with 6 stub modules, extend SQLiteStore _DDL with 3 new tables, and create the empty investigation_router.

Purpose: No implementation yet. All stubs must XFAIL (not ERROR). The regression suite must stay green. This baseline ensures subsequent plans can be verified incrementally.

Output: test_phase7.py (16 stubs), backend/investigation/ package (6 stubs), SQLiteStore DDL extension.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-threat-hunting-case-management/07-CONTEXT.md
@.planning/phases/07-threat-hunting-case-management/07-RESEARCH.md
@.planning/phases/07-threat-hunting-case-management/07-VALIDATION.md

<interfaces>
<!-- Existing patterns to follow exactly -->

From backend/src/tests/test_phase6.py (xfail pattern):
```python
import pytest
from fastapi.testclient import TestClient
from backend.src.api.main import app

client = TestClient(app)

class TestEntityResolver:
    @pytest.mark.xfail(strict=False, reason="Phase 6 Plan 01 not yet implemented")
    def test_resolve_host_returns_lowercase(self):
        from backend.causality.entity_resolver import resolve_canonical_id
        result = resolve_canonical_id({"host": "WORKSTATION01"}, "host")
        assert result == "host:workstation01"
```

From backend/stores/sqlite_store.py (existing _DDL pattern):
```python
_DDL = """
PRAGMA journal_mode=WAL;
PRAGMA foreign_keys=ON;

CREATE TABLE IF NOT EXISTS cases (
    id          TEXT PRIMARY KEY,
    name        TEXT NOT NULL,
    ...
);
...
"""
```
The _DDL string must be EXTENDED (append new table DDL), not replaced.
CRITICAL: Do NOT rename or modify the existing `cases` table — it has foreign key references from entities/detections.

New table name: `investigation_cases` (NOT `cases` — conflict with existing table).

From backend/causality/causality_routes.py (empty router stub pattern):
```python
from fastapi import APIRouter
investigation_router = APIRouter(prefix="/api", tags=["investigation"])
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create backend/investigation/ package with 6 stub modules + extend SQLiteStore DDL</name>
  <files>
    backend/investigation/__init__.py
    backend/investigation/case_manager.py
    backend/investigation/hunt_engine.py
    backend/investigation/timeline_builder.py
    backend/investigation/artifact_store.py
    backend/investigation/tagging.py
    backend/investigation/investigation_routes.py
    backend/stores/sqlite_store.py
  </files>
  <action>
    1. Create `backend/investigation/__init__.py` — empty file (package marker only).

    2. Create `backend/investigation/case_manager.py` — stub class with pass-through methods:
    ```python
    """Case manager stub — Phase 7 Plan 01 will implement."""
    class CaseManager:
        def create_investigation_case(self, sqlite_conn, title: str, description: str = "", case_id=None) -> str:
            raise NotImplementedError
        def get_investigation_case(self, sqlite_conn, case_id: str) -> dict | None:
            raise NotImplementedError
        def list_investigation_cases(self, sqlite_conn, status=None) -> list:
            raise NotImplementedError
        def update_investigation_case(self, sqlite_conn, case_id: str, updates: dict) -> None:
            raise NotImplementedError
    ```

    3. Create `backend/investigation/hunt_engine.py` — stub with empty HUNT_TEMPLATES dict and stub execute_hunt:
    ```python
    """Hunt engine stub — Phase 7 Plan 02 will implement."""
    HUNT_TEMPLATES: dict = {}

    async def execute_hunt(duckdb_store, template_name: str, params: dict) -> list:
        raise NotImplementedError
    ```

    4. Create `backend/investigation/timeline_builder.py` — stub:
    ```python
    """Timeline builder stub — Phase 7 Plan 03 will implement."""
    async def build_timeline(case_id: str, duckdb_store, sqlite_store) -> list:
        raise NotImplementedError
    ```

    5. Create `backend/investigation/artifact_store.py` — stub:
    ```python
    """Artifact store stub — Phase 7 Plan 03 will implement."""
    async def save_artifact(data_dir: str, case_id: str, artifact_id: str, filename: str, content: bytes, sqlite_store) -> dict:
        raise NotImplementedError
    ```

    6. Create `backend/investigation/tagging.py` — stub:
    ```python
    """Tagging stub — Phase 7 Plan 01 will implement."""
    def add_tag(sqlite_conn, case_id: str, tag: str) -> None:
        raise NotImplementedError

    def remove_tag(sqlite_conn, case_id: str, tag: str) -> None:
        raise NotImplementedError

    def list_tags(sqlite_conn, case_id: str) -> list:
        raise NotImplementedError
    ```

    7. Create `backend/investigation/investigation_routes.py` — empty router stub:
    ```python
    """Investigation router stub — Phase 7 Plans 01-04 will add endpoints."""
    from fastapi import APIRouter

    investigation_router = APIRouter(prefix="/api", tags=["investigation"])
    ```

    8. Extend `backend/stores/sqlite_store.py` _DDL string — APPEND the following DDL to the existing _DDL string (do NOT replace any existing content):

    Append to the _DDL string before the closing triple-quote:
    ```sql
    CREATE TABLE IF NOT EXISTS investigation_cases (
        case_id         TEXT PRIMARY KEY,
        title           TEXT NOT NULL,
        description     TEXT DEFAULT '',
        case_status     TEXT NOT NULL DEFAULT 'open',
        related_alerts  TEXT DEFAULT '[]',
        related_entities TEXT DEFAULT '[]',
        timeline_events TEXT DEFAULT '[]',
        analyst_notes   TEXT DEFAULT '',
        tags            TEXT DEFAULT '[]',
        artifacts       TEXT DEFAULT '[]',
        created_at      TEXT NOT NULL,
        updated_at      TEXT NOT NULL
    );

    CREATE TABLE IF NOT EXISTS case_artifacts (
        artifact_id     TEXT PRIMARY KEY,
        case_id         TEXT NOT NULL,
        filename        TEXT NOT NULL,
        file_path       TEXT NOT NULL,
        file_size       INTEGER,
        mime_type       TEXT,
        description     TEXT DEFAULT '',
        created_at      TEXT NOT NULL,
        FOREIGN KEY (case_id) REFERENCES investigation_cases (case_id)
    );

    CREATE TABLE IF NOT EXISTS case_tags (
        id              INTEGER PRIMARY KEY AUTOINCREMENT,
        case_id         TEXT NOT NULL,
        tag             TEXT NOT NULL,
        created_at      TEXT NOT NULL,
        UNIQUE(case_id, tag),
        FOREIGN KEY (case_id) REFERENCES investigation_cases (case_id)
    );

    CREATE INDEX IF NOT EXISTS idx_inv_cases_status ON investigation_cases (case_status);
    CREATE INDEX IF NOT EXISTS idx_artifacts_case   ON case_artifacts (case_id);
    CREATE INDEX IF NOT EXISTS idx_tags_case        ON case_tags (case_id);
    ```

    Also add stub SQLiteStore methods for Phase 7 (append to the class, before the final closing):
    - `create_investigation_case(self, title, description='', case_id=None) -> str` — raise NotImplementedError
    - `get_investigation_case(self, case_id) -> dict | None` — raise NotImplementedError
    - `list_investigation_cases(self, status=None) -> list` — raise NotImplementedError
    - `update_investigation_case(self, case_id, updates) -> None` — raise NotImplementedError
    - `insert_artifact(self, artifact_id, case_id, filename, file_path, file_size) -> None` — raise NotImplementedError
    - `get_artifacts_by_case(self, case_id) -> list` — raise NotImplementedError
  </action>
  <verify>
    <automated>cd /c/Users/Admin/AI-SOC-Brain && uv run python -c "from backend.investigation import case_manager, hunt_engine, timeline_builder, artifact_store, tagging, investigation_routes; print('All stubs import OK')"</automated>
  </verify>
  <done>All 7 backend/investigation/ files exist and import without errors. SQLiteStore._DDL contains investigation_cases table DDL.</done>
</task>

<task type="auto">
  <name>Task 2: Write test_phase7.py with 16 xfail stubs (P7-T01 through P7-T16)</name>
  <files>
    backend/src/tests/test_phase7.py
  </files>
  <action>
    Create `backend/src/tests/test_phase7.py` following the exact pattern from `test_phase6.py`.

    File structure:
    - Module docstring explaining Wave 0 stubs
    - `import pytest`, `from fastapi.testclient import TestClient`, `from backend.src.api.main import app`
    - `client = TestClient(app)`
    - 7 test classes, each with deferred imports inside test methods, all `@pytest.mark.xfail(strict=False, reason="...")`

    Test classes and methods (map to P7-T01 through P7-T16):

    **TestCaseManager** (P7-T01, P7-T02, P7-T03):
    - `test_create_case_returns_id`: import `from backend.investigation.case_manager import CaseManager`; instantiate CaseManager, call `create_investigation_case` on an in-memory sqlite3.connect(':memory:') connection (after running the investigation_cases DDL); assert result is a string UUID.
    - `test_list_cases_empty`: same setup; call `list_investigation_cases`; assert result == [].
    - `test_update_case_status`: create a case, call `update_investigation_case(conn, case_id, {"case_status": "in-progress"})`; get case back; assert case_status == "in-progress".

    **TestCaseAPI** (P7-T04, P7-T05, P7-T06, P7-T07):
    - `test_create_case_endpoint`: `client.post("/api/cases", json={"title": "Test Case", "description": ""})` → assert response.status_code == 200 and "case_id" in response.json().
    - `test_list_cases_endpoint`: `client.get("/api/cases")` → assert response.status_code == 200.
    - `test_get_case_detail`: create a case via POST; GET `/api/cases/{case_id}` → assert 200 and response contains "case_status".
    - `test_patch_case_status`: create case, PATCH `/api/cases/{case_id}` with `{"case_status": "in-progress"}` → assert 200.

    **TestHuntEngine** (P7-T08, P7-T09):
    - `test_suspicious_ip_template`: `from backend.investigation.hunt_engine import HUNT_TEMPLATES`; assert "suspicious_ip_comms" in HUNT_TEMPLATES; assert "dst_ip" in HUNT_TEMPLATES["suspicious_ip_comms"].param_keys.
    - `test_powershell_children_template`: assert "powershell_children" in HUNT_TEMPLATES; assert HUNT_TEMPLATES["powershell_children"].sql contains "parent_process_name".

    **TestHuntAPI** (P7-T10, P7-T11):
    - `test_list_hunt_templates`: `client.get("/api/hunt/templates")` → assert 200 and len(response.json()["templates"]) == 4.
    - `test_execute_hunt`: `client.post("/api/hunt", json={"template": "powershell_children", "params": {}})` → assert 200 and "results" in response.json().

    **TestTimelineBuilder** (P7-T12):
    - `test_timeline_entry_shape`: `from backend.investigation.timeline_builder import build_timeline`; run via `asyncio.run(build_timeline("test-case", None, None))` but catch NotImplementedError; assert the function is callable (just checks it exists with the right name).
    NOTE: Since build_timeline raises NotImplementedError in stub, the test just checks the function exists — the xfail will catch the NotImplementedError as a failure which causes xfail.

    **TestTimelineAPI** (P7-T13):
    - `test_get_timeline`: create case, `client.get(f"/api/cases/{case_id}/timeline")` → assert 200 and "timeline" in response.json().

    **TestArtifactStore** (P7-T14):
    - `test_save_artifact`: `from backend.investigation.artifact_store import save_artifact`; call via asyncio.run with tmp_path-equivalent (use tempfile.mkdtemp); assert result has "artifact_id" key.

    **TestArtifactAPI** (P7-T15):
    - `test_upload_artifact`: create case, `client.post(f"/api/cases/{case_id}/artifacts", files={"file": ("test.txt", b"evidence data", "text/plain")}, data={"description": "test artifact"})` → assert 200 and "artifact_id" in response.json().

    **Build verification** (P7-T16) — standalone test function (not in a class):
    - `test_dashboard_build`: use subprocess to run `npm run build` in `dashboard/` directory; mark xfail; this test is checked at Wave 4 only.

    IMPORTANT: All tests must be `@pytest.mark.xfail(strict=False, ...)`. The xfail prevents test failures from blocking the suite. "strict=False" means an unexpected XPASS is also acceptable (does not break the suite).
  </action>
  <verify>
    <automated>cd /c/Users/Admin/AI-SOC-Brain && uv run pytest backend/src/tests/test_phase7.py -v --tb=short 2>&1 | tail -30</automated>
  </verify>
  <done>test_phase7.py has 16 test methods. All show as XFAIL (not ERROR) in pytest output. Full regression suite still passes: `uv run pytest backend/src/tests/ -v --tb=short` shows no new failures.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. `uv run python -c "from backend.investigation.investigation_routes import investigation_router; print(investigation_router)"` — prints the router object.
2. `uv run pytest backend/src/tests/test_phase7.py -v` — 16 xfail, 0 errors, 0 failures.
3. `uv run pytest backend/src/tests/ -v --tb=short 2>&1 | tail -20` — no regressions in existing tests.
4. `uv run python -c "from backend.stores.sqlite_store import SQLiteStore; s = SQLiteStore(':memory:'); print('DDL OK')"` — DDL runs without error (CREATE TABLE IF NOT EXISTS is idempotent).
</verification>

<success_criteria>
- backend/investigation/ package has 7 files (6 stubs + investigation_routes.py)
- test_phase7.py exists with 16 xfail-marked test methods
- SQLiteStore._DDL includes investigation_cases, case_artifacts, case_tags table definitions
- `uv run pytest backend/src/tests/ -v` shows 0 new failures (xfail is not failure)
- All 6 stub modules importable from their canonical paths
</success_criteria>

<output>
After completion, create `.planning/phases/07-threat-hunting-case-management/07-00-SUMMARY.md`
</output>
