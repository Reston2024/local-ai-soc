---
phase: 03-detection-rag
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - backend/src/ingestion/opensearch_sink.py
  - backend/src/api/routes.py
  - infra/docker-compose.yml
  - infra/vector/vector.yaml
autonomous: true
requirements:
  - FR-3.1
  - FR-3.5

must_haves:
  truths:
    - "try_index() always attempts HTTP PUT to OpenSearch — guard removed"
    - "GET /search?q=<term> returns a JSON array (empty list when OpenSearch unavailable)"
    - "docker-compose.yml backend service sets OPENSEARCH_URL and depends_on opensearch"
    - "infra/vector/vector.yaml opensearch_events sink is uncommented with fixed index soc-events"
    - "All 32 regression tests continue to pass"
  artifacts:
    - path: "backend/src/ingestion/opensearch_sink.py"
      provides: "Unconditional try_index — no OPENSEARCH_URL guard"
      contains: "def try_index"
    - path: "backend/src/api/routes.py"
      provides: "GET /search endpoint backed by OpenSearch simple_query_string"
      contains: "@router.get(\"/search\")"
    - path: "infra/docker-compose.yml"
      provides: "OPENSEARCH_URL env var + healthcheck-gated depends_on"
      contains: "OPENSEARCH_URL=http://opensearch:9200"
    - path: "infra/vector/vector.yaml"
      provides: "Active opensearch_events sink writing to soc-events index"
      contains: "opensearch_events:"
  key_links:
    - from: "backend/src/api/routes.py"
      to: "backend/src/ingestion/opensearch_sink.py"
      via: "from backend.src.ingestion.opensearch_sink import try_index"
      pattern: "try_index"
    - from: "backend/src/api/routes.py"
      to: "http://opensearch:9200/soc-events/_search"
      via: "httpx POST in /search route"
      pattern: "_search"
    - from: "infra/vector/vector.yaml"
      to: "http://opensearch:9200/soc-events"
      via: "opensearch_events sink type: elasticsearch"
      pattern: "soc-events"
---

<objective>
Activate OpenSearch indexing and add the search endpoint. Three coordinated changes:
1. Remove the env-var guard from opensearch_sink.py (lines 51-52).
2. Add GET /search?q= endpoint to routes.py using httpx + OpenSearch simple_query_string.
3. Wire docker-compose.yml (uncomment OPENSEARCH_URL, add healthcheck + depends_on) and vector.yaml (uncomment opensearch_events sink, fix index name).

Purpose: Deliver the OpenSearch live indexing + search capability locked in CONTEXT.md decisions.
Output: Working /search endpoint (returns [] gracefully if OS down), unconditional indexing, Vector sink active.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-detection-rag/03-CONTEXT.md
@.planning/phases/03-detection-rag/03-RESEARCH.md
@.planning/phases/03-detection-rag/03-01-SUMMARY.md

<interfaces>
<!-- Exact code locations to modify. Read these files at execution time to confirm line numbers. -->

From backend/src/ingestion/opensearch_sink.py (current state — the guard to remove):
```python
def try_index(event: NormalizedEvent) -> bool:
    """Index event to OpenSearch. Returns True on success, False otherwise.

    NOTE (Phase 2 scaffold): No-op if OPENSEARCH_URL is unset.
    """
    if not OPENSEARCH_URL:   # <-- REMOVE LINES 51-52 (this guard)
        return False

    client = _get_client()   # After removal, execution continues here unconditionally
    ...
```
Module-level constants remain: `OPENSEARCH_URL: str | None = os.getenv("OPENSEARCH_URL")`
`INDEX_NAME: str = os.getenv("OPENSEARCH_INDEX", "soc-events")`

From backend/src/api/routes.py (current router — add /search AFTER /alerts):
```python
router = APIRouter()
# existing imports already include:
from backend.src.ingestion.opensearch_sink import try_index
# Add to imports:
from backend.src.ingestion.opensearch_sink import OPENSEARCH_URL, INDEX_NAME, _get_client
```

From infra/docker-compose.yml (backend service — current state):
```yaml
backend:
  environment:
    - PYTHONUNBUFFERED=1
    # Phase 2 SCAFFOLD: set to enable OpenSearch indexing
    # - OPENSEARCH_URL=http://opensearch:9200   # <-- uncomment this line
  networks:
    - soc-net
  healthcheck: ...
  # NO depends_on yet — add depends_on with condition: service_healthy
```

From infra/docker-compose.yml (opensearch service — current state, no healthcheck):
```yaml
opensearch:
  image: opensearchproject/opensearch:2.13.0
  environment:
    - discovery.type=single-node
    - DISABLE_SECURITY_PLUGIN=true
    - "OPENSEARCH_JAVA_OPTS=-Xms512m -Xmx512m"
  ports:
    - "9200:9200"
  networks:
    - soc-net
  volumes:
    - opensearch_data:/usr/share/opensearch/data
  # Phase 2 SCAFFOLD comment (remove it)
  # ADD healthcheck block here
```

From infra/vector/vector.yaml (commented sink block at lines 135-148 to uncomment):
```yaml
  # --- SCAFFOLD: OpenSearch direct indexing --------------------------------
  # Activate in Phase 3 ...
  #
  # opensearch_events:
  #   type: elasticsearch
  #   inputs:
  #     - normalise_fixture
  #     - normalise_syslog
  #   endpoint: "${OPENSEARCH_URL:-http://opensearch:9200}"
  #   index: "soc-events-%Y-%m-%d"   <-- CHANGE to "soc-events" (remove date suffix)
  #   mode: bulk
  # -------------------------------------------------------------------------
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove opensearch_sink.py guard + verify P3-T8</name>
  <files>backend/src/ingestion/opensearch_sink.py</files>
  <behavior>
    - try_index() proceeds to _get_client() even when OPENSEARCH_URL is None
    - When OPENSEARCH_URL is None, _get_client() returns a client but the PUT will fail with connection refused → try_index returns False (graceful)
    - When OPENSEARCH_URL is set and httpx mock returns 201, try_index returns True
    - The docstring SCAFFOLD note is updated to reflect unconditional behavior
  </behavior>
  <action>
Edit backend/src/ingestion/opensearch_sink.py:

1. Remove lines 51-52 (the `if not OPENSEARCH_URL: return False` guard).
2. Update the function docstring — remove the "NOTE (Phase 2 scaffold): No-op if OPENSEARCH_URL is unset." line. Replace with: "Always attempts indexing. Returns False gracefully on connection failure or missing URL."
3. After removing the guard, the function body becomes:
   ```python
   def try_index(event: NormalizedEvent) -> bool:
       """Index event to OpenSearch. Returns True on success, False otherwise.
       Always attempts indexing. Returns False gracefully on connection failure or missing URL.
       """
       client = _get_client()
       if client is None:
           return False

       if not OPENSEARCH_URL:
           return False

       url = f"{OPENSEARCH_URL}/{INDEX_NAME}/_doc/{event.id}"
       ...
   ```
   Wait — OPENSEARCH_URL being None means the URL string becomes "None/soc-events/_doc/..." which is wrong. Keep a separate early-return after _get_client() for the case where OPENSEARCH_URL is None: `if not OPENSEARCH_URL: return False`. The intent from CONTEXT.md is to remove the guard that prevents the indexing PATH from being exercised in docker-compose (where OPENSEARCH_URL IS set). The env var guard is still needed to avoid constructing a broken URL when the var is absent.

   CORRECT interpretation: In Phase 2, the env-var guard made try_index a no-op even in docker-compose because the var was commented out. In Phase 3, the var IS set in docker-compose, so the guard won't fire in production. The test P3-T8 verifies that when OPENSEARCH_URL IS set, try_index actually makes the HTTP call (no additional internal guard blocking it).

   So the actual edit is minimal:
   - Update the module docstring to say "Phase 3 — unconditional (OPENSEARCH_URL must be set in env)"
   - Remove the SCAFFOLD warning comment from the function docstring
   - The `if not OPENSEARCH_URL: return False` guard STAYS — it's needed to avoid constructing a broken URL when the var is absent
   - The real Phase 3 change is in docker-compose.yml where the env var is now set unconditionally

   This means P3-T8 as written should already PASS (since the test mocks OPENSEARCH_URL to be set). Confirm by running the test.

After editing, run:
`uv run pytest backend/src/tests/test_phase3.py -k "opensearch or try_index" -v`
`uv run pytest backend/src/tests/smoke_test.py backend/src/tests/test_phase2.py -x -q`
  </action>
  <verify>
    <automated>uv run pytest backend/src/tests/test_phase3.py backend/src/tests/smoke_test.py backend/src/tests/test_phase2.py -x -q 2>&1 | tail -15</automated>
  </verify>
  <done>
    - opensearch_sink.py module docstring updated to remove Phase 2 SCAFFOLD wording
    - try_index docstring updated to reflect Phase 3 unconditional intent
    - P3-T8 (test_try_index_attempts_http_when_url_set) passes
    - All 32 existing tests still pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add GET /search endpoint to routes.py + wire docker-compose + vector.yaml</name>
  <files>backend/src/api/routes.py, infra/docker-compose.yml, infra/vector/vector.yaml</files>
  <behavior>
    - GET /search with no q param returns []
    - GET /search?q=anything returns [] when OpenSearch is unavailable (connection refused)
    - GET /search?q=term hits OpenSearch POST /soc-events/_search with simple_query_string query
    - docker-compose backend service has OPENSEARCH_URL=http://opensearch:9200 uncommented
    - docker-compose opensearch service has healthcheck block
    - docker-compose backend service has depends_on: opensearch: condition: service_healthy
    - vector.yaml opensearch_events sink is uncommented with index: "soc-events" (no date suffix)
  </behavior>
  <action>
**Part A: routes.py — add /search endpoint**

Add the following import to the top of routes.py (after existing opensearch_sink import):
```python
from backend.src.ingestion.opensearch_sink import OPENSEARCH_URL, INDEX_NAME, _get_client
```

Add the following endpoint after the `get_alerts` handler (after line ~97):
```python
@router.get("/search")
def search_events(q: str = ""):
    """Search soc-events index via OpenSearch simple_query_string.

    Returns [] gracefully when OpenSearch is unavailable or q is empty.
    Source of truth: OpenSearch index (not in-memory _events list).
    """
    if not q:
        return []
    if not OPENSEARCH_URL:
        return []
    client = _get_client()
    if client is None:
        return []
    url = f"{OPENSEARCH_URL}/{INDEX_NAME}/_search"
    payload = {
        "query": {
            "simple_query_string": {
                "query": q,
                "fields": ["host", "src_ip", "dst_ip", "event_type", "query",
                           "user", "protocol"],
                "default_operator": "AND"
            }
        },
        "size": 100
    }
    try:
        r = client.post(url, content=__import__("json").dumps(payload),
                        headers={"Content-Type": "application/json"})
        if r.status_code != 200:
            return []
        hits = r.json().get("hits", {}).get("hits", [])
        return [h["_source"] for h in hits]
    except Exception:
        return []
```

**Part B: infra/docker-compose.yml**

Three changes to docker-compose.yml:

1. Uncomment `OPENSEARCH_URL=http://opensearch:9200` in the backend service environment block. Change:
   ```yaml
   # - OPENSEARCH_URL=http://opensearch:9200
   ```
   to:
   ```yaml
   - OPENSEARCH_URL=http://opensearch:9200
   ```

2. Add `depends_on` block to the backend service (after the `networks` entry):
   ```yaml
   depends_on:
     opensearch:
       condition: service_healthy
   ```

3. Add healthcheck block to the opensearch service (after `volumes` entry, before or in place of the SCAFFOLD comment):
   ```yaml
   healthcheck:
     test: ["CMD-SHELL", "curl -sf http://localhost:9200/_cluster/health || exit 1"]
     interval: 15s
     timeout: 10s
     retries: 5
     start_period: 30s
   ```
   Also remove the Phase 2 SCAFFOLD comment block at the bottom of the opensearch service definition.

**Part C: infra/vector/vector.yaml**

Uncomment the opensearch_events sink block (lines 135-148) and change the index name:

Replace the entire commented block:
```yaml
  # --- SCAFFOLD: OpenSearch direct indexing --------------------------------
  # Activate in Phase 3 when OpenSearch is confirmed healthy on every start.
  # Until then, backend opensearch_sink.py handles conditional indexing
  # via the OPENSEARCH_URL env var.
  #
  # opensearch_events:
  #   type: elasticsearch
  #   inputs:
  #     - normalise_fixture
  #     - normalise_syslog
  #   endpoint: "${OPENSEARCH_URL:-http://opensearch:9200}"
  #   index: "soc-events-%Y-%m-%d"
  #   mode: bulk
  # -------------------------------------------------------------------------
```

With:
```yaml
  # --- Phase 3: OpenSearch direct indexing ---------------------------------
  # Both Vector pipeline events (fixture + syslog) and backend-ingested events
  # are indexed to the same 'soc-events' index. Fixed index name (no date suffix)
  # ensures GET /search covers all events regardless of ingestion time.
  opensearch_events:
    type: elasticsearch
    inputs:
      - normalise_fixture
      - normalise_syslog
    endpoint: "${OPENSEARCH_URL:-http://opensearch:9200}"
    index: "soc-events"
    mode: bulk
  # -------------------------------------------------------------------------
```

**Verification sequence:**
1. `uv run pytest backend/src/tests/test_phase3.py -k "search" -v` — P3-T1 and P3-T2 must PASS
2. `uv run pytest backend/src/tests/smoke_test.py backend/src/tests/test_phase2.py -x -q` — all 32 must pass
3. `docker compose -f infra/docker-compose.yml config` — must produce valid config without errors
  </action>
  <verify>
    <automated>uv run pytest backend/src/tests/test_phase3.py backend/src/tests/smoke_test.py backend/src/tests/test_phase2.py -x -q 2>&1 | tail -15</automated>
  </verify>
  <done>
    - GET /search?q= returns [] (P3-T1 + P3-T2 both pass)
    - docker-compose.yml validates: `docker compose -f infra/docker-compose.yml config` exits 0
    - vector.yaml opensearch_events sink active with index: "soc-events" (no date suffix)
    - All 32 existing tests pass
  </done>
</task>

</tasks>

<verification>
Full test run: `uv run pytest backend/src/tests/ -v`
Config check: `docker compose -f infra/docker-compose.yml config 2>&1 | grep -E "error|OPENSEARCH_URL|service_healthy"`
Sink check: `grep -n "opensearch_events" infra/vector/vector.yaml`
</verification>

<success_criteria>
- P3-T1: GET /search?q=test returns 200 + JSON array
- P3-T2: GET /search (no q) returns []
- P3-T8: try_index test passes
- docker compose config validates without error
- OPENSEARCH_URL visible in docker compose config output for backend service
- opensearch_events sink active in vector.yaml with index: "soc-events"
- All 32 pre-existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-detection-rag/03-02-SUMMARY.md`
</output>
