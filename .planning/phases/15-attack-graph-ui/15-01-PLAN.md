---
phase: 15-attack-graph-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/test_graph_api.py
  - dashboard/package.json
  - dashboard/package-lock.json
autonomous: true
requirements:
  - P15-T01
  - P15-T02
must_haves:
  truths:
    - "Three xfail tests for the new graph endpoints exist and fail with XFAIL (not ERROR)"
    - "cytoscape-fcose and cytoscape-dagre are present in dashboard/node_modules"
    - "npm run build exits 0 with the new packages installed"
  artifacts:
    - path: "tests/unit/test_graph_api.py"
      provides: "Wave 0 xfail stubs for P15-T01 new endpoints"
      contains: "test_investigation_graph, test_global_graph, test_global_route_precedence"
    - path: "dashboard/package.json"
      provides: "cytoscape-fcose and cytoscape-dagre dependencies declared"
  key_links:
    - from: "tests/unit/test_graph_api.py"
      to: "/api/graph/{investigation_id} and /api/graph/global"
      via: "TestClient xfail stubs"
      pattern: "pytest.mark.xfail"
---

<objective>
Wave 0 setup: add three xfail test stubs for the new graph API endpoints to the existing test file, and install the Cytoscape layout plugins that Plan 03 will import.

Purpose: Establish RED tests before implementation (Nyquist rule). Give Plan 02 executor clear failing tests to satisfy. Install npm packages needed by Plan 03 to avoid blocking it mid-task.
Output: Three xfail stubs in test_graph_api.py; cytoscape-fcose@2.2.0 and cytoscape-dagre@2.5.0 in dashboard/node_modules.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-attack-graph-ui/15-RESEARCH.md

@tests/unit/test_graph_api.py
@dashboard/package.json
</context>

<interfaces>
<!-- Existing test infrastructure from tests/unit/test_graph_api.py -->
<!-- _build_app(tmp_path) returns (TestClient, SQLiteStore) -->
<!-- All existing tests use pytestmark = pytest.mark.unit -->
<!-- SQLiteStore.upsert_entity(id, type, name, attributes, case_id) is available -->
<!-- SQLiteStore.create_case(name, description, case_id=None) is available -->

<!-- New endpoints the stubs will test (implemented in Plan 02): -->
<!-- GET /api/graph/global?limit=N  → { entities: [...], edges: [...], total_entities: N } -->
<!-- GET /api/graph/{investigation_id} → { entities: [...], edges: [...], case_id: str } -->
<!-- Route ordering: /global MUST be declared before /{investigation_id} in router -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add xfail stubs for new graph endpoints to test_graph_api.py</name>
  <files>tests/unit/test_graph_api.py</files>
  <action>
    Append a new class `TestPhase15NewEndpoints` to the END of tests/unit/test_graph_api.py (do not modify any existing tests). The class must contain exactly three methods:

    1. `test_investigation_graph(self, tmp_path)` — marked `@pytest.mark.xfail(reason="P15-T01: GET /api/graph/{investigation_id} not yet implemented", strict=True)`.
       Body: call `client.get("/api/graph/inv-001")` where "inv-001" is NOT a known case. Assert `resp.status_code == 200` AND `"entities" in resp.json()`.
       This will xfail because the endpoint does not exist yet (returns 404 or 422).

    2. `test_global_graph(self, tmp_path)` — marked `@pytest.mark.xfail(reason="P15-T01: GET /api/graph/global not yet implemented", strict=True)`.
       Body: call `client.get("/api/graph/global")`. Assert `resp.status_code == 200` AND `"entities" in resp.json()`.

    3. `test_global_route_precedence(self, tmp_path)` — marked `@pytest.mark.xfail(reason="P15-T01: /global route precedence over /{investigation_id} not yet implemented", strict=True)`.
       Body: call `client.get("/api/graph/global")`. Assert `resp.status_code == 200` (i.e. "global" is NOT treated as an investigation_id by routing).

    Each method must call `_build_app(tmp_path)` to get `(client, sqlite)`.

    IMPORTANT: Use `strict=True` on all xfail marks. This means once Plan 02 implements the endpoints correctly, these tests become xpass (expected pass), prompting removal of the xfail mark.
  </action>
  <verify>
    <automated>cd /c/Users/Admin/AI-SOC-Brain && uv run pytest tests/unit/test_graph_api.py::TestPhase15NewEndpoints -v 2>&1 | tail -20</automated>
  </verify>
  <done>Three tests run and show XFAIL (not ERROR, not PASSED). All other tests in the file remain green.</done>
</task>

<task type="auto">
  <name>Task 2: Install cytoscape-fcose and cytoscape-dagre npm packages</name>
  <files>dashboard/package.json, dashboard/package-lock.json</files>
  <action>
    Run:
      cd /c/Users/Admin/AI-SOC-Brain/dashboard && npm install cytoscape-fcose@2.2.0 cytoscape-dagre@2.5.0

    After install verify:
    1. `dashboard/node_modules/cytoscape-fcose` directory exists
    2. `dashboard/node_modules/cytoscape-dagre` directory exists
    3. Both appear in `dashboard/package.json` under `dependencies`

    Do NOT add TypeScript type packages (@types/cytoscape-fcose) — they are not needed because the existing codebase casts layout options with `as any` (confirmed in GraphView.svelte line 178: `{ name: 'cose', ... } as any`).

    Run `npm run build` to verify the dashboard still compiles with the new packages present.
  </action>
  <verify>
    <automated>cd /c/Users/Admin/AI-SOC-Brain/dashboard && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>npm run build exits 0. package.json lists cytoscape-fcose@2.2.0 and cytoscape-dagre@2.5.0 under dependencies.</done>
</task>

</tasks>

<verification>
Full check after both tasks:
1. `uv run pytest tests/unit/test_graph_api.py -v 2>&1 | tail -30` — all pre-existing tests PASS, three new tests XFAIL
2. `cd dashboard && npm run build` — exits 0
</verification>

<success_criteria>
- TestPhase15NewEndpoints has 3 methods all marked xfail(strict=True)
- Running the 3 new tests yields XFAIL result (not ERROR)
- cytoscape-fcose and cytoscape-dagre present in node_modules and package.json
- npm run build exits 0
- No existing tests broken
</success_criteria>

<output>
After completion, create `.planning/phases/15-attack-graph-ui/15-01-SUMMARY.md`
</output>
