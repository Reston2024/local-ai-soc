---
phase: 34-asset-inventory
plan: 04
type: execute
wave: 3
depends_on:
  - 34-03
files_modified:
  - dashboard/src/views/AssetsView.svelte
  - dashboard/src/views/AttackCoverageView.svelte
  - dashboard/src/lib/api.ts
  - dashboard/src/App.svelte
autonomous: false
requirements:
  - P34-T09

must_haves:
  truths:
    - "AssetsView shows a table with columns: hostname/IP, tag chip (internal/external), risk score, last seen, alert count"
    - "Clicking an asset row expands an inline detail panel with three blocks: event timeline, associated detections, OSINT enrichment"
    - "AttackCoverageView shows 14 tactic columns in MITRE_TACTICS order, each showing covered/total count badge"
    - "Tactic column click expands an inline list of techniques with covered (checkmark) or uncovered (dash) status"
    - "Heat colours applied correctly: 0 rules=#333, 1-2=#7a4400, 3-9=#c96a00, 10+=#e84a00"
    - "ATT&CK Coverage nav entry appears in the Intelligence sidebar group between Threat Intel and Threat Map"
    - "AssetsView nav entry exists in the sidebar (was previously a shell — now fully wired)"
    - "TypeScript compiles clean: npm run check passes with zero errors"
  artifacts:
    - path: "dashboard/src/views/AssetsView.svelte"
      provides: "Full rewrite — IP-centric asset table with inline detail panel (event timeline + detections + OSINT)"
    - path: "dashboard/src/views/AttackCoverageView.svelte"
      provides: "New 14-column ATT&CK heatmap view with inline tactic drill-down"
    - path: "dashboard/src/lib/api.ts"
      provides: "Asset and Attack typed API client methods: api.assets.list(), api.assets.get(ip), api.assets.tag(ip, tag), api.attack.coverage(), api.attack.actorMatches()"
    - path: "dashboard/src/App.svelte"
      provides: "Route bindings and sidebar nav entries for AssetsView and AttackCoverageView"
  key_links:
    - from: "dashboard/src/views/AssetsView.svelte"
      to: "dashboard/src/lib/api.ts"
      via: "api.assets.list() in $effect on mount; api.assets.get(ip) on row expand"
      pattern: "api.assets"
    - from: "dashboard/src/views/AttackCoverageView.svelte"
      to: "dashboard/src/lib/api.ts"
      via: "api.attack.coverage() in $effect on mount"
      pattern: "api.attack.coverage"
    - from: "dashboard/src/App.svelte"
      to: "dashboard/src/views/AssetsView.svelte"
      via: "route binding + sidebar nav item"
      pattern: "AssetsView"
---

<objective>
Full rewrite of AssetsView.svelte, creation of AttackCoverageView.svelte, new typed API methods in api.ts, and sidebar/routing wiring in App.svelte.

Purpose: Makes asset inventory and ATT&CK heatmap visible to the analyst. AssetsView replaces the old coverage shell with the real IP-centric table + detail panel. AttackCoverageView delivers the 14-column heat-scaled grid.
Output: Two functional Svelte views, typed API client extensions, nav wiring. Followed by a human-verify checkpoint.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/34-asset-inventory/34-CONTEXT.md
@.planning/phases/34-asset-inventory/34-RESEARCH.md
@.planning/phases/34-asset-inventory/34-03-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From dashboard/src/lib/api.ts (existing client pattern to follow):
```typescript
export const api = {
  intel: {
    iocHits: async (limit = 100): Promise<IocHit[]> => {
      const res = await fetch(`/api/intel/ioc-hits?limit=${limit}`, {
        headers: { Authorization: `Bearer ${getToken()}` },
      })
      if (!res.ok) throw new Error(await res.text())
      return res.json()
    },
  },
}
```

New TypeScript interfaces to add to api.ts:
```typescript
export interface Asset {
  ip: string
  hostname: string | null
  tag: "internal" | "external"
  risk_score: number
  last_seen: string
  first_seen: string
  alert_count: number
}

export interface TacticCoverage {
  tactic: string
  tactic_short: string
  total_techniques: number
  covered_count: number
  techniques: Array<{
    tech_id: string
    name: string
    covered: boolean
    rule_titles: string[]
  }>
}

export interface ActorMatch {
  name: string
  aliases: string[]
  group_id: string
  overlap_pct: number
  confidence: "High" | "Medium" | "Low"
  matched_count: number
  total_count: number
}
```

New api groups to add:
```typescript
assets: {
  list: async (limit = 200): Promise<Asset[]>  // GET /api/assets?limit={limit}
  get:  async (ip: string): Promise<Asset>     // GET /api/assets/{encodeURIComponent(ip)}
  tag:  async (ip: string, tag: string): Promise<void>  // POST /api/assets/{ip}/tag body {tag}
}
attack: {
  coverage:     async (): Promise<TacticCoverage[]>  // GET /api/attack/coverage
  actorMatches: async (): Promise<ActorMatch[]>      // GET /api/attack/actor-matches
}
```

From ThreatIntelView.svelte (expand/collapse UX pattern):
- let expandedId = $state(null) — string | null
- function toggleExpand(id) { expandedId = expandedId === id ? null : id }
- Template: {#if expandedId === item.id} inline panel {/if}

Heat colour mapping (locked in CONTEXT.md):
- 0 rules: #333, 1-2: #7a4400, 3-9: #c96a00, 10+: #e84a00

App.svelte Intelligence group already has Threat Intel and Threat Map.
Add "ATT&CK Coverage" between them. Read App.svelte for exact nav item component before editing.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: api.ts — Asset and Attack typed methods</name>
  <files>dashboard/src/lib/api.ts</files>
  <action>
    Read dashboard/src/lib/api.ts fully to understand the existing structure before editing.

    Add the three TypeScript interfaces (Asset, TacticCoverage, ActorMatch) near the other interface definitions at the top.

    Add two new method groups to the api object:

    assets group:
    - list(limit = 200): GET /api/assets?limit={limit} returning Asset[]
    - get(ip: string): GET /api/assets/{encodeURIComponent(ip)} returning Asset
    - tag(ip: string, tag: string): POST /api/assets/{encodeURIComponent(ip)}/tag with body JSON.stringify({tag}) and Content-Type: application/json header

    attack group:
    - coverage(): GET /api/attack/coverage returning TacticCoverage[]
    - actorMatches(): GET /api/attack/actor-matches returning ActorMatch[]

    All fetches include: headers: { Authorization: `Bearer ${getToken()}` }
    All fetches throw on !res.ok.
    Use encodeURIComponent for IP path segments.

    After editing: cd dashboard && npm run check — zero errors required.
  </action>
  <verify>
    <automated>cd /c/Users/Admin/AI-SOC-Brain/dashboard && npm run check 2>&1 | tail -10</automated>
  </verify>
  <done>npm run check passes with zero errors. Asset, TacticCoverage, ActorMatch interfaces exported. api.assets and api.attack groups present with all 5 methods.</done>
</task>

<task type="auto">
  <name>Task 2: AssetsView rewrite + AttackCoverageView + App.svelte nav</name>
  <files>dashboard/src/views/AssetsView.svelte, dashboard/src/views/AttackCoverageView.svelte, dashboard/src/App.svelte</files>
  <action>
    Read dashboard/src/views/AssetsView.svelte (existing shell to replace), ThreatIntelView.svelte (expand pattern), HuntingView.svelte (OSINT panel pattern), and App.svelte (routing and nav structure) before writing.

    ASSETSVIEW.SVELTE — complete rewrite (replace existing file entirely):

    Script block (Svelte 5 runes only — no stores, no writable()):
    - Import api and Asset type from $lib/api
    - let assets = $state(null) typed Asset[] | null; let expandedIp = $state(null) typed string | null
    - let expandedData = $state(null) typed object with events/detections/osint fields, or null
    - let loading = $state(true); let error = $state(null)
    - $effect loads assets on mount via api.assets.list()
    - toggleExpand(ip): if already expanded collapse; else set expandedIp, fetch api.assets.get(ip) for timeline+detections data, and (if not private IP) fetch api.osint.enrich(ip); use Promise.allSettled to handle partial failures
    - isPrivateIp(ip): regex test for RFC1918 and loopback ranges
    - riskClass(score): 70+ "risk-high", 30+ "risk-medium", else "risk-low"

    Template: h1 "Assets" header, loading/error/empty states, table with thead columns (Hostname/IP, Tag, Risk Score, Last Seen, Alert Count), tbody {#each assets} rows with onclick=toggleExpand. Each row: hostname cell shows asset.hostname ?? asset.ip (bold) with secondary monospace IP if hostname differs; tag chip with class for internal/external; risk score badge with riskClass; formatted last_seen date; alert count bold if >0. After each row: {#if expandedIp === asset.ip} detail panel spanning all columns with Event Timeline, Detections, OSINT subsections.

    Style: dark table matching existing views (#1a1a1a rows, #111 header). Internal chip: green. External chip: orange. Risk high=red, medium=amber, low=green. Detail panel bg #1a1a1a.

    ATTACKCOVERAGEVIEW.SVELTE — new file:

    Script block: import api, TacticCoverage, ActorMatch from $lib/api. State: coverage, actorMatches, expandedTactic, loading, error. $effect loads api.attack.coverage() and api.attack.actorMatches() in parallel. heatColour(n): 0=>#333, 1-2=>#7a4400, 3-9=>#c96a00, 10+=>#e84a00. toggleTactic(tactic): expand/collapse.

    Template: h1 "ATT&CK Coverage" + subtitle text. Actor Matches section at top: {#if actorMatches && actorMatches.length} show up to 3 cards — actor name, confidence badge (High=red, Medium=amber, Low=grey), overlap percentage, matched_count/total_count. Heatmap grid: CSS grid repeat(14, 1fr). Each column: header div with background=heatColour(covered_count), cursor pointer, onclick=toggleTactic. Header shows tactic_short. Below header: "{covered_count} / {total_techniques}" badge. Expanded tactic panel below grid: find matching tactic, show full tactic name h3, list techniques sorted covered-first — each row shows tech_id, name, checkmark or dash. Empty state when no data.

    Style: dark column cards with heat colour header. Pointer cursor. Expanded panel #1a1a1a bg with left accent border.

    APP.SVELTE — routing and sidebar nav:

    Read App.svelte first to understand import style, route mechanism (currentView or router), and Intelligence group nav structure. Then make minimal targeted changes only:
    1. Add import AttackCoverageView from './views/AttackCoverageView.svelte' (keep existing AssetsView import)
    2. Add AttackCoverageView to route binding using key 'attack-coverage' (match existing route pattern)
    3. In Intelligence group sidebar: insert "ATT&CK Coverage" nav entry between Threat Intel and Threat Map — use identical nav item component pattern as neighbours
    4. Confirm AssetsView nav entry is present and wired

    After all changes: cd dashboard && npm run check — zero errors.
  </action>
  <verify>
    <automated>cd /c/Users/Admin/AI-SOC-Brain/dashboard && npm run check 2>&1 | tail -10</automated>
  </verify>
  <done>npm run check passes with zero TypeScript errors. AssetsView.svelte is the new IP-centric table (old coverage shell fully replaced). AttackCoverageView.svelte exists and compiles. App.svelte routes both views and shows ATT&CK Coverage in Intelligence nav group between Threat Intel and Threat Map.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify — AssetsView, ATT&CK heatmap, nav wiring</name>
  <action>Automated checks run before presenting checkpoint to user: uv run pytest tests/unit/ -x -q and cd dashboard && npm run check. Both must pass before requesting human verification.</action>
  <what-built>
    - AssetsView.svelte: full rewrite — IP-centric asset table with hostname, tag chip, risk score, last seen, alert count; inline detail panel with event timeline + detections + OSINT blocks
    - AttackCoverageView.svelte: 14-column ATT&CK heatmap with heat-scale colours, inline tactic drill-down, actor match cards at top
    - api.ts: Asset, TacticCoverage, ActorMatch interfaces + api.assets (list/get/tag) + api.attack (coverage/actorMatches)
    - App.svelte: both views routed + ATT&CK Coverage nav item in Intelligence group between Threat Intel and Threat Map
    - Backend: 5 live endpoints — GET /api/assets, GET /api/assets/{ip}, POST /api/assets/{ip}/tag, GET /api/attack/coverage, GET /api/attack/actor-matches
    - STIX bootstrap runs at startup (skipped if data already seeded)
    - Asset upsert in loader.py — every ingested event upserts src_ip and dst_ip as assets
  </what-built>
  <how-to-verify>
    1. Start backend: uv run uvicorn backend.main:app --reload (or existing start script)
    2. Open https://localhost in browser
    3. Navigate to Assets in sidebar:
       - Table shows columns: Hostname/IP (with tag chip), Risk Score, Last Seen, Alert Count
       - Internal IPs (192.168.x.x, 10.x.x.x) show green "internal" chip
       - External IPs show orange "external" chip
       - Click any asset row: inline detail panel expands with Event Timeline, Detections, OSINT sections
       - For internal IPs: OSINT section shows "Internal asset — no OSINT enrichment"
    4. Navigate to ATT&CK Coverage (should appear between Threat Intel and Threat Map in sidebar):
       - 14 tactic columns visible in a grid layout
       - Column colours: 0 coverage = dark grey #333, some coverage = orange/red gradient
       - Each column shows "N / M" badge (covered rules / total techniques)
       - Click a tactic column: inline technique list expands below the grid
       - Each technique shows checkmark (covered) or dash (not covered)
       - If detections fired in past 30 days: Actor Matches section appears at top with 1-3 actor cards
    5. Automated checks:
       - cd dashboard && npm run check  (must show 0 errors)
       - uv run pytest tests/unit/ -x -q  (must be fully green)
  </how-to-verify>
  <verify>
    <automated>cd /c/Users/Admin/AI-SOC-Brain && uv run pytest tests/unit/ -x -q 2>&1 | tail -5 && cd dashboard && npm run check 2>&1 | tail -5</automated>
  </verify>
  <done>All automated checks pass and user approves the visual verification of both views.</done>
  <resume-signal>Type "approved" when all checks pass, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
1. `cd /c/Users/Admin/AI-SOC-Brain/dashboard && npm run check` — zero TypeScript errors
2. `uv run pytest tests/unit/ -x -q` — full suite green
3. Human visual verification: asset table + detail panel + heatmap + tactic drill-down
</verification>

<success_criteria>
- AssetsView shows real asset data — IP-centric rows with inline detail panel
- AttackCoverageView shows 14-column heat-scaled grid with inline expansion
- TypeScript compiles clean
- All unit tests pass
- Human-verify checkpoint approved by user
</success_criteria>

<output>
After completion, create `.planning/phases/34-asset-inventory/34-04-SUMMARY.md`
</output>
