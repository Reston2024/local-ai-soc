---
phase: 06-hardening-integration
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/src/tests/test_phase6.py
  - backend/causality/__init__.py
  - backend/causality/engine.py
  - backend/causality/entity_resolver.py
  - backend/causality/attack_chain_builder.py
  - backend/causality/mitre_mapper.py
  - backend/causality/scoring.py
  - frontend/src/components/graph/ThreatGraph.svelte
autonomous: true
requirements:
  - FR-6-causality-engine
  - FR-6-entity-resolution
  - FR-6-attack-chain
  - FR-6-mitre-mapper
  - FR-6-scoring
  - FR-6-api-endpoints
  - FR-6-dashboard

must_haves:
  truths:
    - "14 xfail tests exist in test_phase6.py covering all Phase 6 test classes"
    - "backend/causality/ package is importable (stubs return safe empty values)"
    - "ThreatGraph.svelte maps e.src/e.dst correctly (not e.source/e.target)"
    - "All 68 prior tests still pass after this plan"
  artifacts:
    - path: "backend/src/tests/test_phase6.py"
      provides: "14 xfail test stubs for Phase 6"
    - path: "backend/causality/__init__.py"
      provides: "Package marker — empty"
    - path: "backend/causality/engine.py"
      provides: "Stub build_causality_sync returning empty dict"
    - path: "backend/causality/entity_resolver.py"
      provides: "Stub resolve_canonical_id returning None"
    - path: "backend/causality/attack_chain_builder.py"
      provides: "Stub find_causal_chain returning []"
    - path: "backend/causality/mitre_mapper.py"
      provides: "Stub map_techniques returning []"
    - path: "backend/causality/scoring.py"
      provides: "Stub score_chain returning 0"
    - path: "frontend/src/components/graph/ThreatGraph.svelte"
      provides: "Fixed edge mapping: e.src/e.dst -> source/target"
  key_links:
    - from: "backend/src/tests/test_phase6.py"
      to: "backend/causality/*.py"
      via: "deferred per-method imports inside each test"
      pattern: "from backend\\.causality\\."
---

<objective>
Wave 0 TDD red phase: create all causality package stubs and 14 xfail test stubs. Also fix the confirmed ThreatGraph.svelte src/dst mapping bug before new components build on top of it.

Purpose: Establish the TDD red baseline. All 14 tests must be importable (not error) but fail as xfail. Causality package is importable without crashing the backend. The ThreatGraph bug fix prevents the "edges never visible" defect from propagating to AttackChain.svelte in Wave 4.

Output: test_phase6.py (14 xfail stubs), backend/causality/ (7 stub files), ThreatGraph.svelte (line 27 fixed).
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-hardening-integration/06-CONTEXT.md
@.planning/phases/06-hardening-integration/06-RESEARCH.md
@.planning/phases/06-hardening-integration/06-VALIDATION.md

<interfaces>
<!-- Existing models from backend/src/api/models.py -->
```python
class GraphEdge(BaseModel):
    id: str
    type: str
    src: str   # NOT "source"
    dst: str   # NOT "target"
    timestamp: str = ""
    evidence_event_ids: list[str] = Field(default_factory=list)

class Alert(BaseModel):
    id: str
    timestamp: str
    rule: str
    severity: str
    event_id: str
    description: str
    threat_score: int = 0
    attack_tags: list[dict] = Field(default_factory=list)
```

<!-- ThreatGraph.svelte bug: line 27 currently reads -->
```typescript
data: { id: e.id, source: e.source, target: e.target, label: e.type },
// MUST be fixed to:
data: { id: e.id, source: e.src, target: e.dst, label: e.type },
```

<!-- Existing test pattern from test_phase5.py -->
<!-- Per-method deferred imports inside xfail tests, strict=False -->
```python
@pytest.mark.xfail(strict=False, reason="Phase 6 Plan NN not yet implemented")
def test_something(self):
    from backend.causality.module import function
    result = function(...)
    assert result == expected
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create causality package stubs</name>
  <files>backend/causality/__init__.py, backend/causality/engine.py, backend/causality/entity_resolver.py, backend/causality/attack_chain_builder.py, backend/causality/mitre_mapper.py, backend/causality/scoring.py</files>
  <action>
Create the backend/causality/ directory with 6 Python stub files. Each stub must be importable without errors and return safe empty values so the test file can import cleanly.

Exact stub contents:

**backend/causality/__init__.py** — empty (just a docstring):
```python
"""Causality engine package — Phase 6."""
```

**backend/causality/engine.py**:
```python
"""Causality engine orchestrator — Phase 6 stub."""


def build_causality_sync(alert_id: str, events: list, alerts: list) -> dict:
    """Stub: returns empty causality result. Implemented in Plan 03."""
    return {}
```

**backend/causality/entity_resolver.py**:
```python
"""Entity normalization and canonical ID resolution — Phase 6 stub."""


def resolve_canonical_id(event: dict, entity_type: str) -> str | None:
    """Stub: returns None. Implemented in Plan 01."""
    return None
```

**backend/causality/attack_chain_builder.py**:
```python
"""Causal chain construction via shared-entity BFS — Phase 6 stub."""


def find_causal_chain(
    start_event_id: str,
    all_events: list,
    max_depth: int = 5,
    max_events: int = 50,
) -> list:
    """Stub: returns empty list. Implemented in Plan 01."""
    return []
```

**backend/causality/mitre_mapper.py**:
```python
"""MITRE ATT&CK technique/tactic mapping — Phase 6 stub."""


def map_techniques(sigma_tags: list, event_type: str, alert_category: str) -> list:
    """Stub: returns empty list. Implemented in Plan 02."""
    return []
```

**backend/causality/scoring.py**:
```python
"""Attack chain severity scoring — Phase 6 stub."""


def score_chain(chain_events: list, chain_alerts: list, techniques: list) -> int:
    """Stub: returns 0. Implemented in Plan 02."""
    return 0
```
  </action>
  <verify>
    <automated>cd /c/Users/Admin/AI-SOC-Brain && uv run python -c "from backend.causality.engine import build_causality_sync; from backend.causality.entity_resolver import resolve_canonical_id; from backend.causality.attack_chain_builder import find_causal_chain; from backend.causality.mitre_mapper import map_techniques; from backend.causality.scoring import score_chain; print('all stubs importable')"</automated>
  </verify>
  <done>All 6 stub modules import without errors. Each function returns its safe empty value when called.</done>
</task>

<task type="auto">
  <name>Task 2: Write test_phase6.py with 14 xfail stubs</name>
  <files>backend/src/tests/test_phase6.py</files>
  <action>
Create backend/src/tests/test_phase6.py following the exact pattern from test_phase5.py: module-level docstring, minimal top-level imports, all 14 test classes with per-method deferred imports inside xfail methods. Use strict=False on all xfail markers.

The 14 test classes and their behaviors:

1. **TestEntityResolver** — `resolve_canonical_id({"host": "WORKSTATION01"}, "host")` must return `"host:workstation01"` (lowercase)
2. **TestEntityResolverCaseFolding** — same call returns same ID regardless of input case: `resolve_canonical_id({"host": "WORKSTATION01"}, "host") == resolve_canonical_id({"host": "workstation01"}, "host")`
3. **TestAttackChainBuilder** — `find_causal_chain(start_id, events)` returns list sorted by timestamp where first item has `id == start_id`
4. **TestAttackChainDepthCap** — BFS with max_depth=1 on a 3-hop chain returns only events at depth ≤ 1
5. **TestAttackChainCycleDetection** — events referencing each other circularly do not cause infinite loop; function returns within 1 second
6. **TestMitreMapper** — `map_techniques(["attack.t1059.001"], "", "")` returns `[{"technique": "T1059.001", "tactic": "Execution", "name": "PowerShell"}]`
7. **TestMitreMapperGraceful** — `map_techniques(["attack.tunknown999"], "", "")` returns `[]` without raising exception
8. **TestScoring** — `score_chain([], [], [])` returns 0; score with critical alert returns value > 0; score is capped at 100
9. **TestCausalityEngine** — `build_causality_sync("alert1", events, alerts)` returns a dict with keys "nodes", "edges", "chain" when alert exists
10. **TestGraphEndpoint** — `GET /graph/alert1` with seeded alert returns 200 and JSON with "nodes" key
11. **TestEntityEndpoint** — `GET /entity/host:workstation01` returns 200 with entity data dict
12. **TestAttackChainEndpoint** — `GET /attack_chain/alert1` returns 200 with "edges" key
13. **TestQueryEndpoint** — `POST /query` with `{"q": "test", "entity_id": null}` returns 200
14. **TestDashboardBuild** — `subprocess.run(["npm", "run", "build"], cwd="frontend", ...)` exits 0 (this test is NOT xfail — it tests the current build state; mark xfail only if build was previously broken)

Actually for TestDashboardBuild: mark it xfail strict=False with reason "dashboard build test — passes when npm build is green". This keeps the wave 0 run clean regardless of dashboard state.

Use `from fastapi.testclient import TestClient` and `from backend.src.api.main import app` at module level (same as test_phase5.py). All causality imports are deferred inside each test method.

Integration test helper: for TestGraphEndpoint/TestEntityEndpoint/TestAttackChainEndpoint/TestQueryEndpoint, seed the in-memory store first using `client.post("/events", json={...})` before calling the causality endpoint.
  </action>
  <verify>
    <automated>cd /c/Users/Admin/AI-SOC-Brain && uv run pytest backend/src/tests/test_phase6.py -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>All 14 tests run as xfail (XFAIL or XPASS). Zero ERROR outcomes. uv run pytest backend/src/tests/ -v exits 0 with all prior 68 tests still passing.</done>
</task>

<task type="auto">
  <name>Task 3: Fix ThreatGraph.svelte src/dst mapping bug</name>
  <files>frontend/src/components/graph/ThreatGraph.svelte</files>
  <action>
Fix line 27 of ThreatGraph.svelte. The current code uses `e.source` and `e.target` but `GraphEdge` schema (locked in Phase 4) uses `src` and `dst` fields. This means the existing graph view renders nodes but no edges.

Change line 27 from:
```typescript
data: { id: e.id, source: e.source, target: e.target, label: e.type },
```
to:
```typescript
data: { id: e.id, source: e.src, target: e.dst, label: e.type },
```

No other changes to ThreatGraph.svelte. This is a one-line bug fix.

After fixing, run `cd dashboard && npm run build` to confirm TypeScript still compiles. (The build already uses `e.src` and `e.dst` from the GraphEdge type — this aligns the runtime mapping with the type definition.)
  </action>
  <verify>
    <automated>cd /c/Users/Admin/AI-SOC-Brain/frontend && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>ThreatGraph.svelte line 27 reads `source: e.src, target: e.dst`. `npm run build` exits 0. No other changes to the file.</done>
</task>

</tasks>

<verification>
After all three tasks:
1. `uv run pytest backend/src/tests/test_phase6.py -v` — 14 tests, all XFAIL or XPASS, zero ERROR
2. `uv run pytest backend/src/tests/ -v` — all prior tests (68+) still pass
3. `cd frontend && npm run build` — exits 0
4. `uv run python -c "import backend.causality.engine; import backend.causality.entity_resolver; print('ok')"` — prints 'ok'
</verification>

<success_criteria>
- backend/causality/ package exists with 6 stub files, all importable
- test_phase6.py exists with 14 xfail stubs
- No ERROR outcomes in test run (only XFAIL/XPASS)
- Prior 68 tests still pass
- ThreatGraph.svelte line 27 uses e.src/e.dst
- npm run build exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/06-hardening-integration/06-00-SUMMARY.md`
</output>
