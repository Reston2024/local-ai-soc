---
phase: 28-dashboard-integration-fixes
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard/src/App.svelte
autonomous: true
requirements:
  - P28-T03
gap_closure: true

must_haves:
  truths:
    - "App.svelte imports SettingsView from './views/SettingsView.svelte'"
    - "The View type union includes 'settings'"
    - "A nav item with label 'Settings' and gear icon exists in the nav bar"
    - "Navigating to the settings view renders SettingsView.svelte (the {:else if} branch exists)"
    - "svelte-check passes with 0 errors after changes"
  artifacts:
    - path: "dashboard/src/App.svelte"
      provides: "SettingsView import, nav entry, view type, render branch"
      contains: "SettingsView"
  key_links:
    - from: "nav settings item click"
      to: "currentView === 'settings'"
      via: "App.svelte view routing"
      pattern: "currentView === 'settings'"
    - from: "App.svelte"
      to: "dashboard/src/views/SettingsView.svelte"
      via: "import statement"
      pattern: "import SettingsView from"
---

<objective>
Wire `SettingsView.svelte` into the navigation (INT-04 / P28-T03). The Settings view is fully
implemented (operators CRUD, key rotation, TOTP, model-status) but is unreachable because
`App.svelte` has no import, no nav item, no View type entry, and no render branch for it.

Purpose: Give operators access to the Settings page via the nav bar — required for RBAC
operator management and key rotation workflows added in Phase 19 (Identity & RBAC).
Output: Four targeted additions to App.svelte — import, type union, nav item, render branch.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md

Key source files:
- `dashboard/src/App.svelte` — nav groups, View type union, routing template
- `dashboard/src/views/SettingsView.svelte` — fully implemented, no changes needed

<interfaces>
From App.svelte (confirmed by research — read entire file):

Current View type (add 'settings'):
```typescript
type View = 'detections' | 'events' | 'ingest' | 'query' | 'graph' | 'investigations'
           | 'recommendations' | ...
// Add: | 'settings'
```

Current navGroups structure (add settings item to Platform group or end of existing group):
```typescript
const navGroups = [
  {
    label: 'Analyze',
    items: [
      { id: 'detections', label: 'Detections', ... },
      ...
    ]
  },
  {
    label: 'Platform',
    items: [
      { id: 'ingest', label: 'Ingest', ... },
      ...
    ]
  }
]
// Add settings item to the Platform group or append a new Admin group
```

Current render block pattern (existing example):
```svelte
{:else if currentView === 'recommendations'}
  <RecommendationsView />
```

SettingsView.svelte is at `dashboard/src/views/SettingsView.svelte` — fully implemented.
No changes required inside SettingsView.svelte.

Svelte 5 rules (CLAUDE.md): use `$state()` for reactive state, NOT `writable()`.
The nav item click handler already uses `$state` for `currentView` — do not refactor it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire SettingsView into App.svelte nav and routing</name>
  <files>dashboard/src/App.svelte</files>
  <action>
Open `dashboard/src/App.svelte`. Make four additions:

**Addition 1 — Import at top of script block:**
Find the block of `import ... from './views/...'` statements. Add:
```typescript
import SettingsView from './views/SettingsView.svelte'
```

**Addition 2 — Add 'settings' to the View type union:**
Find the `type View = ...` declaration. Add `'settings'` to the union:
```typescript
type View = 'detections' | ... | 'recommendations' | 'settings'
```

**Addition 3 — Add nav item:**
Find the `navGroups` array. Add a settings entry to the Platform group (or the last group
in the array if no Platform group exists):
```typescript
{ id: 'settings', label: 'Settings', color: '#a78bfa' }
```

For the icon, look at how other nav items render icons in the template. If icons are SVG
inline or a component, use the gear icon SVG path (⚙):
```
M12 15.5A3.5 3.5 0 0 1 8.5 12 3.5 3.5 0 0 1 12 8.5a3.5 3.5 0 0 1 3.5 3.5 3.5 3.5 0 0 1-3.5 3.5m7.43-2.92c.04-.33.07-.64.07-.97 0-.33-.03-.66-.07-1l2.11-1.63c.19-.15.24-.42.12-.64l-2-3.46c-.12-.22-.39-.3-.61-.22l-2.49 1c-.52-.4-1.08-.73-1.69-.98l-.38-2.65C14.46 2.18 14.25 2 14 2h-4c-.25 0-.46.18-.49.42l-.38 2.65c-.61.25-1.17.59-1.69.98l-2.49-1c-.23-.09-.49 0-.61.22l-2 3.46c-.13.22-.07.49.12.64L4.57 11c-.04.34-.07.67-.07 1 0 .33.03.65.07.97l-2.11 1.66c-.19.15-.25.42-.12.64l2 3.46c.12.22.39.3.61.22l2.49-1.01c.52.4 1.08.73 1.69.98l.38 2.65c.03.24.24.42.49.42h4c.25 0 .46-.18.49-.42l.38-2.65c.61-.25 1.17-.58 1.69-.98l2.49 1.01c.22.08.49 0 .61-.22l2-3.46c.12-.22.07-.49-.12-.64l-2.11-1.66z
```
If the nav template does NOT use inline SVG paths (e.g. uses emoji or text icons only),
use a simple text label "⚙" as the icon placeholder — match whatever pattern is already
in use for other nav items.

**Addition 4 — Add render branch in main content area:**
Find the block of `{:else if currentView === '...'}` branches. Add settings BEFORE the
final `{:else}` fallback (if one exists), after the last existing view branch:
```svelte
{:else if currentView === 'settings'}
  <SettingsView />
```

Preserve all existing nav items, view types, and render branches exactly as they are.
This is an additive change — do not remove or reorder anything.
  </action>
  <verify>
    <automated>cd dashboard && npx svelte-check --tsconfig ./tsconfig.json 2>&1 | tail -10</automated>
  </verify>
  <done>svelte-check 0 errors. App.svelte contains: "import SettingsView", "settings" in View type, settings nav item, and "{:else if currentView === 'settings'}" render branch. All four additions confirmed by grep.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>SettingsView wired into App.svelte nav — import, View type, nav item, render branch all added.</what-built>
  <how-to-verify>
    1. Start the dashboard dev server: `cd dashboard && npm run dev`
    2. Open browser at http://localhost:5173 (or whatever port is shown)
    3. Look for a "Settings" item (with gear icon) in the navigation bar
    4. Click "Settings"
    5. Confirm the Settings view renders — should show operator management, key rotation, TOTP, and model-status sections
    6. Confirm no console errors in browser DevTools
  </how-to-verify>
  <resume-signal>Type "approved" if Settings nav item appears and view renders, or describe what is missing/broken</resume-signal>
</task>

</tasks>

<verification>
Automated: `cd dashboard && npx svelte-check --tsconfig ./tsconfig.json` — 0 errors.

Grep spot-checks:
- `grep -n "SettingsView" dashboard/src/App.svelte` → 2+ matches (import + render)
- `grep -n "'settings'" dashboard/src/App.svelte` → 2+ matches (type union + nav id)
- `grep -n "currentView === 'settings'" dashboard/src/App.svelte` → 1 match
</verification>

<success_criteria>
- App.svelte imports SettingsView
- 'settings' is in the View type union
- Settings nav item visible in the navigation bar
- {:else if currentView === 'settings'} render branch exists
- svelte-check 0 errors
- Human confirms Settings view renders in browser
</success_criteria>

<output>
After completion, create `.planning/phases/28-dashboard-integration-fixes/28-04-SUMMARY.md`
</output>
