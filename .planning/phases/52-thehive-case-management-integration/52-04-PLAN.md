---
phase: 52-thehive-case-management-integration
plan: "04"
type: execute
wave: 4
depends_on:
  - "52-03"
files_modified:
  - dashboard/src/lib/api.ts
  - dashboard/src/views/DetectionsView.svelte
  - dashboard/src/views/InvestigationView.svelte
autonomous: false
requirements:
  - REQ-52-05
user_setup:
  - service: thehive
    why: "Deep-link URL validation and manual UX verification require a live TheHive instance at http://192.168.1.22:9000"
    env_vars: []
    dashboard_config:
      - task: "Deploy TheHive on GMKtec (docker compose -f infra/docker-compose.thehive.yml up -d)"
        location: "SSH to GMKtec"
must_haves:
  truths:
    - "Detection row shows case badge '#N · New/In Progress/Resolved' when thehive_case_num is set"
    - "Expanded detection panel shows 'Open in TheHive' button deep-linking to http://192.168.1.22:9000/cases/{thehive_case_num}"
    - "InvestigationView header shows case badge + 'Open in TheHive' button when thehive_case_num is present"
    - "Badge color is amber for New/In Progress, green for Resolved/True Positive, red for False Positive"
    - "No TypeScript errors"
  artifacts:
    - path: "dashboard/src/lib/api.ts"
      provides: "thehive_* fields on Detection interface"
      contains: "thehive_case_num"
    - path: "dashboard/src/views/DetectionsView.svelte"
      provides: "Case badge + Open in TheHive button in expanded panel"
      contains: "Open in TheHive"
    - path: "dashboard/src/views/InvestigationView.svelte"
      provides: "TheHive badge + button in investigation header"
      contains: "thehive_case_num"
  key_links:
    - from: "DetectionsView.svelte"
      to: "TheHive case URL"
      via: "window.open deep-link"
      pattern: "192.168.1.22:9000/cases"
    - from: "Detection interface"
      to: "thehive_case_num"
      via: "optional field added to existing Detection"
      pattern: "thehive_case_num"
---

<objective>
Add TheHive case badge and "Open in TheHive" deep-link button to DetectionsView and InvestigationView.

Purpose: Closes the analyst workflow loop — detections with cases show live status and allow one-click navigation to TheHive.
Output: Case badge on detection rows, expanded panel button, InvestigationView header button. TypeScript clean.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/52-thehive-case-management-integration/52-CONTEXT.md
@.planning/phases/52-thehive-case-management-integration/52-03-SUMMARY.md
@dashboard/src/lib/api.ts
@dashboard/src/views/DetectionsView.svelte
@dashboard/src/views/InvestigationView.svelte

<interfaces>
<!-- Key interfaces extracted from existing codebase -->

Existing Detection interface (api.ts lines 94-115):
```typescript
export interface Detection {
  id: string
  detection_id?: string
  rule_id: string
  rule_name: string
  // ... existing fields
  verdict?: string | null          // Phase 44
  detection_source?: string | null // Phase 48/49
  // Phase 52 additions:
  // thehive_case_id?: string | null     -- internal TheHive _id (for API calls, not display)
  // thehive_case_num?: number | null    -- sequential case number for badge (#42) and URL
  // thehive_status?: string | null      -- "New" | "InProgress" | "Resolved"
}
```

InvestigationView.svelte existing header (line ~401):
```svelte
<div class="panel-header">
  <!-- case badge + open-in-thehive button slots here, alongside existing header content -->
  <div class="header-actions">
    ...
  </div>
```

InvestigationView.svelte activeTab pattern:
```svelte
let activeTab = $state<'summary' | 'agent' | 'osint'>('summary')
```

DetectionsView.svelte detection row (line ~509):
```svelte
{#each displayDetections as d (getDetectionId(d))}
  <!-- collapsed row -->
  {#if expandedId === getDetectionId(d)}
    <!-- expanded panel -- "Open in TheHive" button goes here -->
  {/if}
{/each}
```

DetectionsView.svelte verdict badge pattern (existing Phase 44):
```svelte
{#if verdicts.get(getDetectionId(d)) === 'TP'}
  <span class="verdict-badge verdict-tp">TP</span>
{:else if verdicts.get(getDetectionId(d)) === 'FP'}
  <span class="verdict-badge verdict-fp">FP</span>
{/if}
```

CSS patterns already in DetectionsView for chips:
- .badge-hayabusa { background: #b45309; color: #fff; }  (amber)
- .verdict-badge { ... }
- .chip { ... }

THEHIVE_URL: hardcode "http://192.168.1.22:9000" directly in the frontend components (config constant per CONTEXT.md decision).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Detection interface and DetectionsView with case badge and button</name>
  <files>dashboard/src/lib/api.ts, dashboard/src/views/DetectionsView.svelte</files>
  <action>
In dashboard/src/lib/api.ts, add 3 fields to the Detection interface after detection_source:

  thehive_case_id?: string | null    // Phase 52: internal TheHive _id (for API calls)
  thehive_case_num?: number | null   // Phase 52: sequential case number for display (#42) and URL
  thehive_status?: string | null     // Phase 52: "New" | "InProgress" | "Resolved" | "TruePositive" | "FalsePositive"

In dashboard/src/views/DetectionsView.svelte:

1. Add at the top of the script block:

  const THEHIVE_CASE_URL = (caseNum: number) => `http://192.168.1.22:9000/cases/${caseNum}`

  function thehiveBadgeLabel(status: string | null | undefined): string {
    if (!status) return 'New'
    if (status === 'TruePositive') return 'True Positive'
    if (status === 'FalsePositive') return 'False Positive'
    return status
  }
  function thehiveBadgeClass(status: string | null | undefined): string {
    if (status === 'Resolved' || status === 'TruePositive') return 'badge-thehive badge-thehive-resolved'
    if (status === 'FalsePositive') return 'badge-thehive badge-thehive-fp'
    return 'badge-thehive badge-thehive-open'
  }

2. On the collapsed detection row (after the existing verdict badge):

  {#if d.thehive_case_num}
    &lt;span class={thehiveBadgeClass(d.thehive_status)} title="TheHive case"&gt;
      #{d.thehive_case_num} · {thehiveBadgeLabel(d.thehive_status)}
    &lt;/span&gt;
  {/if}

3. In the expanded detection panel (after the verdict TP/FP buttons):

  {#if d.thehive_case_num}
    &lt;div class="thehive-case-row"&gt;
      &lt;span class={thehiveBadgeClass(d.thehive_status)}&gt;
        Case #{d.thehive_case_num} · {thehiveBadgeLabel(d.thehive_status)}
      &lt;/span&gt;
      &lt;button
        class="btn-thehive"
        onclick={() => window.open(THEHIVE_CASE_URL(d.thehive_case_num!), '_blank')}
      &gt;
        Open in TheHive
      &lt;/button&gt;
    &lt;/div&gt;
  {/if}

4. Add CSS at bottom of the style block:

  .badge-thehive {
    display: inline-flex; align-items: center; gap: 4px;
    padding: 2px 8px; border-radius: 4px;
    font-size: 11px; font-weight: 600; white-space: nowrap;
  }
  .badge-thehive-open     { background: #92400e; color: #fef3c7; }
  .badge-thehive-resolved { background: #065f46; color: #d1fae5; }
  .badge-thehive-fp       { background: #7f1d1d; color: #fee2e2; }
  .thehive-case-row { display: flex; align-items: center; gap: 8px; margin-top: 8px; }
  .btn-thehive {
    padding: 4px 12px; border-radius: 4px;
    background: #1a1a2e; border: 1px solid #374151; color: #e5e7eb;
    cursor: pointer; font-size: 12px;
  }
  .btn-thehive:hover { background: #2d2d44; }
  </action>
  <verify>
    <automated>cd C:/Users/Admin/AI-SOC-Brain/dashboard && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <done>TypeScript 0 errors. Detection interface has 3 new thehive_* fields. DetectionsView shows case badge on rows where thehive_case_num is set and "Open in TheHive" button in expanded panel.</done>
</task>

<task type="auto">
  <name>Task 2: Add TheHive case badge and button to InvestigationView header</name>
  <files>dashboard/src/views/InvestigationView.svelte</files>
  <action>
In dashboard/src/views/InvestigationView.svelte:

1. Add near the top of the script block:

  const THEHIVE_CASE_URL = (caseNum: number) => `http://192.168.1.22:9000/cases/${caseNum}`

  function thehiveBadgeLabel(status: string | null | undefined): string {
    if (!status) return 'New'
    if (status === 'TruePositive') return 'True Positive'
    if (status === 'FalsePositive') return 'False Positive'
    return status
  }
  function thehiveBadgeClass(status: string | null | undefined): string {
    if (status === 'Resolved' || status === 'TruePositive') return 'badge-thehive badge-thehive-resolved'
    if (status === 'FalsePositive') return 'badge-thehive badge-thehive-fp'
    return 'badge-thehive badge-thehive-open'
  }

2. Inspect the component to find the reactive variable holding the loaded detection/investigation data. Look for a variable like `investigationResult`, `detectionData`, or the result from `api.investigate(investigationId)`. Use the actual variable name — do NOT use a placeholder.

3. In the panel-header section (where Summary/Agent/OSINT tab buttons are), add BEFORE the tab buttons:

  {#if [ACTUAL_VAR]?.thehive_case_num}
    &lt;div class="thehive-header-badge"&gt;
      &lt;span class="badge-thehive {thehiveBadgeClass([ACTUAL_VAR].thehive_status)}"&gt;
        Case #{[ACTUAL_VAR].thehive_case_num} · {thehiveBadgeLabel([ACTUAL_VAR].thehive_status)}
      &lt;/span&gt;
      &lt;button
        class="btn-thehive"
        onclick={() => window.open(THEHIVE_CASE_URL([ACTUAL_VAR]!.thehive_case_num!), '_blank')}
      &gt;
        Open in TheHive
      &lt;/button&gt;
    &lt;/div&gt;
  {/if}

Replace [ACTUAL_VAR] with the real variable name found in the component. Per STATE.md Phase 39/44 decisions: duplicate CSS classes across components (Svelte scoping makes sharing idiomatic, no shared stylesheet exists).

4. Add CSS to style block (same rules as DetectionsView):

  .badge-thehive { display: inline-flex; align-items: center; gap: 4px; padding: 2px 8px; border-radius: 4px; font-size: 11px; font-weight: 600; white-space: nowrap; }
  .badge-thehive-open     { background: #92400e; color: #fef3c7; }
  .badge-thehive-resolved { background: #065f46; color: #d1fae5; }
  .badge-thehive-fp       { background: #7f1d1d; color: #fee2e2; }
  .thehive-header-badge { display: flex; align-items: center; gap: 8px; margin-right: 12px; }
  .btn-thehive { padding: 4px 12px; border-radius: 4px; background: #1a1a2e; border: 1px solid #374151; color: #e5e7eb; cursor: pointer; font-size: 12px; }
  .btn-thehive:hover { background: #2d2d44; }
  </action>
  <verify>
    <automated>cd C:/Users/Admin/AI-SOC-Brain/dashboard && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <done>TypeScript 0 errors. InvestigationView header shows TheHive badge + Open button when detection has thehive_case_num. Full build clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify TheHive frontend badges and deep-links</name>
  <action>Human verification of TheHive badge rendering and deep-link behavior in the dashboard. Follow the how-to-verify steps.</action>
  <verify>
    <automated>cd C:/Users/Admin/AI-SOC-Brain/dashboard && npx tsc --noEmit 2>&1 | tail -5 && echo "TypeScript clean"</automated>
  </verify>
  <done>Dashboard builds with 0 TypeScript errors. No visual regressions. Badge shows "#N · status" with correct color. "Open in TheHive" button visible in expanded panel and investigation header. User types "approved".</done>
  <what-built>
    Phase 52 frontend complete:
    - Detection rows show "#N · New/In Progress/Resolved" amber/green/red badge when thehive_case_num is set
    - Expanded detection panel has "Open in TheHive" button deep-linking to http://192.168.1.22:9000/cases/{N}
    - InvestigationView header shows same badge + button
    Note: Live badge testing requires TheHive deployed on GMKtec (docker-compose.thehive.yml provided)
  </what-built>
  <how-to-verify>
    1. Build dashboard: cd dashboard and npm run build -- must succeed with 0 TypeScript errors
    2. Start backend: uv run uvicorn backend.main:app --reload
    3. Open http://localhost:5173 (or Caddy HTTPS)
    4. Navigate to Detections view -- no visual regressions on existing rows
    5. Test badge with browser devtools: temporarily set a detection's thehive_case_num=42 and thehive_status="InProgress" in Svelte state -- verify amber "#42 · InProgress" badge appears on the row
    6. In expanded panel: verify "Open in TheHive" button appears only when thehive_case_num is set
    7. Navigate to Investigation view -- verify header area has no visual regressions
    8. Run full unit suite: uv run pytest tests/unit/ -x --tb=short -q -- must still pass
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
cd dashboard and npx tsc --noEmit -- 0 errors.
uv run pytest tests/unit/ -x --tb=short -q -- all tests passing, 0 failures.
</verification>

<success_criteria>
- Detection interface: thehive_case_id, thehive_case_num, thehive_status fields added
- DetectionsView: case badge on collapsed rows, badge + "Open in TheHive" button in expanded panel
- InvestigationView: case badge + "Open in TheHive" button in panel header
- TypeScript 0 errors, zero regressions in unit suite
- Checkpoint approved by user
</success_criteria>

<output>
After completion, create `.planning/phases/52-thehive-case-management-integration/52-04-SUMMARY.md`
</output>
