---
phase: 52-thehive-case-management-integration
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/unit/test_thehive_client.py
  - tests/unit/test_thehive_sync.py
autonomous: true
requirements:
  - REQ-52-01
  - REQ-52-02
  - REQ-52-03
  - REQ-52-04
  - REQ-52-06
must_haves:
  truths:
    - "8 TDD stubs exist covering client, payload, observables, retry queue, closure sync"
    - "All stubs SKIP cleanly (importorskip on thehive4py) — zero new failures"
    - "thehive4py==2.0.3 installed and importable"
  artifacts:
    - path: "tests/unit/test_thehive_client.py"
      provides: "Wave 0 stubs for REQ-52-01/02/03/04"
      min_lines: 60
    - path: "tests/unit/test_thehive_sync.py"
      provides: "Wave 0 stubs for REQ-52-04/06"
      min_lines: 50
  key_links:
    - from: "tests/unit/test_thehive_client.py"
      to: "backend/services/thehive_client.py"
      via: "pytest.importorskip"
      pattern: "importorskip.*thehive"
---

<objective>
Install thehive4py and create Wave 0 TDD stubs for all TheHive unit tests.

Purpose: Establish RED test contracts before any implementation code is written. Ensures all Phase 52 behavior is test-driven from the start.
Output: 2 test files with 8 stubs total, all SKIP cleanly until Plan 02/03 implement the source modules.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-thehive-case-management-integration/52-CONTEXT.md
@.planning/phases/52-thehive-case-management-integration/52-RESEARCH.md
@.planning/phases/52-thehive-case-management-integration/52-VALIDATION.md

<interfaces>
<!-- Established importorskip pattern from existing test files -->

From tests/unit/test_misp_sync.py (module-level importorskip):
```python
misp_sync = pytest.importorskip("backend.services.intel.misp_sync")
```

From tests/unit/test_spiderfoot_client.py (try/except guard + pytestmark):
```python
_SF_AVAILABLE = False
try:
    from backend.services.spiderfoot_client import SpiderFootClient
    _SF_AVAILABLE = True
except ImportError:
    pass
pytestmark = pytest.mark.skipif(not _SF_AVAILABLE, reason="SpiderFootClient not available")
```

Expected source modules (do NOT exist yet — stubs should skip when absent):
  backend/services/thehive_client.py  — TheHiveClient, build_case_payload, build_observables
  backend/services/thehive_sync.py    — sync_thehive_closures, drain_pending_cases
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install thehive4py and create test_thehive_client.py stubs</name>
  <files>tests/unit/test_thehive_client.py</files>
  <action>
Install thehive4py: `uv add thehive4py==2.0.3`

Create tests/unit/test_thehive_client.py with module-level import guard (try/except pattern like test_spiderfoot_client.py). Import guard targets backend.services.thehive_client (does not exist yet → all tests skip).

Include these 5 stubs:

1. test_case_payload_severity — build_case_payload() maps severity "high" to 3, "critical" to 4 (TheHive numeric scale). Assert payload["severity"] == 3 for high, 4 for critical.

2. test_suppress_rules_skip — _maybe_create_thehive_case() returns early (no HTTP call) when rule_id is in a suppress list. Use a mock TheHiveClient and assert create_case never called.

3. test_observable_builder — build_observables() returns list containing {"dataType": "ip", "data": "1.2.3.4"} when detection has src_ip="1.2.3.4". Assert other observable has dataType "other".

4. test_enqueue_on_failure — When TheHiveClient.create_case raises Exception, _maybe_create_thehive_case() writes one row to thehive_pending_cases table (assert sqlite row count == 1). Use in-memory sqlite3 connection.

5. test_ping_returns_false_when_unreachable — TheHiveClient.ping() returns False when thehive4py raises a connection exception. Patch the underlying TheHiveApi.case attribute.

Use pytest.mark.asyncio on async tests. Use `pytest.mark.skipif(not _TH_AVAILABLE, reason="...")` per test (not module-level pytestmark) so that individual tests fail RED independently when the source exists but is broken — matching Phase 44/45/48 pattern where the guard is at test level not module level.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_thehive_client.py -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>5 stubs all SKIP cleanly. thehive4py importable via `uv run python -c "import thehive4py"`. Zero new test failures in full suite.</done>
</task>

<task type="auto">
  <name>Task 2: Create test_thehive_sync.py stubs</name>
  <files>tests/unit/test_thehive_sync.py</files>
  <action>
Create tests/unit/test_thehive_sync.py with module-level import guard targeting backend.services.thehive_sync (does not exist yet).

Include these 3 stubs:

1. test_retry_queue_drains — drain_pending_cases() reads rows from thehive_pending_cases, calls create_case for each, then deletes the row on success. Use an in-memory sqlite3 with 2 pending rows; mock TheHiveClient.create_case to succeed. Assert table is empty after drain.

2. test_closure_sync_writes_sqlite — sync_thehive_closures() updates the detections table: given a mock TheHiveClient.find_resolved_cases() returning one resolved case dict (with _id, number, resolutionStatus="TruePositive", endDate=1713220800000, assignee="analyst1"), assert the detections row has thehive_status="TruePositive" and thehive_analyst="analyst1". Use in-memory sqlite3 pre-seeded with a detections row that has thehive_case_id matching the mock case's _id.

3. test_closure_sync_tolerates_failure — sync_thehive_closures() does NOT raise even when the TheHive client raises an exception. Assert function returns None (not raises).

Per-test skipif guard (same _TH_SYNC_AVAILABLE pattern). pytest.mark.asyncio on async tests.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_thehive_client.py tests/unit/test_thehive_sync.py -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>8 stubs total across both files, all SKIP cleanly. Full test suite still passing: `uv run pytest tests/unit/ -x --tb=short -q` shows 1177+ passing, 0 failures.</done>
</task>

</tasks>

<verification>
`uv run pytest tests/unit/ -x --tb=short -q` — all existing 1177 tests pass, 8 new stubs SKIP cleanly.
`uv run python -c "import thehive4py; print(thehive4py.__version__)"` — prints 2.0.3.
</verification>

<success_criteria>
- thehive4py==2.0.3 in pyproject.toml dependencies
- tests/unit/test_thehive_client.py: 5 stubs, all SKIP
- tests/unit/test_thehive_sync.py: 3 stubs, all SKIP
- Zero new test failures
</success_criteria>

<output>
After completion, create `.planning/phases/52-thehive-case-management-integration/52-01-SUMMARY.md`
</output>
