---
phase: 25-receipt-ingestion-and-case-state-propagation
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/unit/test_receipt_transitions.py
  - tests/unit/test_receipt_api.py
  - tests/unit/test_notifications_api.py
autonomous: true
requirements:
  - P25-T01
  - P25-T02
  - P25-T03
  - P25-T04
  - P25-T05

must_haves:
  truths:
    - "Three test files exist and are importable by pytest"
    - "All tests in the three files are marked @pytest.mark.skip so the full suite stays green"
    - "Each skip stub documents the requirement ID and behavior it will verify"
  artifacts:
    - path: "tests/unit/test_receipt_transitions.py"
      provides: "Skipped stubs for 5 taxonomy transitions + schema file validation"
      contains: "pytest.mark.skip"
    - path: "tests/unit/test_receipt_api.py"
      provides: "Skipped stubs for POST /api/receipts, idempotency, state propagation"
      contains: "pytest.mark.skip"
    - path: "tests/unit/test_notifications_api.py"
      provides: "Skipped stubs for notification triggers and GET /api/notifications"
      contains: "pytest.mark.skip"
  key_links:
    - from: "tests/unit/test_receipt_transitions.py"
      to: "backend/models/receipt.py"
      via: "import at test activation (Wave 3)"
      pattern: "from backend.models.receipt import"
    - from: "tests/unit/test_receipt_api.py"
      to: "backend/api/receipts.py"
      via: "import at test activation (Wave 3)"
      pattern: "from backend.api.receipts import"
---

<objective>
Create the three Wave-0 test stub files. All tests are pre-skipped so the full suite remains green while implementation plans run in Waves 1 and 2. Wave 3 (plan 05) will replace the skips with real implementations.

Purpose: Establish the Nyquist test scaffold before any implementation exists. Prevents the "tests added after the fact" anti-pattern.
Output: Three importable test files with `@pytest.mark.skip` on every test function.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-receipt-ingestion-and-case-state-propagation/25-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test_receipt_transitions.py — pre-skipped stubs</name>
  <files>tests/unit/test_receipt_transitions.py</files>
  <action>
Create `tests/unit/test_receipt_transitions.py` with all tests decorated `@pytest.mark.skip(reason="Wave 0 stub — activated in plan 25-05")`.

Include these stub test functions (all skipped):

```python
import pytest

pytestmark = pytest.mark.skip(reason="Wave 0 stub — activated in plan 25-05")


def test_schema_file_valid():
    """P25-T04: contracts/execution-receipt.schema.json is valid JSON Schema; version is '1.0.0-stub'."""


def test_applied_transition():
    """P25-T02: failure_taxonomy='applied' maps to case_status='containment_confirmed'."""


def test_noop_transition():
    """P25-T02: failure_taxonomy='noop_already_present' maps to case_status='containment_confirmed'."""


def test_validation_failed_transition():
    """P25-T02: failure_taxonomy='validation_failed' maps to case_status='containment_failed'."""


def test_expired_rejected_transition():
    """P25-T02: failure_taxonomy='expired_rejected' maps to case_status='containment_failed'."""


def test_rolled_back_transition():
    """P25-T02: failure_taxonomy='rolled_back' maps to case_status='containment_rolled_back'."""
```

Use `pytestmark = pytest.mark.skip(...)` at module level to skip all tests in the file with a single decorator.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_receipt_transitions.py -v 2>&1 | grep -E "SKIP|passed|error"</automated>
  </verify>
  <done>File exists; pytest collects 6 tests and reports all as SKIPPED; full suite still passes.</done>
</task>

<task type="auto">
  <name>Task 2: Create test_receipt_api.py and test_notifications_api.py — pre-skipped stubs</name>
  <files>tests/unit/test_receipt_api.py, tests/unit/test_notifications_api.py</files>
  <action>
Create `tests/unit/test_receipt_api.py` with all tests decorated at module level:

```python
import pytest

pytestmark = pytest.mark.skip(reason="Wave 0 stub — activated in plan 25-05")


def test_post_receipt_valid_returns_202():
    """P25-T01: POST /api/receipts with valid body returns 202 Accepted."""


def test_post_receipt_invalid_body_returns_422():
    """P25-T01: POST /api/receipts with invalid body returns 422."""


def test_post_receipt_stores_in_duckdb():
    """P25-T01: execute_write called with INSERT INTO execution_receipts."""


def test_case_state_propagated():
    """P25-T02: update_investigation_case called with correct status after receipt ingest."""


def test_duplicate_receipt_returns_409():
    """P25-T05: Same receipt_id posted twice returns 409 Conflict."""
```

Create `tests/unit/test_notifications_api.py` with all tests decorated at module level:

```python
import pytest

pytestmark = pytest.mark.skip(reason="Wave 0 stub — activated in plan 25-05")


def test_validation_failed_emits_notification():
    """P25-T03: failure_taxonomy='validation_failed' emits notification with required_action='manual_review_required'."""


def test_rolled_back_emits_notification():
    """P25-T03: failure_taxonomy='rolled_back' emits notification with required_action='manual_review_required'."""


def test_expired_rejected_emits_notification():
    """P25-T03: failure_taxonomy='expired_rejected' emits notification with required_action='re_approve_required'."""


def test_applied_no_notification():
    """P25-T03: failure_taxonomy='applied' does NOT emit a notification."""


def test_noop_no_notification():
    """P25-T03: failure_taxonomy='noop_already_present' does NOT emit a notification."""


def test_get_notifications_returns_pending():
    """P25-T03: GET /api/notifications returns list of pending notifications."""
```
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_receipt_api.py tests/unit/test_notifications_api.py -v 2>&1 | grep -E "SKIP|passed|error" && uv run pytest 2>&1 | tail -3</automated>
  </verify>
  <done>Both files exist; pytest collects 5 + 6 = 11 tests, all SKIPPED; full suite green with no new failures.</done>
</task>

</tasks>

<verification>
uv run pytest tests/unit/test_receipt_transitions.py tests/unit/test_receipt_api.py tests/unit/test_notifications_api.py -v
Expected: 17 tests collected, all SKIPPED, 0 errors.

uv run pytest
Expected: Full suite passes with the same count as before plus 17 new SKIPs.
</verification>

<success_criteria>
- tests/unit/test_receipt_transitions.py exists with 6 skipped stubs
- tests/unit/test_receipt_api.py exists with 5 skipped stubs
- tests/unit/test_notifications_api.py exists with 6 skipped stubs
- `uv run pytest` full suite green (0 failures, 17 new skips)
</success_criteria>

<output>
After completion, create `.planning/phases/25-receipt-ingestion-and-case-state-propagation/25-00-SUMMARY.md`
</output>
