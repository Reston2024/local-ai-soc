---
phase: 20-schema-standardisation-ecs-ocsf
plan: "02"
type: tdd
wave: 1
depends_on: ["20-01"]
files_modified:
  - ingestion/field_mapper.py
  - ingestion/loader.py
  - ingestion/parsers/evtx_parser.py
  - ingestion/parsers/json_parser.py
  - ingestion/parsers/csv_parser.py
  - ingestion/parsers/osquery_parser.py
autonomous: true
requirements: [P20-T02]

must_haves:
  truths:
    - "ingestion/field_mapper.py exists and exports FieldMapper class"
    - "FieldMapper.map() is a pure function: (raw_dict) -> dict with NormalizedEvent field names"
    - "ECS dotted keys (process.name, user.domain, network.protocol) translate to DuckDB snake_case column names"
    - "Unknown keys are passed through unchanged (not silently dropped)"
    - "loader.py _INSERT_SQL includes the 6 new ECS columns so new field values are persisted"
    - "Each parser (EVTX, JSON, CSV, osquery) calls FieldMapper.map() on the raw dict before building NormalizedEvent"
    - "All existing 600+ tests still pass"
  artifacts:
    - path: "ingestion/field_mapper.py"
      provides: "FieldMapper class with _FIELD_VARIANTS mapping"
      exports: ["FieldMapper"]
    - path: "ingestion/loader.py"
      provides: "Updated _INSERT_SQL with 6 new columns"
      contains: "ocsf_class_uid"
    - path: "ingestion/parsers/evtx_parser.py"
      provides: "EVTX parser calls FieldMapper.map() on raw event dict"
      contains: "FieldMapper"
    - path: "ingestion/parsers/json_parser.py"
      provides: "JSON parser calls FieldMapper.map() on raw event dict"
      contains: "FieldMapper"
    - path: "ingestion/parsers/csv_parser.py"
      provides: "CSV parser calls FieldMapper.map() on raw event dict"
      contains: "FieldMapper"
    - path: "ingestion/parsers/osquery_parser.py"
      provides: "osquery parser calls FieldMapper.map() on raw event dict"
      contains: "FieldMapper"
  key_links:
    - from: "ingestion/field_mapper.py"
      to: "backend/models/event.py"
      via: "mapped keys match NormalizedEvent field names"
      pattern: "process_executable|user_domain|network_protocol"
    - from: "ingestion/loader.py"
      to: "backend/models/event.py"
      via: "_INSERT_SQL column list matches to_duckdb_row() tuple length (35)"
      pattern: "ocsf_class_uid.*event_outcome"
    - from: "ingestion/parsers/evtx_parser.py"
      to: "ingestion/field_mapper.py"
      via: "FieldMapper().map(raw_dict) called in parse() before NormalizedEvent construction"
      pattern: "FieldMapper"
    - from: "ingestion/parsers/json_parser.py"
      to: "ingestion/field_mapper.py"
      via: "FieldMapper().map(raw_dict) called in parse() before NormalizedEvent construction"
      pattern: "FieldMapper"
    - from: "ingestion/parsers/csv_parser.py"
      to: "ingestion/field_mapper.py"
      via: "FieldMapper().map(raw_dict) called in parse() before NormalizedEvent construction"
      pattern: "FieldMapper"
    - from: "ingestion/parsers/osquery_parser.py"
      to: "ingestion/field_mapper.py"
      via: "FieldMapper().map(raw_dict) called in parse() before NormalizedEvent construction"
      pattern: "FieldMapper"
---

<objective>
Create ingestion/field_mapper.py (FieldMapper utility), update loader.py to persist the 6 new ECS columns, and wire FieldMapper into all four parsers.

Purpose: Centralise all vendor-field-to-NormalizedEvent translation in one place. After this plan, every parser calls FieldMapper.map() instead of maintaining its own field-name frozensets. loader.py _INSERT_SQL must be extended to write the new columns so plan 20-03's migration is immediately exercised on ingest.
Output: ingestion/field_mapper.py (new), ingestion/loader.py (updated _INSERT_SQL), all four parsers updated to call FieldMapper.map().
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-schema-standardisation-ecs-ocsf/20-RESEARCH.md
@.planning/phases/20-schema-standardisation-ecs-ocsf/20-01-SUMMARY.md
@ingestion/loader.py
@ingestion/parsers/evtx_parser.py
@ingestion/parsers/json_parser.py
@ingestion/parsers/csv_parser.py
@ingestion/parsers/osquery_parser.py
@backend/models/event.py

<interfaces>
<!-- NormalizedEvent now has 35-element to_duckdb_row() tuple (from plan 20-01). -->
<!-- New columns at positions 29-34: -->
<!--   ocsf_class_uid (int|None), event_outcome (str|None), user_domain (str|None) -->
<!--   process_executable (str|None), network_protocol (str|None), network_direction (str|None) -->

<!-- Current _INSERT_SQL in loader.py uses 29 columns and 29 ? placeholders. -->
<!-- It must be extended to 35 columns and 35 placeholders. -->
<!-- DuckDB uses INSERT OR IGNORE — keep that clause. -->

<!-- _FIELD_VARIANTS mapping to implement (from RESEARCH.md Pattern 2): -->
<!-- ECS dotted → snake_case:
  "process.name" → "process_name"
  "process.pid" → "process_id"
  "process.command_line" → "command_line"
  "process.executable" → "process_executable"
  "process.parent.name" → "parent_process_name"
  "process.parent.pid" → "parent_process_id"
  "user.name" → "username"
  "user.domain" → "user_domain"
  "host.hostname" → "hostname"
  "host.name" → "hostname"
  "source.ip" → "src_ip"
  "source.port" → "src_port"
  "destination.ip" → "dst_ip"
  "destination.port" → "dst_port"
  "destination.domain" → "domain"
  "file.path" → "file_path"
  "file.hash.sha256" → "file_hash_sha256"
  "network.protocol" → "network_protocol"
  "network.direction" → "network_direction"
  "event.action" → "event_type"
  "event.outcome" → "event_outcome"
  "event.original" → "raw_event"
  "threat.technique.id" → "attack_technique"
  "threat.tactic.name" → "attack_tactic"
  "url.original" → "url"
  "dns.question.name" → "domain"
-->
</interfaces>
</context>

<feature>
  <name>FieldMapper utility + loader.py _INSERT_SQL extension + parser wiring</name>
  <files>ingestion/field_mapper.py, ingestion/loader.py, tests/unit/test_field_mapper.py, ingestion/parsers/evtx_parser.py, ingestion/parsers/json_parser.py, ingestion/parsers/csv_parser.py, ingestion/parsers/osquery_parser.py</files>
  <behavior>
    RED phase (stubs already exist from plan 20-00):
    - test_field_mapper_importable: from ingestion.field_mapper import FieldMapper succeeds
    - test_map_evtx_fields: FieldMapper().map({"Image": "cmd.exe", "CommandLine": "cmd /c", "ProcessId": 1234}) returns {"Image": "cmd.exe", "CommandLine": "cmd /c", "ProcessId": 1234} — NOTE: EVTX-style PascalCase keys are NOT in _FIELD_VARIANTS (those are handled by parsers); unknown keys pass through
    - test_map_ecs_dotted_fields: FieldMapper().map({"process.name": "cmd.exe", "user.name": "SYSTEM", "host.hostname": "WS01"}) returns {"process_name": "cmd.exe", "username": "SYSTEM", "hostname": "WS01"}
    - test_map_network_fields: FieldMapper().map({"source.ip": "10.0.0.1", "destination.ip": "8.8.8.8", "network.protocol": "dns"}) returns {"src_ip": "10.0.0.1", "dst_ip": "8.8.8.8", "network_protocol": "dns"}
    - test_map_unknown_fields_passthrough: FieldMapper().map({"custom_field": "value", "another": 42}) returns {"custom_field": "value", "another": 42} unchanged
    - test_map_new_ecs_fields: FieldMapper().map({"process.executable": "/bin/sh", "user.domain": "CORP", "event.outcome": "success"}) returns {"process_executable": "/bin/sh", "user_domain": "CORP", "event_outcome": "success"}
  </behavior>
  <implementation>
    Create ingestion/field_mapper.py as a pure module with no I/O, no imports from backend.

    Update loader.py _INSERT_SQL:
    - Add 6 new columns to the INSERT column list: ocsf_class_uid, event_outcome, user_domain, process_executable, network_protocol, network_direction
    - Add 6 new ? placeholders to VALUES (...)
    - The VALUES clause must have exactly 35 ? placeholders matching to_duckdb_row() tuple length
    - Update the inline comment on _INSERT_SQL if present
    - Do NOT change _EXISTS_SQL or any other loader logic

    Wire FieldMapper into each parser:
    - In each parser's parse() method, apply FieldMapper().map(raw_dict) to the raw event dict
      BEFORE building the NormalizedEvent. The mapped dict is then used as the source for
      NormalizedEvent field population.
    - The EVTX parser already translates PascalCase keys internally; FieldMapper runs on top
      to catch any ECS dotted-path keys that may appear in WindowsEventData.
    - Parsers must NOT be refactored beyond adding the FieldMapper call — all existing
      field-extraction logic stays intact.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ingestion/field_mapper.py</name>
  <files>ingestion/field_mapper.py</files>
  <behavior>
    - FieldMapper class with map(self, raw: dict[str, Any]) -> dict[str, Any] method
    - _FIELD_VARIANTS dict with 26 ECS dotted-path → snake_case entries
    - Unknown keys pass through unchanged
    - Case-insensitive fallback: if exact key not found, try key.lower()
    - Pure function (no I/O, no logging, no imports from backend)
  </behavior>
  <action>
    RED: uv run pytest tests/unit/test_field_mapper.py -v — all 6 tests FAIL.

    GREEN: Create ingestion/field_mapper.py with the full _FIELD_VARIANTS dict from the context interfaces block above. Class structure from RESEARCH.md Pattern 2.

    Module structure:
    ```
    from __future__ import annotations
    from typing import Any

    _FIELD_VARIANTS: dict[str, str] = { ... 26 entries ... }

    class FieldMapper:
        def map(self, raw: dict[str, Any]) -> dict[str, Any]:
            result: dict[str, Any] = {}
            for k, v in raw.items():
                canonical = _FIELD_VARIANTS.get(k) or _FIELD_VARIANTS.get(k.lower())
                result[canonical if canonical else k] = v
            return result

    __all__ = ["FieldMapper"]
    ```
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_field_mapper.py -v</automated>
  </verify>
  <done>
    - 6/6 test_field_mapper.py tests pass (GREEN)
    - ingestion/field_mapper.py exists and is importable
    - FieldMapper.map() is a pure function with no side effects
  </done>
</task>

<task type="auto">
  <name>Task 2: Update loader.py _INSERT_SQL for 6 new ECS columns</name>
  <files>ingestion/loader.py</files>
  <action>
    Modify _INSERT_SQL in ingestion/loader.py to include the 6 new ECS columns.

    The new _INSERT_SQL must:
    1. Add these 6 column names to the INSERT column list after case_id:
       ocsf_class_uid, event_outcome, user_domain, process_executable, network_protocol, network_direction
    2. Add 6 corresponding ? placeholders to the VALUES (...) section
    3. The final VALUES clause must have exactly 35 ? placeholders total

    Updated _INSERT_SQL structure:
    ```sql
    INSERT OR IGNORE INTO normalized_events (
        event_id, timestamp, ingested_at, source_type, source_file,
        hostname, username, process_name, process_id,
        parent_process_name, parent_process_id,
        file_path, file_hash_sha256, command_line,
        src_ip, src_port, dst_ip, dst_port, domain, url,
        event_type, severity, confidence, detection_source,
        attack_technique, attack_tactic,
        raw_event, tags, case_id,
        ocsf_class_uid, event_outcome, user_domain,
        process_executable, network_protocol, network_direction
    ) VALUES (
        ?, ?, ?, ?, ?,
        ?, ?, ?, ?,
        ?, ?,
        ?, ?, ?,
        ?, ?, ?, ?, ?, ?,
        ?, ?, ?, ?,
        ?, ?,
        ?, ?, ?,
        ?, ?, ?,
        ?, ?, ?
    )
    ```

    Do NOT modify any other part of loader.py. The executemany call uses event.to_duckdb_row() which now returns 35 elements — no other change needed.
  </action>
  <verify>
    <automated>uv run pytest tests/ -q --tb=short -x</automated>
  </verify>
  <done>
    - loader.py _INSERT_SQL has 35 column names and 35 ? placeholders
    - Full test suite passes with no regressions
    - uv run python -c "from ingestion.loader import _INSERT_SQL; assert _INSERT_SQL.count('?') == 35" exits 0
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire FieldMapper into all four parsers</name>
  <files>ingestion/parsers/evtx_parser.py, ingestion/parsers/json_parser.py, ingestion/parsers/csv_parser.py, ingestion/parsers/osquery_parser.py</files>
  <action>
    Read each parser file first to understand its parse() method structure before editing.

    For EACH of the four parser files, apply the following minimal change:

    1. Add the import near the top of the file (after existing imports):
       ```python
       from ingestion.field_mapper import FieldMapper
       _field_mapper = FieldMapper()
       ```

    2. Locate the parse() method (or equivalent generator function that yields NormalizedEvent).
       Find the point where a raw dict (from the source format) has been assembled but
       NormalizedEvent construction has not yet occurred.

    3. Insert a single call to apply FieldMapper:
       ```python
       raw = _field_mapper.map(raw)
       ```
       where `raw` is the variable name holding the raw event dict at that point.
       The variable name may differ per parser (e.g. `record`, `row`, `event`, `data`) —
       read each file to identify the correct variable.

    4. Do NOT change any other logic. Do NOT restructure the parser. Do NOT remove existing
       field-extraction frozensets or key-variant handling — FieldMapper augments rather than
       replaces the existing logic. The existing handling remains as a safety net; FieldMapper
       runs first to translate ECS dotted-path keys that the parser would otherwise not recognise.

    Keep changes minimal: one import block addition and one map() call per parser.
  </action>
  <verify>
    <automated>uv run pytest tests/ -q --tb=short -x</automated>
  </verify>
  <done>
    - All four parser files import FieldMapper and call _field_mapper.map() in their parse() method
    - Full test suite passes with no regressions
    - grep -r "FieldMapper" ingestion/parsers/ shows all four files
  </done>
</task>

</tasks>

<verification>
uv run pytest tests/unit/test_field_mapper.py -v
uv run pytest tests/ -q --tb=short
</verification>

<success_criteria>
- 6/6 test_field_mapper.py tests GREEN
- ingestion/field_mapper.py exists with FieldMapper class
- loader.py _INSERT_SQL has 35 placeholders
- All four parsers call FieldMapper.map() in their parse() method
- 0 regressions in full test suite
</success_criteria>

<output>
After completion, create .planning/phases/20-schema-standardisation-ecs-ocsf/20-02-SUMMARY.md
</output>
