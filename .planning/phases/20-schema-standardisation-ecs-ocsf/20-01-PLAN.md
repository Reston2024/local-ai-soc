---
phase: 20-schema-standardisation-ecs-ocsf
plan: "01"
type: tdd
wave: 1
depends_on: ["20-00"]
files_modified:
  - backend/models/event.py
autonomous: true
requirements: [P20-T01]

must_haves:
  truths:
    - "NormalizedEvent accepts six new ECS-aligned fields, all Optional with None default"
    - "OCSF_CLASS_UID_MAP dict exported from event.py maps event_type strings to integer class UIDs"
    - "to_duckdb_row() tuple includes the 6 new columns at the end (backward-compatible append)"
    - "All existing 600+ tests still pass after the model change"
  artifacts:
    - path: "backend/models/event.py"
      provides: "Extended NormalizedEvent + OCSF_CLASS_UID_MAP"
      contains: "ocsf_class_uid"
      exports: ["NormalizedEvent", "OCSF_CLASS_UID_MAP"]
  key_links:
    - from: "backend/models/event.py"
      to: "tests/unit/test_normalized_event_ecs.py"
      via: "NormalizedEvent field access + OCSF_CLASS_UID_MAP lookup"
      pattern: "OCSF_CLASS_UID_MAP"
---

<objective>
Extend NormalizedEvent with six ECS-aligned optional fields and an OCSF class_uid lookup dict.

Purpose: Establish the canonical event model that all subsequent plans (FieldMapper, DuckDB migration, Sigma update, entity extractor) depend on. Every new field is Optional with None default — zero breakage to existing parsers, tests, or API endpoints.
Output: Updated backend/models/event.py with new fields and OCSF_CLASS_UID_MAP.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-schema-standardisation-ecs-ocsf/20-RESEARCH.md
@.planning/phases/20-schema-standardisation-ecs-ocsf/20-00-SUMMARY.md
@backend/models/event.py

<interfaces>
<!-- Current NormalizedEvent — executor modifies this file. -->
<!-- to_duckdb_row() currently returns a 29-element tuple in this order: -->
<!--   event_id, timestamp, ingested_at, source_type, source_file, -->
<!--   hostname, username, process_name, process_id, -->
<!--   parent_process_name, parent_process_id, -->
<!--   file_path, file_hash_sha256, command_line, -->
<!--   src_ip, src_port, dst_ip, dst_port, domain, url, -->
<!--   event_type, severity, confidence, detection_source, -->
<!--   attack_technique, attack_tactic, raw_event, tags, case_id -->
<!-- New fields must be APPENDED at the end of the tuple (positions 29-34). -->
<!-- loader.py _INSERT_SQL is NOT modified in this plan — that is plan 20-02. -->
</interfaces>
</context>

<feature>
  <name>ECS-aligned NormalizedEvent extension</name>
  <files>backend/models/event.py, tests/unit/test_normalized_event_ecs.py</files>
  <behavior>
    RED phase (stubs already exist from plan 20-00):
    - test_ecs_new_fields_present: NormalizedEvent(event_id="x", timestamp=..., ingested_at=..., ocsf_class_uid=1007, event_outcome="success", user_domain="CORP", process_executable="C:\\Windows\\System32\\cmd.exe", network_protocol="tcp", network_direction="outbound") must not raise ValidationError
    - test_ecs_new_fields_optional_default_none: NormalizedEvent(event_id="x", timestamp=..., ingested_at=...) must have ocsf_class_uid=None, event_outcome=None, user_domain=None, process_executable=None, network_protocol=None, network_direction=None
    - test_ocsf_class_uid_process_create: OCSF_CLASS_UID_MAP["process_create"] == 1007
    - test_ocsf_class_uid_network_connect: OCSF_CLASS_UID_MAP["network_connect"] == 4001
    - test_ocsf_class_uid_logon: OCSF_CLASS_UID_MAP["logon_success"] == 3002
    - test_ocsf_class_uid_dns_query: OCSF_CLASS_UID_MAP["dns_query"] == 4003
    - test_backward_compat_existing_fields: NormalizedEvent created with only legacy fields (hostname, username, process_name etc.) still works — no change to existing field names or types
    - test_to_duckdb_row_includes_new_fields: to_duckdb_row() on an event with new fields returns a tuple of length 35, positions 29-34 are (ocsf_class_uid, event_outcome, user_domain, process_executable, network_protocol, network_direction)

    GREEN phase implementation:
    1. Add six new Optional fields to NormalizedEvent AFTER case_id:
       ocsf_class_uid: Optional[int] = None
       event_outcome: Optional[str] = None
       user_domain: Optional[str] = None
       process_executable: Optional[str] = None
       network_protocol: Optional[str] = None
       network_direction: Optional[str] = None
    2. Add OCSF_CLASS_UID_MAP dict at module level (before NormalizedEvent class):
       Maps all event_type strings from the research doc to their OCSF integer class_uid.
       Full mapping from RESEARCH.md section "OCSF class_uid mapping".
    3. Update to_duckdb_row() to append 6 new values at positions 29-34.
       The docstring column order comment must be updated.
       DO NOT change positions 0-28 — loader.py depends on this exact order.
    4. Update __all__ to export OCSF_CLASS_UID_MAP.
    5. Do NOT modify to_embedding_text() — content is unchanged.
  </behavior>
  <implementation>
    Add fields after `case_id` in the class body. The six new fields follow this exact order:
    ocsf_class_uid, event_outcome, user_domain, process_executable, network_protocol, network_direction.

    OCSF_CLASS_UID_MAP must cover all event_type values listed in RESEARCH.md:
    process_create=1007, process_terminate=1007, process_access=1007,
    file_create=1001, file_delete=1001, file_create_stream_hash=1001,
    file_creation_time_changed=1001, file_delete_detected=1001,
    network_connect=4001, dns_query=4003,
    logon_success=3002, logon_failure=3002, logoff=3002,
    explicit_credential_logon=3002, kerberos_tgs_request=3002,
    kerberos_service_ticket=3002, ntlm_auth=3002,
    registry_event=1001, registry_value_set=1001,
    driver_load=1005, image_load=1005,
    scheduled_task_created=1006,
    user_account_created=3001, user_account_deleted=3001,
    wmi_event=1009, wmi_consumer=1009, wmi_subscription=1009,
    logon_event=3002.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend NormalizedEvent with ECS fields and OCSF_CLASS_UID_MAP</name>
  <files>backend/models/event.py</files>
  <behavior>
    - NormalizedEvent accepts ocsf_class_uid (int), event_outcome (str), user_domain (str), process_executable (str), network_protocol (str), network_direction (str) — all Optional, all default None
    - OCSF_CLASS_UID_MAP maps 28 event_type strings to their OCSF integer class_uid values
    - to_duckdb_row() tuple length is 35; new fields occupy positions 29-34 in insertion order
    - All existing NormalizedEvent construction patterns (e.g. NormalizedEvent(event_id=..., timestamp=..., ingested_at=...)) still work without change
  </behavior>
  <action>
    RED: Run uv run pytest tests/unit/test_normalized_event_ecs.py -v — all 8 tests must FAIL.

    GREEN: Modify backend/models/event.py:

    1. Before the NormalizedEvent class, add:
       OCSF_CLASS_UID_MAP: dict[str, int] = { ... }  (28 entries as listed in behavior above)

    2. In NormalizedEvent, after `case_id: Optional[str] = None`, add the six new fields in this exact order:
       ocsf_class_uid: Optional[int] = None
       event_outcome: Optional[str] = None
       user_domain: Optional[str] = None
       process_executable: Optional[str] = None
       network_protocol: Optional[str] = None
       network_direction: Optional[str] = None

    3. Update to_duckdb_row() return tuple: append six new values after self.case_id:
       ..., self.case_id,
       self.ocsf_class_uid,
       self.event_outcome,
       self.user_domain,
       self.process_executable,
       self.network_protocol,
       self.network_direction,
       )
       Update the docstring column order comment accordingly.

    4. Add OCSF_CLASS_UID_MAP to __all__.

    Do NOT modify to_embedding_text(), DetectionRecord, EventListResponse, GraphEntity, GraphEdge, GraphResponse, or any other class.
    Do NOT add model_config — existing Pydantic defaults are sufficient.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_normalized_event_ecs.py -v</automated>
  </verify>
  <done>
    - 8/8 tests in test_normalized_event_ecs.py pass (GREEN)
    - uv run pytest tests/ -q --tb=short shows no regressions (all pre-existing tests pass)
    - OCSF_CLASS_UID_MAP exported from backend.models.event
    - to_duckdb_row() returns 35-element tuple
  </done>
</task>

</tasks>

<verification>
uv run pytest tests/unit/test_normalized_event_ecs.py -v
uv run pytest tests/ -q --tb=short
</verification>

<success_criteria>
- 8/8 test_normalized_event_ecs.py tests GREEN
- 0 regressions in full test suite
- backend/models/event.py has OCSF_CLASS_UID_MAP dict and 6 new Optional fields
- to_duckdb_row() tuple is 35 elements long
</success_criteria>

<output>
After completion, create .planning/phases/20-schema-standardisation-ecs-ocsf/20-01-SUMMARY.md
</output>
