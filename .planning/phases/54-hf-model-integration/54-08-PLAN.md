---
phase: 54-hf-model-integration
plan: "08"
type: tdd
wave: 3
title: "Reranker unit tests and integration smoke test"
depends_on: ["54-07"]
files_modified:
  - tests/unit/test_reranker.py
autonomous: true
requirements:
  - REQ-54-03
  - REQ-54-04
must_haves:
  truths:
    - "tests/unit/test_reranker.py stubs unskipped and all 3 tests passing"
    - "uv run pytest tests/unit/test_reranker.py -q exits 0 with all tests green"
    - "Reranker service end-to-end smoke test passes (health check, /rerank, /api/query with RERANKER_ENABLED=True)"
  artifacts:
    - path: "tests/unit/test_reranker.py"
      provides: "3 implemented unit tests for reranker_client (sorted scores, graceful degradation, empty passages)"
      min_lines: 60
  key_links:
    - from: "tests/unit/test_reranker.py"
      to: "backend/services/reranker_client.py"
      via: "unittest.mock.patch of httpx.AsyncClient and settings"
---

<objective>
Unskip and implement the three reranker unit test stubs created in wave 0 (54-01), now that `backend/services/reranker_client.py` exists from 54-07. Run the full wave 3 integration smoke test to confirm the reranker service, client, and query route work end-to-end. This plan gates wave 3 completion.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/54-hf-model-integration/54-RESEARCH.md
@.planning/phases/54-hf-model-integration/54-VALIDATION.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Unskip and implement tests/unit/test_reranker.py</name>
  <files>tests/unit/test_reranker.py</files>
  <action>
Open tests/unit/test_reranker.py and remove all @pytest.mark.skip decorators and pytest.skip() calls from the three stub functions.

**test_rerank_returns_sorted_scores:**
- Patch settings.RERANKER_URL = "http://127.0.0.1:8100" and settings.RERANKER_ENABLED = True
- Mock httpx.AsyncClient.post to return a response with JSON:
  {"ranked": [{"passage": "best", "score": 0.9, "original_index": 2}, {"passage": "ok", "score": 0.5, "original_index": 0}, {"passage": "worst", "score": 0.1, "original_index": 1}]}
- Call asyncio.run(rerank_passages("query", ["ok", "worst", "best"], top_k=3))
- Assert result == ["best", "ok", "worst"]

**test_rerank_graceful_degradation:**
- Set settings.RERANKER_URL = "" (empty)
- Call asyncio.run(rerank_passages("query", ["a", "b", "c", "d", "e"], top_k=3))
- Assert result == ["a", "b", "c"] (first 3, unchanged order, no HTTP call)
- Also test RERANKER_ENABLED=False path: set URL to non-empty but ENABLED=False, verify same passthrough behavior

**test_rerank_empty_passages:**
- Call asyncio.run(rerank_passages("query", [], top_k=5))
- Assert result == []

Use unittest.mock.patch or pytest-mock (whichever is already used in adjacent test files) to mock httpx and settings. Do NOT make real HTTP calls in unit tests.

Use pytest.mark.asyncio if the test functions are async, or asyncio.run() if they are sync wrappers. Check the pattern used in test_thehive_client.py for the project convention.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_reranker.py -v --tb=short</automated>
  </verify>
  <done>Output shows 3 tests PASSED with 0 skipped, 0 errors.</done>
</task>

<task type="tdd">
  <name>Task 2: Run full unit test regression gate for wave 3</name>
  <files>tests/unit/</files>
  <action>
Run the combined unit test suite to confirm all wave 3 changes are clean:
```
uv run pytest tests/unit/ -q --tb=short
```

Key checks:
- tests/unit/test_reranker.py: 3 PASSED (was SKIPPED)
- tests/unit/test_chroma_store.py: all PASSED (including bge-m3 dimension test)
- All pre-existing tests: same pass count as before wave 3
- Zero new failures or errors
  </action>
  <verify>
    <automated>uv run pytest tests/unit/ -q --tb=short</automated>
  </verify>
  <done>Exit code 0. All 3 reranker tests PASSED. No regressions. Zero errors.</done>
</task>

<task type="manual">
  <name>Task 3: End-to-end integration smoke test with reranker enabled</name>
  <files>N/A — live service verification</files>
  <action>
Prerequisites: reranker service running (uv run python scripts/start_reranker.py), backend running.

Step 1 — verify reranker health:
```
curl http://127.0.0.1:8100/health
```
Expected: {"status": "ok", "model": "BAAI/bge-reranker-v2-m3"}

Step 2 — enable reranker in .env:
Set RERANKER_URL=http://127.0.0.1:8100 and RERANKER_ENABLED=True, then restart the backend.

Step 3 — verify backend health shows reranker as "ok":
```
curl http://127.0.0.1:8000/health
```
Expected: components.reranker.status == "ok"

Step 4 — issue a RAG query with reranker active:
```
curl -X POST http://127.0.0.1:8000/api/query \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $AUTH_TOKEN" \
  -d '{"query": "PowerShell execution suspicious activity"}'
```
Expected: HTTP 200, non-error response, total latency under 10 seconds.

Step 5 — reset to safe defaults:
Restore RERANKER_URL= (empty) and RERANKER_ENABLED=False in .env so the system stays in passthrough mode by default.
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:8100/health</automated>
  </verify>
  <done>Reranker service health check passes. /api/query with RERANKER_ENABLED=True returns results in under 10 seconds. .env reset to safe defaults after smoke test.</done>
</task>

</tasks>
