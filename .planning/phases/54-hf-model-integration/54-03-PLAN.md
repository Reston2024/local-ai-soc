---
phase: 54-hf-model-integration
plan: "03"
type: auto
wave: 1
title: "GPU migration — script update and reproducibility docs"
depends_on: ["54-02"]
files_modified:
  - REPRODUCIBILITY_RECEIPT.md
autonomous: true
requirements:
  - REQ-54-01
must_haves:
  truths:
    - "REPRODUCIBILITY_RECEIPT.md updated with GPU migration note (date, GPU model, CUDA version, TTFT before/after)"
    - "Full unit test suite exits 0 with no regressions from wave 1 changes"
  artifacts:
    - path: "REPRODUCIBILITY_RECEIPT.md"
      provides: "Phase 54 GPU migration section with hardware and timing data"
      min_lines: 12
  key_links:
    - from: "REPRODUCIBILITY_RECEIPT.md"
      to: "scripts/_start-backend.ps1"
      via: "cross-reference to GPU warning block added in 54-02"
---

<objective>
Follow-up documentation and regression gate for the manual GPU migration completed in 54-02. Updates REPRODUCIBILITY_RECEIPT.md with the hardware snapshot and timing measurements, then runs the unit test suite to confirm the GPU environment changes introduced no Python-level regressions. This plan is autonomous because it only writes documentation and runs tests — no privileged system operations.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/54-hf-model-integration/54-RESEARCH.md
@.planning/phases/54-hf-model-integration/54-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update REPRODUCIBILITY_RECEIPT.md with GPU migration note</name>
  <files>REPRODUCIBILITY_RECEIPT.md</files>
  <action>
Read REPRODUCIBILITY_RECEIPT.md and locate the hardware/environment section or last phase entry.

Add a "Phase 54 — GPU Migration" subsection with:
- Date of migration: 2026-04-16
- GPU: NVIDIA RTX 5080 (16 GB VRAM)
- CUDA version: 13.1
- System env var set: CUDA_VISIBLE_DEVICES=0 (Machine scope)
- Ollama version at time of migration: 0.18.2
- Before: CPU-only (TTFT ~300s for qwen3:14b short prompt)
- After: GPU (TTFT <30s, GPU Layers: 40+ for qwen3:14b)
- GPU warning block added to: scripts/_start-backend.ps1

Follow the format/style of existing phase sections in the file (look at the Phase 52 section as a reference).
  </action>
  <verify>
    <automated>python -c "content = open('REPRODUCIBILITY_RECEIPT.md').read(); assert 'Phase 54' in content and 'RTX 5080' in content and 'CUDA_VISIBLE_DEVICES' in content; print('receipt OK')"</automated>
  </verify>
  <done>REPRODUCIBILITY_RECEIPT.md contains a Phase 54 GPU migration section with GPU model, CUDA version, TTFT before/after, and env var name. File is valid Markdown.</done>
</task>

<task type="auto">
  <name>Task 2: Capture live GPU environment snapshot</name>
  <files>REPRODUCIBILITY_RECEIPT.md</files>
  <action>
Append or update the Phase 54 GPU Migration section in REPRODUCIBILITY_RECEIPT.md with live values captured from the running system.

Run and record the output of:
```
ollama --version
nvidia-smi --query-gpu=name,driver_version,memory.total --format=csv,noheader
```

Add these live values to the Phase 54 section under a "Live snapshot" sub-bullet. If either command fails (e.g., nvidia-smi not in PATH), note "command not available — record manually" and proceed without failing.
  </action>
  <verify>
    <automated>python -c "content = open('REPRODUCIBILITY_RECEIPT.md').read(); assert 'Phase 54' in content; print('snapshot section present')"</automated>
  </verify>
  <done>REPRODUCIBILITY_RECEIPT.md Phase 54 section includes live snapshot data (or a note that it must be recorded manually). File is valid Markdown.</done>
</task>

<task type="auto">
  <name>Task 3: Run full unit test regression gate</name>
  <files>tests/unit/</files>
  <action>
Run the full unit test suite to confirm no regressions from the wave 1 GPU environment changes:
```
uv run pytest tests/unit/ -q --tb=short -x
```

Confirm all previously passing tests still pass. No new failures introduced by the config changes in 54-01 or the script changes in 54-02. New stubs from 54-01 appear as SKIPPED (not FAILED).
  </action>
  <verify>
    <automated>uv run pytest tests/unit/ -q --tb=short</automated>
  </verify>
  <done>Exit code 0 (or same pass/skip counts as before wave 1). No test that was previously passing now fails.</done>
</task>

</tasks>
