---
phase: 54-hf-model-integration
plan: "05"
type: tdd
wave: 2
title: "bge-m3 tests and RAG query verification"
depends_on: ["54-04"]
files_modified:
  - tests/unit/test_chroma_store.py
autonomous: true
requirements:
  - REQ-54-02
must_haves:
  truths:
    - "test_bge_m3_embed_dimension unskipped and passing (len(embedding) == 1024)"
    - "uv run pytest tests/unit/test_chroma_store.py -q exits 0 with all tests green"
    - "POST /api/query returns HTTP 200 after bge-m3 rebuild (no embedding dimension mismatch)"
  artifacts:
    - path: "tests/unit/test_chroma_store.py"
      provides: "bge-m3 dimension test implemented and green (was skipped stub in 54-01)"
      min_lines: 15
  key_links:
    - from: "tests/unit/test_chroma_store.py"
      to: "backend/services/ollama_client.py"
      via: "AsyncMock of OllamaClient.embed returning [0.0] * 1024"
---

<objective>
Unskip and implement the bge-m3 dimension test stub created in wave 0, verifying that the Ollama embedding layer correctly produces 1024-dimensional vectors. Then smoke-test the live RAG query endpoint to confirm the ChromaDB rebuild in 54-04 produced consistent embeddings with no dimension mismatch errors. This plan gates wave 2 completion before the reranker service is added in wave 3.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/54-hf-model-integration/54-RESEARCH.md
@.planning/phases/54-hf-model-integration/54-VALIDATION.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Unskip and implement test_bge_m3_embed_dimension</name>
  <files>tests/unit/test_chroma_store.py</files>
  <action>
Open tests/unit/test_chroma_store.py and find the skipped `test_bge_m3_embed_dimension` stub added in wave 0 (54-01).

Remove the `@pytest.mark.skip` decorator and the `pytest.skip()` call inside the body.

Implement the test body:
- Mock `OllamaClient.embed` using `unittest.mock.AsyncMock` (or pytest-mock if already used in adjacent tests) to return `[0.0] * 1024` (a list of 1024 zeros)
- Call the embed function with a short test string (e.g., `"PowerShell execution"`)
- Assert `len(embedding) == 1024`
- The test must NOT call Ollama live — use the mock

Example structure:
```python
async def test_bge_m3_embed_dimension():
    with patch("backend.services.ollama_client.OllamaClient.embed", new_callable=AsyncMock) as mock_embed:
        mock_embed.return_value = [0.0] * 1024
        embedding = await mock_embed("PowerShell execution")
        assert len(embedding) == 1024
```

Adjust the patch path to match the actual import location used in chroma_store.py.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_chroma_store.py::test_bge_m3_embed_dimension -v --tb=short</automated>
  </verify>
  <done>test_bge_m3_embed_dimension shows PASSED (green, not skipped). len([0.0]*1024) == 1024 assertion passes.</done>
</task>

<task type="tdd">
  <name>Task 2: Run full chroma_store unit test suite</name>
  <files>tests/unit/test_chroma_store.py</files>
  <action>
Run:
```
uv run pytest tests/unit/test_chroma_store.py -q --tb=short
```

All tests pass. Zero errors. The bge-m3 dimension test is now green (not skipped). No pre-existing tests regressed.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_chroma_store.py -q --tb=short</automated>
  </verify>
  <done>Exit code 0. All tests PASSED. The bge-m3 stub is now green.</done>
</task>

<task type="manual">
  <name>Task 3: Smoke-test RAG query endpoint with bge-m3 embeddings</name>
  <files>N/A — live service verification</files>
  <action>
Start the backend:
```
uv run python -m backend.main
```
(or use `scripts/_start-backend.ps1`)

Issue a test query:
```
curl -X POST http://127.0.0.1:8000/api/query \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $AUTH_TOKEN" \
  -d '{"query": "PowerShell execution suspicious activity"}'
```

Confirm: HTTP 200 response with non-empty results array (or empty array if no events ingested — NOT a 500 error). Check backend logs for any embedding dimension mismatch errors — there should be none.
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" -X POST http://127.0.0.1:8000/api/query -H "Content-Type: application/json" -d '{"query": "test"}'</automated>
  </verify>
  <done>POST /api/query returns HTTP 200. No 500 errors. No embedding dimension mismatch errors in backend logs.</done>
</task>

</tasks>
