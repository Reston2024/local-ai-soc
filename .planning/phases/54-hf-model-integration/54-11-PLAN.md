---
id: "54-11"
title: "Fix: Rebuild dashboard dist to include Reranker health row"
phase: 54
type: gap_closure
gap_closure: true
requires: []
---

# Plan 54-11 — Fix: Rebuild dashboard dist

## Context

Plan 54-10 added a Reranker health row to `dashboard/src/views/OverviewView.svelte` (line 440) using an `{#if componentHealth?.components?.reranker}` guard. However the dashboard dist was never rebuilt. The `dashboard/dist/assets/*.js` bundle does not contain "reranker" so the row never appears in the live dashboard.

## Must Haves

<must_haves>
  <truths>
    <truth>dashboard/dist assets contain "reranker" string after rebuild</truth>
    <truth>Reranker health row visible in live dashboard System Health panel</truth>
    <truth>No other dashboard UI regressions (svelte-check passes)</truth>
  </truths>
  <artifacts>
    <artifact>dashboard/dist/ — updated build output</artifact>
  </artifacts>
  <key_links>
    <link>dashboard/src/views/OverviewView.svelte (line 440 — reranker row source)</link>
    <link>dashboard/package.json (build script)</link>
  </key_links>
</must_haves>

## Tasks

<task type="auto">
  <name>Rebuild dashboard dist</name>
  <files>dashboard/dist/ (output)</files>
  <action>
    Run `npm run build` in the `dashboard/` directory:
    ```
    cd dashboard && npm run build
    ```
    This compiles OverviewView.svelte (including the reranker row at line 440) into the dist/ bundle.
  </action>
  <verify>
    grep -r "reranker" dashboard/dist/assets/*.js | head -3
    Should return at least one match.
  </verify>
  <done>dist/assets/*.js contains "reranker" string</done>
</task>

<task type="auto">
  <name>Commit updated dist</name>
  <files>dashboard/dist/</files>
  <action>
    Stage and commit all changed dist files:
    ```
    git add dashboard/dist/
    git commit -m "fix(54-11): rebuild dashboard dist — include reranker health row"
    ```
  </action>
  <verify>
    git show --stat HEAD | grep "dashboard/dist"
    Should show dist files changed.
  </verify>
  <done>Commit exists with dashboard/dist changes</done>
</task>
