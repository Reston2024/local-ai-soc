---
phase: 54-hf-model-integration
plan: "01"
type: tdd
wave: 0
title: "Test stubs + config additions"
depends_on: []
files_modified:
  - backend/core/config.py
  - tests/unit/test_reranker.py
  - tests/unit/test_chroma_store.py
autonomous: true
requirements:
  - REQ-54-01
  - REQ-54-02
  - REQ-54-03
must_haves:
  truths:
    - "RERANKER_URL, RERANKER_TOP_K, RERANKER_ENABLED added to Settings with safe defaults"
    - "tests/unit/test_reranker.py exists with 3 skipped stubs (not error-on-import)"
    - "tests/unit/test_chroma_store.py gains a skipped bge-m3 dimension stub"
    - "uv run pytest tests/unit/test_reranker.py -v exits 0 with all tests skipped, no collection errors"
  artifacts:
    - path: "backend/core/config.py"
      provides: "RERANKER_URL, RERANKER_TOP_K, RERANKER_ENABLED settings fields"
      min_lines: 5
    - path: "tests/unit/test_reranker.py"
      provides: "3 skipped stub tests for reranker client behavior"
      min_lines: 30
    - path: "tests/unit/test_chroma_store.py"
      provides: "bge-m3 dimension stub appended to existing test file"
      min_lines: 10
  key_links:
    - from: "tests/unit/test_reranker.py"
      to: "backend/services/reranker_client.py"
      via: "pytest.mark.skip stub — implemented in 54-08"
    - from: "backend/core/config.py"
      to: "backend/services/reranker_client.py"
      via: "settings.RERANKER_URL, settings.RERANKER_ENABLED"
---

<objective>
Create the scaffold for Phase 54 before any implementation begins. Add three new settings fields to `backend/core/config.py` (with safe defaults that preserve existing behavior), then write placeholder test files so the import graph can be validated immediately. No implementation of the reranker logic yet — only shapes and stubs. This wave establishes the RED contracts that all subsequent waves must turn GREEN.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/54-hf-model-integration/54-RESEARCH.md
@.planning/phases/54-hf-model-integration/54-VALIDATION.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Add reranker settings to backend/core/config.py</name>
  <files>backend/core/config.py</files>
  <action>
Open backend/core/config.py and locate the "Phase 52: TheHive" block near line 169 as an insertion reference point.

Add a "Phase 54: Reranker" settings block immediately after the TheHive block and before the Server block. Insert these three fields:

```python
# Phase 54: Reranker microservice (bge-reranker-v2-m3)
RERANKER_URL: str = ""          # e.g. "http://127.0.0.1:8100" — empty disables reranking
RERANKER_TOP_K: int = 5         # number of passages to return after reranking
RERANKER_ENABLED: bool = False  # set True once reranker service is confirmed running
```

Verify the module still imports cleanly:
```
uv run python -c "from backend.core.config import settings; print(settings.RERANKER_URL, settings.RERANKER_TOP_K, settings.RERANKER_ENABLED)"
```
Expected output: ` 5 False` (empty string, 5, False) — safe defaults that keep existing behavior unchanged.
  </action>
  <verify>
    <automated>uv run python -c "from backend.core.config import settings; assert settings.RERANKER_URL == ''; assert settings.RERANKER_TOP_K == 5; assert settings.RERANKER_ENABLED == False; print('config OK')"</automated>
  </verify>
  <done>Command prints "config OK" with no exceptions. Three new fields exist on the Settings class with correct defaults.</done>
</task>

<task type="tdd">
  <name>Task 2: Create tests/unit/test_reranker.py with skipped stubs</name>
  <files>tests/unit/test_reranker.py</files>
  <action>
Create tests/unit/test_reranker.py. The file must be importable without any implementation existing yet — use pytest.mark.skip decorators (not importorskip, which would skip the whole module).

Add a module-level comment: `# Phase 54 — Reranker unit tests. Stubs in wave 0; implemented in wave 3 (54-08).`

Write three stub test functions:

1. `test_rerank_returns_sorted_scores` — stub asserting the reranker client returns passages sorted by descending score. Body: `pytest.skip("stub — implement in 54-08")`

2. `test_rerank_graceful_degradation` — stub asserting when RERANKER_URL is empty, rerank_passages returns passages[:top_k] unchanged. Body: `pytest.skip("stub — implement in 54-08")`

3. `test_rerank_empty_passages` — stub asserting calling rerank_passages with an empty list returns an empty list. Body: `pytest.skip("stub — implement in 54-08")`

Each stub must have `@pytest.mark.skip(reason="stub — implement in 54-08")` decorator AND a `pytest.skip()` call in the body for safety.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_reranker.py -v --tb=short 2>&1 | tail -15</automated>
  </verify>
  <done>Exit code 0. Output shows 3 tests with status SKIPPED and 0 errors. No collection errors.</done>
</task>

<task type="tdd">
  <name>Task 3: Add bge-m3 dimension stub to tests/unit/test_chroma_store.py</name>
  <files>tests/unit/test_chroma_store.py</files>
  <action>
Open tests/unit/test_chroma_store.py and read its current content to find a natural insertion point (after the last existing test function).

Append a new test function `test_bge_m3_embed_dimension` decorated with:
```python
@pytest.mark.skip(reason="stub — implement in 54-05 after bge-m3 is pulled")
```

The stub body should document the intent (assert that embedding a short string via Ollama with model=bge-m3 returns a vector of length 1024) and call `pytest.skip("stub — implement in 54-05")` to be safe even if the decorator is somehow not applied.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_chroma_store.py -v -k test_bge_m3 --tb=short 2>&1 | tail -10</automated>
  </verify>
  <done>Output shows 1 test SKIPPED with no errors.</done>
</task>

<task type="tdd">
  <name>Task 4: Smoke-run unit tests to confirm no regressions</name>
  <files>tests/unit/test_reranker.py, tests/unit/test_chroma_store.py</files>
  <action>
Run the combined smoke check:
```
uv run pytest tests/unit/test_reranker.py tests/unit/test_chroma_store.py -v --tb=short
```

Confirm: all pre-existing chroma_store tests still pass, the 3 new reranker stubs are SKIPPED, the bge-m3 stub is SKIPPED, 0 ERRORs.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_reranker.py tests/unit/test_chroma_store.py -q --tb=short</automated>
  </verify>
  <done>Exit code 0. No test that was previously passing now fails. All new stubs appear as SKIPPED.</done>
</task>

</tasks>
