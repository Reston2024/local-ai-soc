---
phase: 23.5-security-hardening
plan: "07"
type: execute
wave: 5
depends_on: ["23.5-02", "23.5-03", "23.5-04", "23.5-05", "23.5-06"]
files_modified: []
autonomous: false
requirements:
  - P23.5-T01
  - P23.5-T02
  - P23.5-T03
  - P23.5-T04
  - P23.5-T05
  - P23.5-T06
  - P23.5-T07
  - P23.5-T08
  - P23.5-T09
  - P23.5-T10
  - P23.5-T11
  - P23.5-T12

must_haves:
  truths:
    - "All 12 requirement IDs have green tests or verified manual checks"
    - "Full test suite exits 0"
    - "No CRITICAL or HIGH security findings remain unaddressed"
  artifacts:
    - path: "tests/security/test_auth_hardening.py"
      provides: "5 active tests: T01, T02, T09, T11, T12 — all PASS"
    - path: "tests/security/test_injection_hardening.py"
      provides: "2 active tests: T03, T04 — all PASS"
    - path: "tests/security/test_sigma_hardening.py"
      provides: "1 active test: T05 — PASS"
    - path: "tests/sigma_smoke/"
      provides: "meta rule smoke tests: T07 — 3 PASS"
  key_links:
    - from: "All security tests"
      to: "Phase 24 gate"
      via: "uv run pytest exits 0 before Phase 24 can proceed"
      pattern: "uv run pytest"
---

<objective>
Final verification checkpoint for Phase 23.5. Confirm all 12 security requirements are
satisfied before Phase 24 proceeds.

Purpose: The phase goal states "all security tests must be green before Phase 24 proceeds."
This checkpoint verifies that condition is met and that manual checks for T06, T08, T10
are documented.
Output: Verified phase completion. Phase 24 gate is open.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full automated verification</name>
  <files></files>
  <action>
Run the complete test suite and collect results. Do not skip any test file.

Commands to run in sequence:

1. Full suite:
   `uv run pytest --tb=short -q`

2. Security tests with verbose output:
   `uv run pytest tests/security/ -v`

3. Sigma smoke tests including meta rules:
   `uv run pytest tests/sigma_smoke/ -v`

4. Eval tests (injection eval fixture):
   `uv run pytest tests/eval/ -k injection -v`

5. Spot checks for infrastructure changes:
   ```
   uv run python -c "
   from backend.core.logging import setup_logging
   import logging.handlers, logging, tempfile
   d = tempfile.mkdtemp()
   setup_logging(log_dir=d)
   handlers = logging.getLogger().handlers
   timed = [h for h in handlers if isinstance(h, logging.handlers.TimedRotatingFileHandler)]
   print(f'TimedRotatingFileHandler count: {len(timed)}')
   assert len(timed) >= 1, 'FAIL: no TimedRotatingFileHandler'
   print('PASS: Log rotation configured correctly')
   "
   ```

   ```
   uv run python -c "
   from pydantic import ValidationError
   from backend.core.config import Settings
   try:
       Settings(AUTH_TOKEN='changeme')
       print('FAIL: changeme token was accepted')
   except (ValueError, ValidationError) as e:
       print(f'PASS: changeme token rejected: {e}')
   "
   ```

6. Caddyfile header check (no running Caddy needed — grep the file):
   ```
   uv run python -c "
   content = open('config/caddy/Caddyfile').read()
   assert 'Content-Security-Policy' in content, 'FAIL: CSP header missing'
   assert 'X-Frame-Options' in content, 'FAIL: X-Frame-Options missing'
   assert 'X-Content-Type-Options' in content, 'FAIL: X-Content-Type-Options missing'
   print('PASS: All security headers present in Caddyfile')
   "
   ```

Collect all output. If any assertion fails, do NOT proceed to checkpoint — fix the
regression first by referencing the appropriate plan (02-06) for that requirement.
  </action>
  <verify>
    <automated>uv run pytest --tb=short -q 2>&1 | tail -5</automated>
  </verify>
  <done>uv run pytest exits 0. All security tests PASS. No regressions in existing suite.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
  Complete Phase 23.5 security hardening:
  - T01: AUTH_TOKEN "changeme" rejected at startup (Settings validation)
  - T02: Legacy-admin path requires TOTP code (no MFA bypass)
  - T03: _scrub_injection decodes base64 + Unicode NFC before pattern scan; EVIDENCE in system turn
  - T04: chat.py scrubs body.question before prompt construction
  - T05: Sigma SQL injection test active and passing (rule values parameterized)
  - T06: scripts/status.ps1 checks port 11434 reachability; THREAT_MODEL.md updated
  - T07: Three meta-detection Sigma rules (auth burst, LLM spike, collection delete) parse cleanly
  - T08: CSP + security headers added to Caddyfile
  - T09: /health endpoint returns generic error strings, not exception text or file paths
  - T10: TimedRotatingFileHandler with 30-day retention replaces size-based rotation
  - T11: TOTP replay prevention persists to SQLite, survives restart simulation
  - T12: Full prompt_text (truncated 64KB) + prompt_hash stored in llm_calls
  </what-built>
  <how-to-verify>
  Automated checks (from Task 1 output):
  1. Confirm `uv run pytest` exits 0 with no failures
  2. Confirm all 8 tests in tests/security/ PASS (5 auth_hardening + 2 injection + 1 sigma)
  3. Confirm 3 meta rule tests in tests/sigma_smoke/ PASS

  Manual spot checks (optional but recommended):
  4. Open config/caddy/Caddyfile — confirm `header { Content-Security-Policy ... }` block appears before the first `handle` directive in the localhost site block
  5. Open scripts/status.ps1 — confirm `Test-NetConnection` and `11434` are present
  6. Open backend/core/logging.py — confirm `TimedRotatingFileHandler` with `when="midnight"` and `backupCount=30`

  If running Caddy: `curl -I https://localhost/ | grep -i content-security-policy`
  should return the CSP header.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass. Describe any failures to trigger targeted fixes.</resume-signal>
</task>

</tasks>

<verification>
This plan IS the verification. Success when checkpoint receives "approved".
</verification>

<success_criteria>
- `uv run pytest` exits 0
- All 12 requirement IDs verified (8 automated tests + 4 manual infrastructure checks)
- Human approves checkpoint
- Phase 23.5 marked COMPLETE in STATE.md
- Phase 24 gate is open
</success_criteria>

<output>
After checkpoint approval:
1. Create `.planning/phases/23.5-security-hardening/23.5-07-SUMMARY.md`
2. Update `.planning/STATE.md`:
   - current_phase: Phase 23.5 (complete)
   - current_plan: 23.5-07 complete — all 12 security findings closed; 8 security tests pass; Phase 23.5 FULLY COMPLETE
3. Update `.planning/ROADMAP.md`:
   - Phase 23.5 Status: COMPLETE
   - All plan checkboxes: [x]
</output>
