---
phase: 10-compliance-hardening
plan: 05
type: execute
wave: 2
depends_on: [10-02, 10-03, 10-04]
files_modified:
  - pyproject.toml
  - REPRODUCIBILITY_RECEIPT.md
autonomous: true
requirements: [P10-T05]

must_haves:
  truths:
    - "All 12 previously-loose (>=) direct dependencies in pyproject.toml now use == exact version pins"
    - "backend/requirements.txt no longer exists"
    - "REPRODUCIBILITY_RECEIPT.md status changed from BOOTSTRAPPING to VERIFIED; all TBD entries filled"
    - "uv run pip-audit exits 0 OR a baseline advisory report is captured before enforcing clean"
    - "pytest-cov is added to pyproject.toml dev dependencies"
    - "Existing tests still pass after pyproject.toml change"
  artifacts:
    - path: "pyproject.toml"
      provides: "== pins for pydantic, pydantic-settings, httpx, chromadb, langgraph, langchain-ollama, pySigma, PyYAML, evtx, pytest, pytest-asyncio, ruff; pytest-cov in dev deps"
    - path: "REPRODUCIBILITY_RECEIPT.md"
      provides: "status=VERIFIED; all TBD fields filled from uv.lock and system state"
  key_links:
    - from: "pyproject.toml"
      to: "uv.lock"
      via: "== pins must match locked versions exactly"
---

<objective>
Pin all loose Python dependencies in pyproject.toml to exact `==` versions using uv.lock as the source of truth. Delete the stale `backend/requirements.txt`. Add `pytest-cov` to dev dependencies (required by CI in Plan 06). Update `REPRODUCIBILITY_RECEIPT.md` from BOOTSTRAPPING to VERIFIED, filling all TBD entries.

Purpose: REQ-07 / CIS Controls v8.2 — reproducible builds require exact pinning. The current >= specifiers mean a `uv sync` could install a breaking version. Deleting requirements.txt eliminates the version divergence vector.
Output: Updated pyproject.toml with == pins, deleted requirements.txt, verified REPRODUCIBILITY_RECEIPT.md.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-compliance-hardening/10-CONTEXT.md
@.planning/phases/10-compliance-hardening/10-RESEARCH.md
@pyproject.toml
@REPRODUCIBILITY_RECEIPT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin dependencies in pyproject.toml, delete backend/requirements.txt, add pytest-cov</name>
  <files>pyproject.toml</files>
  <action>
    First, read pyproject.toml in full and read the current uv.lock to confirm exact versions.

    Run the following to get current installed versions as a cross-check:
    `uv run pip show pydantic pydantic-settings httpx chromadb langgraph langchain-ollama pySigma PyYAML evtx pytest pytest-asyncio ruff 2>&1`

    Apply these exact version pins (from uv.lock research — verify against actual uv.lock before applying):
    - `pydantic>=2.0` → `pydantic==2.12.5`
    - `pydantic-settings>=2.0` → `pydantic-settings==2.13.1`
    - `httpx>=0.28.1` (appears twice — deduplicate to single entry) → `httpx==0.28.1`
    - `chromadb>=0.6.3` → `chromadb==1.5.5`
    - `langgraph>=0.4.0` → `langgraph==1.1.2`
    - `langchain-ollama>=0.3.0` → `langchain-ollama==1.0.1`
    - `pySigma>=0.11.0` → `pySigma==1.2.0`
    - `PyYAML>=6.0.2` → `PyYAML==6.0.3`
    - `evtx>=0.8.2` → `evtx==0.11.0`
    - `pytest>=8.3.5` → `pytest==8.3.5` (verify exact from uv.lock — may differ)
    - `pytest-asyncio>=0.25.0` → `pytest-asyncio==1.3.0`
    - `ruff>=0.9.0` → `ruff==0.15.6`

    IMPORTANT: Before applying any pin, verify the version against `uv.lock` by running:
    `grep -A2 "name = \"pydantic\"" uv.lock | head -5`
    for each package. Use the uv.lock version, NOT the research document version, as the source of truth.

    Also: if `httpx` appears twice in pyproject.toml (once in main deps, once in test deps), deduplicate — keep it in main dependencies only with the pinned version.

    Add `pytest-cov` to the dev/optional dependencies section. If no `[project.optional-dependencies]` dev group exists, add:
    ```toml
    [project.optional-dependencies]
    dev = [
        "pytest-cov>=6.0.0",
    ]
    ```
    If the dev group already exists, append `pytest-cov>=6.0.0` to it.

    After editing pyproject.toml, run `uv sync` to verify the lock file is consistent:
    `uv sync 2>&1 | tail -10`

    Delete `backend/requirements.txt`:
    `rm /c/Users/Admin/AI-SOC-Brain/backend/requirements.txt`
  </action>
  <verify>
    <automated>cd /c/Users/Admin/AI-SOC-Brain && uv run python -c "import tomllib; d=tomllib.load(open('pyproject.toml','rb')); deps=d['project']['dependencies']; bad=[x for x in deps if '>=' in x and 'pytest' not in x.lower() and 'ruff' not in x.lower()]; print('Loose deps:', bad or 'NONE')" 2>&1</automated>
  </verify>
  <done>No >= specifiers remain for the 12 target packages; backend/requirements.txt is deleted; uv sync completes without error; pytest-cov present in optional deps</done>
</task>

<task type="auto">
  <name>Task 2: Update REPRODUCIBILITY_RECEIPT.md to VERIFIED status</name>
  <files>REPRODUCIBILITY_RECEIPT.md</files>
  <action>
    Read `REPRODUCIBILITY_RECEIPT.md` in full. Identify all TBD entries.

    The research doc identifies these TBD fields:
    - pySigma version → fill from uv.lock: `pySigma==1.2.0`
    - langgraph version → fill from uv.lock: `langgraph==1.1.2`
    - evtx version → fill from uv.lock: `evtx==0.11.0`
    - Ollama version → run `ollama --version 2>&1` to get actual version
    - Model hashes (qwen3:14b, mxbai-embed-large) → run `ollama show --verbose qwen3:14b 2>&1 | head -5` and `ollama show --verbose mxbai-embed-large 2>&1 | head -5`; if Ollama is not running, use "see: ollama show --verbose <model>" as the value
    - Docker version → run `docker --version 2>&1`
    - Caddy version → `docker run --rm caddy:2.9-alpine caddy version 2>&1` or use `caddy:2.9-alpine (see digest in docker-compose.yml)`
    - Svelte/Vite version → run `cd dashboard && cat package.json | grep -E '"svelte"|"vite"'` to get versions
    - Cytoscape, D3 versions → same approach from package.json

    For each TBD entry found in the file:
    1. Run the appropriate command to get the actual value
    2. Replace the TBD placeholder with the real value

    Also:
    - Change the status line from `BOOTSTRAPPING` to `VERIFIED`
    - Update the date to `2026-03-26`
    - Add a note: "Pinned via uv.lock — run `uv export --no-hashes` to reproduce"

    Run `uv run pip-audit --desc 2>&1` to get the baseline audit state. If vulnerabilities are found, DO NOT fail the task — instead append a "## pip-audit Baseline (2026-03-26)" section to REPRODUCIBILITY_RECEIPT.md listing the findings as a known baseline. If clean, note "pip-audit: clean as of 2026-03-26".
  </action>
  <verify>
    <automated>cd /c/Users/Admin/AI-SOC-Brain && grep -c "TBD" REPRODUCIBILITY_RECEIPT.md && echo "TBD count above (should be 0 or near 0)" 2>&1</automated>
  </verify>
  <done>REPRODUCIBILITY_RECEIPT.md status reads VERIFIED; TBD count is 0 or minimized to items that require running services not available in CI; pip-audit baseline captured</done>
</task>

</tasks>

<verification>
Verify pinning and test suite still passes:
```
uv run pytest tests/unit/ tests/security/ -q 2>&1 | tail -10
uv run pip-audit --desc 2>&1 | tail -10
```

Expected: tests pass; pip-audit output captured (baseline or clean).
</verification>

<success_criteria>
- pyproject.toml: all 12 target packages use == pins matching uv.lock
- backend/requirements.txt deleted (file does not exist)
- pytest-cov in optional dev dependencies
- REPRODUCIBILITY_RECEIPT.md: status=VERIFIED; no ungated TBD entries
- pip-audit baseline captured in REPRODUCIBILITY_RECEIPT.md or clean
- Existing tests still pass after pyproject.toml changes
</success_criteria>

<output>
After completion, create `.planning/phases/10-compliance-hardening/10-05-SUMMARY.md`
</output>
