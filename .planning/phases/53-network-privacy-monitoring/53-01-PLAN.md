---
phase: 53-network-privacy-monitoring
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/unit/test_privacy_blocklist.py
  - tests/unit/test_privacy_detection.py
  - tests/unit/test_privacy_api.py
autonomous: true
requirements:
  - PRIV-01
  - PRIV-02
  - PRIV-03
  - PRIV-04
  - PRIV-05
  - PRIV-06
  - PRIV-07
  - PRIV-08
  - PRIV-09
  - PRIV-10
  - PRIV-11

must_haves:
  truths:
    - "Three test files exist with stubs covering all 12 PRIV requirements"
    - "Every stub SKIPS cleanly — no import errors, no test failures"
    - "Full pytest suite shows N+12 tests with 12 SKIPPED, 0 new failures"
  artifacts:
    - path: "tests/unit/test_privacy_blocklist.py"
      provides: "Stubs for PRIV-01 through PRIV-04 (parsing + store)"
      min_lines: 60
    - path: "tests/unit/test_privacy_detection.py"
      provides: "Stubs for PRIV-05 through PRIV-08 (scanner logic)"
      min_lines: 60
    - path: "tests/unit/test_privacy_api.py"
      provides: "Stubs for PRIV-09 through PRIV-11 (API + normalizer)"
      min_lines: 50
  key_links:
    - from: "tests/unit/test_privacy_blocklist.py"
      to: "backend/services/intel/privacy_blocklist.py"
      via: "pytest.importorskip at module level"
      pattern: "pytest.importorskip.*privacy_blocklist"
    - from: "tests/unit/test_privacy_detection.py"
      to: "backend/api/privacy.py"
      via: "pytest.importorskip at module level"
      pattern: "pytest.importorskip.*privacy"
---

<objective>
Create Wave 0 TDD stubs for Phase 53 network privacy monitoring. Three test files define
the contracts that Plans 02–03 must implement. All stubs use the double-guard skip pattern
established in Phases 48/49/51/52.

Purpose: Define the behavioral contracts before implementation begins. Executor in Wave 1
will turn these stubs GREEN.
Output: Three new test files, all stubs SKIPPED, full suite regression-free.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/53-network-privacy-monitoring/53-RESEARCH.md

<!-- Key patterns from existing codebase -->
<!-- Double-guard skip (Phases 48/49/51/52 standard): -->
<!-- 1. pytest.importorskip at MODULE level (not per-test) — whole file skips atomically -->
<!-- 2. Each test body also calls pytest.skip() as second guard (survives linter decorator strip) -->

<!-- From tests/unit/test_hayabusa_scanner.py (Phase 48 pattern): -->
<!-- hayabusa_scanner = pytest.importorskip("ingestion.hayabusa_scanner") -->
<!-- class TestHayabusaScanner: -->
<!--     def test_scan_returns_list(self): -->
<!--         pytest.skip("Wave 0 stub — implement in Plan 48-02") -->

<!-- From tests/unit/test_thehive_client.py (Phase 52 pattern with _TH_AVAILABLE): -->
<!-- _TH_AVAILABLE = importlib.util.find_spec("backend.services.thehive_client") is not None -->
<!-- @pytest.mark.skip(reason="Wave 0 stub") -->
<!-- def test_foo(self): -->
<!--     if not _TH_AVAILABLE: pytest.skip("module absent") -->
<!--     pytest.skip("Wave 0 stub") -->

<!-- Phase 53 uses pytest.importorskip at module level (cleaner, same as 48/49/51) -->
<!-- PrivacyBlocklistStore will live in: backend/services/intel/privacy_blocklist.py -->
<!-- Privacy scanner + API will live in: backend/api/privacy.py -->
<!-- Normalizer extension: ingestion/jobs/malcolm_collector.py _normalize_http() -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test_privacy_blocklist.py with stubs for PRIV-01 through PRIV-04</name>
  <files>tests/unit/test_privacy_blocklist.py</files>
  <action>
Create `tests/unit/test_privacy_blocklist.py` with module-level importorskip guard and 6 stubs.

Module-level guard (line 1–3):
```python
privacy_blocklist = pytest.importorskip(
    "backend.services.intel.privacy_blocklist",
    reason="Wave 0 — implement in Plan 53-02"
)
```

Stubs to write (all use double-guard: @pytest.mark.skip decorator + pytest.skip() body):

PRIV-01: `test_parse_easyprivacy_extracts_domains`
- Behavior: `_parse_easyprivacy("||tracker.example.com^\n||ads.test.org^\n! comment\n")` returns `["tracker.example.com", "ads.test.org"]`
- Comment lines (starting `!`) and blank lines are excluded
- assert False body (stub)

PRIV-02: `test_parse_disconnect_extracts_all_categories`
- Behavior: `_parse_disconnect(json_text)` where json_text is minimal services.json returns list of `(domain, category)` tuples covering Email and Advertising categories
- assert False body (stub)

PRIV-03: `test_store_upsert_and_lookup`
- Behavior: `PrivacyBlocklistStore(conn)` with in-memory SQLite; `store.upsert_domain("tracker.test", "easyprivacy", None)`; `store.is_tracker("tracker.test")` returns True; `store.is_tracker("legit.example.com")` returns False
- assert False body (stub)

PRIV-04: `test_worker_populates_store`
- Behavior: `PrivacyWorker` with mocked httpx responses calls `_parse_easyprivacy` and `_parse_disconnect`, then upserts into the store
- assert False body (stub)

PRIV-04b: `test_feed_meta_updated_after_sync`
- Behavior: After PrivacyWorker._sync(), `store.get_feed_status()` returns a list containing dicts with `feed`, `last_sync`, `domain_count` keys
- assert False body (stub)

PRIV-11: `test_normalize_http_extended_fields`  (placed here as normalizer test for HTTP schema)
- Behavior: `_normalize_http(doc)` where doc has `zeek.http.referrer="https://mail.example.com"`, `zeek.http.request_body_len=8192`, `zeek.http.response_body_len=45`, `zeek.http.resp_mime_types=["image/gif"]` returns NormalizedEvent with `http_referrer="https://mail.example.com"`, `http_request_body_len=8192`, `http_response_body_len=45`, `http_resp_mime_type="image/gif"`
- assert False body (stub)

NOTE: This stub tests ingestion/jobs/malcolm_collector._normalize_http extension, not the blocklist, but lives here to keep test file count minimal. Use module-level importorskip only for `privacy_blocklist`; the normalizer stub uses `@pytest.mark.skip` alone (separate module concern).
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_privacy_blocklist.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>File exists, 6 stubs all SKIP with "Wave 0 stub" reason, 0 errors, 0 failures</done>
</task>

<task type="auto">
  <name>Task 2: Create test_privacy_detection.py with stubs for PRIV-05 through PRIV-08</name>
  <files>tests/unit/test_privacy_detection.py</files>
  <action>
Create `tests/unit/test_privacy_detection.py` with module-level importorskip guard and 4 stubs covering the scanner logic.

Module-level guard:
```python
privacy_api = pytest.importorskip(
    "backend.api.privacy",
    reason="Wave 0 — implement in Plan 53-03"
)
```

Stubs (all double-guard: decorator + body):

PRIV-05: `test_cookie_exfil_detection_fires_on_large_body_to_tracker`
- Behavior: `run_privacy_scan(app)` with mock DuckDB returning one row with `http_request_body_len=8192`, `domain="tracker.example.com"` (in blocklist) returns at least one detection with `hit_type="cookie_exfil"`
- assert False body (stub)

PRIV-06: `test_tracking_pixel_detection_fires_on_tiny_image_from_tracker`
- Behavior: `run_privacy_scan(app)` with mock DuckDB returning one row with `http_response_body_len=43`, `http_resp_mime_type="image/gif"`, `domain="pixel.example.com"` (in blocklist) returns at least one detection with `hit_type="tracking_pixel"`
- assert False body (stub)

PRIV-07: `test_no_false_positive_for_non_tracker_domain`
- Behavior: `run_privacy_scan(app)` with large body POST to `"github.com"` (NOT in blocklist) returns 0 detections
- assert False body (stub)

PRIV-08: `test_detection_record_uses_privacy_source_tag`
- Behavior: Detections created by scanner have `detection_source="privacy"` and `rule_id` starting with `"privacy-"`
- assert False body (stub)
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_privacy_detection.py -v 2>&1 | tail -15</automated>
  </verify>
  <done>File exists, 4 stubs all SKIP, 0 errors, 0 failures</done>
</task>

<task type="auto">
  <name>Task 3: Create test_privacy_api.py with stubs for PRIV-09 and PRIV-10, then verify full suite</name>
  <files>tests/unit/test_privacy_api.py</files>
  <action>
Create `tests/unit/test_privacy_api.py` with module-level importorskip guard and 2 stubs covering the REST API.

Module-level guard (same as detection file — both come from `backend.api.privacy`):
```python
privacy_api = pytest.importorskip(
    "backend.api.privacy",
    reason="Wave 0 — implement in Plan 53-03"
)
```

Stubs:

PRIV-09: `test_hits_endpoint_returns_list`
- Behavior: `GET /api/privacy/hits` returns 200 with JSON body containing a `hits` list
- Use `TestClient(app)` with a minimal FastAPI app that includes the privacy router
- assert False body (stub)

PRIV-10: `test_feeds_endpoint_returns_status`
- Behavior: `GET /api/privacy/feeds` returns 200 with JSON body containing a `feeds` list where each entry has `feed`, `last_sync`, `domain_count`
- assert False body (stub)

After creating all three files, run the full test suite to confirm zero regressions:
`uv run pytest --tb=short -q 2>&1 | tail -15`

The output must show the existing passing count unchanged plus the 12 new SKIPPED stubs.
  </action>
  <verify>
    <automated>uv run pytest --tb=short -q 2>&1 | tail -15</automated>
  </verify>
  <done>Full suite passes: existing tests unchanged, 12 new SKIPPED stubs visible, 0 new failures or errors</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/unit/test_privacy_blocklist.py tests/unit/test_privacy_detection.py tests/unit/test_privacy_api.py -v 2>&1 | grep -E "SKIP|PASS|FAIL|ERROR"`
- All 12 stubs show SKIPPED, no FAILED or ERROR lines
- `uv run pytest -q 2>&1 | tail -5` shows same passing count as before plus 12 skipped
</verification>

<success_criteria>
- 12 stubs across 3 files, all SKIPPED cleanly via double-guard pattern
- Zero new test failures or import errors
- Stub names match PRIV-01 through PRIV-11 requirement IDs in comments
- Commit message: `test(privacy): wave-0 stubs for PRIV-01 through PRIV-11`
</success_criteria>

<output>
After completion, create `.planning/phases/53-network-privacy-monitoring/53-01-SUMMARY.md`
</output>
