---
phase: 44
plan: 44-01
type: tdd
wave: 0
title: "Wave 0 — TDD stubs for FeedbackStore and FeedbackClassifier"
depends_on: []
autonomous: true
gap_closure: false
files_modified:
  - tests/unit/test_feedback_store.py
  - tests/unit/test_feedback_classifier.py
requirements:
  - P44-T01
  - P44-T02
  - P44-T03
must_haves:
  - tests/unit/test_feedback_store.py exists with stubs that SKIP cleanly (no import errors)
  - tests/unit/test_feedback_classifier.py exists with one RED import test (ImportError) and all other stubs SKIP
  - Full unit suite passes with no regressions (all 1067 existing tests still green)
---

<objective>
Create Wave 0 TDD stubs that define the contracts for FeedbackStore (SQLite) and FeedbackClassifier (River).

Purpose: RED tests define the interface before implementation. One RED import test on FeedbackClassifier ensures Wave 1 must create the module before tests can pass. All other stubs skip cleanly so the existing 1067-test suite stays green.

Output: Two test files with stubs encoding behavioral contracts. No production code yet.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-analyst-feedback-loop/44-CONTEXT.md
@.planning/phases/44-analyst-feedback-loop/44-RESEARCH.md
@.planning/phases/44-analyst-feedback-loop/44-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 44-01-01: SQLite FeedbackStore stubs</name>
  <files>
    <file action="create">tests/unit/test_feedback_store.py</file>
  </files>
  <behavior>
    - test_feedback_table_exists: after SQLiteStore init, cursor.execute("SELECT name FROM sqlite_master WHERE type='table' AND name='feedback'") returns a row
    - test_upsert_feedback_tp: upsert_feedback("det-001", "TP") succeeds, get_verdict_for_detection("det-001") returns "TP"
    - test_upsert_feedback_fp: upsert_feedback("det-002", "FP") succeeds, get_verdict_for_detection("det-002") returns "FP"
    - test_verdict_update: upsert_feedback("det-003", "TP") then upsert_feedback("det-003", "FP") — get_verdict_for_detection("det-003") returns "FP" (upsert semantics)
    - test_verdict_missing: get_verdict_for_detection("nonexistent-id") returns None
    - test_get_feedback_stats_empty: get_feedback_stats() on fresh store returns dict with verdicts_given=0, tp_rate=0.0, fp_rate=0.0
    - test_get_feedback_stats_mixed: after 3 TP + 1 FP, get_feedback_stats() returns verdicts_given=4, tp_rate=0.75, fp_rate=0.25
  </behavior>
  <action>
    Create tests/unit/test_feedback_store.py following the same stub pattern as test_anomaly_scorer.py (Phase 42).

    Use module-level pytestmark for all stubs:
    ```python
    pytestmark = pytest.mark.skip(reason="stub — implemented in Plan 44-02")
    ```

    All 7 tests are stubs that skip cleanly. No RED import test here — SQLiteStore already exists and the methods will be added in Plan 44-02.

    Import guard pattern:
    ```python
    try:
        from backend.stores.sqlite_store import SQLiteStore
        _SQLITE_AVAILABLE = True
    except ImportError:
        _SQLITE_AVAILABLE = False

    pytestmark = pytest.mark.skipif(
        not _SQLITE_AVAILABLE,
        reason="SQLiteStore not available"
    )
    ```

    Each test function body contains only a `pass` (skip fires before body executes).

    Follow the exact fixture pattern used in test_duckdb_store.py and test_anomaly_scorer.py:
    - Use a temporary SQLite path (tmp_path fixture) to avoid polluting data/
    - SQLiteStore constructor accepts an optional path parameter
  </action>
  <verify>
    <automated>cd C:/Users/Admin/AI-SOC-Brain && uv run pytest tests/unit/test_feedback_store.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>7 tests SKIP with "stub — implemented in Plan 44-02" reason. 0 errors, 0 failures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 44-01-02: FeedbackClassifier stubs with one RED import test</name>
  <files>
    <file action="create">tests/unit/test_feedback_classifier.py</file>
  </files>
  <behavior>
    - test_import_feedback_classifier: `from backend.services.feedback.classifier import FeedbackClassifier` — this MUST FAIL RED until Plan 44-02 creates the module
    - test_learn_one_tp: classifier.learn_one({"severity": 3, "rule_id_hash": 1}, "TP") increments n_samples to 1
    - test_learn_one_fp: classifier.learn_one({"severity": 1, "rule_id_hash": 2}, "FP") increments n_samples to 1
    - test_accuracy_hidden_below_10: after 5 learn_one calls, classifier.accuracy() returns None
    - test_accuracy_visible_at_10: after 10 learn_one calls (mix of TP/FP), classifier.accuracy() returns a float
    - test_save_load_roundtrip: classifier with 5 samples saved, reloaded — n_samples == 5
    - test_predict_proba_tp: predict_proba_tp(features) returns float in [0.0, 1.0]
  </behavior>
  <action>
    Create tests/unit/test_feedback_classifier.py following the per-test decorator pattern from Phase 43's test_correlation_engine.py (using @pytest.mark.skip per stub, NOT module-level pytestmark — this allows one test to run RED while others skip).

    The import test does NOT get a skip decorator:
    ```python
    def test_import_feedback_classifier():
        from backend.services.feedback.classifier import FeedbackClassifier  # noqa: F401
    ```
    This runs and fails RED (ModuleNotFoundError) until Plan 44-02 creates the module.

    All other 6 behavioral stubs use per-test skip:
    ```python
    _skip = pytest.mark.skip(reason="stub — implemented in Plan 44-02")

    @_skip
    def test_learn_one_tp():
        pass
    ```

    The _skip decorator is defined once at module level and reused across all stub tests.

    The import guard check pattern is NOT used here — the RED import is intentional and load-bearing for Wave 0 contract.
  </action>
  <verify>
    <automated>cd C:/Users/Admin/AI-SOC-Brain && uv run pytest tests/unit/test_feedback_classifier.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>1 test FAILS RED (ImportError on FeedbackClassifier). 6 tests SKIP. 0 unexpected errors.</done>
</task>

<task type="auto">
  <name>Task 44-01-03: Verify full suite clean</name>
  <files></files>
  <action>
    Run the full unit suite to confirm 0 regressions after adding the two stub files. Expected: 1067 existing tests still green + 7 SKIPs + 6 SKIPs + 1 RED FAIL = 1074 collected, 1 failed (expected RED), 13 skipped, 1067 passed.

    If the RED fail count is not exactly 1 (from test_import_feedback_classifier), investigate and fix the stub files before proceeding.
  </action>
  <verify>
    <automated>cd C:/Users/Admin/AI-SOC-Brain && uv run pytest tests/unit/ -q 2>&1 | tail -10</automated>
  </verify>
  <done>Full suite: 1067+ passed, exactly 1 failed (test_import_feedback_classifier), 13 skipped. No regressions.</done>
</task>

</tasks>

<verification>
- tests/unit/test_feedback_store.py: 7 stubs, all SKIP
- tests/unit/test_feedback_classifier.py: 1 RED FAIL (import), 6 SKIP
- Full suite: 1067 existing tests GREEN, no regressions
</verification>

<success_criteria>
Wave 0 contracts established. The RED import test for FeedbackClassifier ensures Plan 44-02 must create backend/services/feedback/classifier.py before the test turns green. All existing tests unaffected.
</success_criteria>

<output>
After completion, create `.planning/phases/44-analyst-feedback-loop/44-01-SUMMARY.md`
</output>
