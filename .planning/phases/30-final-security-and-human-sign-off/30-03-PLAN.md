---
phase: 30-final-security-and-human-sign-off
plan: 03
type: execute
wave: 2
depends_on:
  - 30-01
files_modified: []
autonomous: false
requirements:
  - P30-T02
must_haves:
  truths:
    - "AI Advisory banner appears on assistant messages with no dismiss/close button"
    - "Settings > System tab loads and renders the AI Model Status card on activation"
    - "Confidence badge renders with correct colour based on grounding level"
    - "Ungrounded warning and citation tags render correctly in live UI"
  artifacts: []
  key_links:
    - from: "InvestigationView.svelte"
      to: ".ai-advisory-banner in live DOM"
      via: "Svelte reactive rendering"
      pattern: "no X/close/dismiss button visible"
    - from: "SettingsView.svelte $effect"
      to: "api.settings.modelStatus()"
      via: "tab activation trigger"
      pattern: "AI Model Status card renders"
    - from: "ChatHistoryMessage.confidence_score"
      to: "confidence badge CSS class"
      via: "Svelte class binding"
      pattern: "green/amber/red badge"
---

<objective>
Human visual verification of four Phase 22 AI Lifecycle Hardening UI behaviours that require a running frontend and backend to confirm.

Purpose: These items were flagged as human_needed in 22-VERIFICATION.md (status: human_needed). Code inspection has confirmed the implementation is correct, but live DOM rendering, event propagation, and CSS class application require browser confirmation for milestone sign-off.

Output: Human confirmation that all four UI behaviours render correctly in the live dashboard.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/22-ai-lifecycle-hardening/22-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start backend and frontend for verification</name>
  <files></files>
  <action>
Start the services needed for visual verification. Run these in separate terminals:

Terminal 1 — Backend:
```bash
uv run uvicorn backend.main:app --reload --port 8000
```
Confirm: `GET http://localhost:8000/health` returns `{"status": "healthy", ...}`

Terminal 2 — Frontend dev server:
```bash
npm run dev --prefix dashboard
```
Confirm: Dashboard accessible at `http://localhost:5173` (or the port shown in output)

If Ollama is not running, start it:
```bash
ollama serve
```

Note: If Docker (Caddy) is running, you can also use `https://localhost` — but the Vite dev server at port 5173 works for UI verification without HTTPS.
  </action>
  <verify>
    <automated>curl -s http://localhost:8000/health | python -m json.tool</automated>
  </verify>
  <done>Backend health endpoint returns JSON with status field; frontend dev server is running and accessible in a browser</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify four Phase 22 UI behaviours in live browser</name>
  <what-built>
Phase 22 implemented: AI Advisory banners (non-dismissable), confidence badges with colour thresholds, ungrounded warning with citation tags, and the SettingsView System tab model-status card. All code-level checks passed (10/10 observable truths in 22-VERIFICATION.md). This step confirms the browser rendering matches the implementation.
  </what-built>
  <how-to-verify>
    Open `http://localhost:5173` (or the frontend URL from Task 1) in a browser.

    **Check 1: AI Advisory banner — no dismiss button**
    1. Navigate to an Investigation (or create one)
    2. Send any question to the AI Copilot (e.g. "What happened?")
    3. Wait for the response to appear
    4. CONFIRM: A yellow/amber-bordered banner labelled "AI Advisory" (or similar) appears above the response text
    5. CONFIRM: There is NO X button, close button, or any clickable element to dismiss the banner
    6. CONFIRM: Clicking the banner area does nothing (no dismiss, no collapse)

    **Check 2: Confidence badge colour thresholds**
    1. On the same AI response (with no investigation context loaded): observe the confidence badge
    2. CONFIRM: Badge is RED (or lowest tier colour) for a response with no retrieved evidence
    3. Load some events into the investigation (or find one with existing events) and send another question
    4. CONFIRM: Badge colour changes to AMBER or GREEN based on how many events were retrieved

    **Check 3: Ungrounded warning and citation tags**
    1. On an ungrounded response (no context): CONFIRM a warning triangle or warning message appears stating the response is "not grounded in retrieved evidence" (or similar)
    2. On a grounded response (events retrieved): CONFIRM "Sources:" appears with one or more event ID tags like `[evt-001]` below the response text

    **Check 4: Settings > System tab — model-status card**
    1. Click the Settings icon/link in the dashboard navigation
    2. Navigate to the "System" tab within Settings
    3. CONFIRM: An "AI Model Status" card appears showing either the active model name or "Unknown (Ollama unreachable)" — NOT placeholder text like "Loading..." or an empty section
    4. CONFIRM: The card activates on tab click (lazy load), not on page load
  </how-to-verify>
  <resume-signal>
Type "approved" if all four checks pass as described.
Type "partial: [describe which checks failed]" if some items do not render correctly — the executor will investigate and fix before re-verification.
Type "blocked: [reason]" if the frontend or backend cannot be started.
  </resume-signal>
</task>

</tasks>

<verification>
Human has confirmed in browser:
1. AI Advisory banner is present and non-dismissable on assistant messages
2. Confidence badge shows correct colour tier (red/amber/green) based on grounding
3. Ungrounded warning shows for responses without retrieved context; citation tags show for grounded responses
4. Settings > System tab renders AI Model Status card on activation (not placeholder)
</verification>

<success_criteria>
- All four Phase 22 human_needed items from 22-VERIFICATION.md confirmed as passing in live browser
- Human typed "approved" as resume-signal
- 22-VERIFICATION.md human_needed items can be marked as complete for milestone sign-off
</success_criteria>

<output>
After completion, create `.planning/phases/30-final-security-and-human-sign-off/30-03-SUMMARY.md`
</output>
