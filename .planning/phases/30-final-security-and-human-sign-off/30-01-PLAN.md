---
phase: 30-final-security-and-human-sign-off
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/api/detect.py
  - rules/sigma/README.md
  - tests/unit/test_sigma_guard.py
autonomous: true
requirements:
  - P30-T03
must_haves:
  truths:
    - "POST /api/detect/run returns HTTP 422 with a descriptive warning when no Sigma rules are loaded"
    - "The warning body clearly states 'No Sigma rules loaded' and names the expected directory"
    - "rules/sigma/ directory exists with a README explaining where to place rules"
    - "When rules ARE present the endpoint continues to behave normally (no regression)"
  artifacts:
    - path: "backend/api/detect.py"
      provides: "0-rule guard in run_detection() raising HTTPException(422)"
      contains: "No Sigma rules loaded"
    - path: "rules/sigma/README.md"
      provides: "Operator guidance for adding Sigma rules"
    - path: "tests/unit/test_sigma_guard.py"
      provides: "Unit tests confirming 0-rule guard raises 422"
      exports: ["test_run_detection_no_rules_raises_422", "test_run_detection_with_rules_proceeds"]
  key_links:
    - from: "backend/api/detect.py run_detection()"
      to: "loaded == 0 check"
      via: "if loaded == 0: raise HTTPException(422, ...)"
      pattern: "loaded.*==.*0"
---

<objective>
Add a guard to POST /api/detect/run that prevents silent 0-detection failures when no Sigma rules are present. Create the rules/sigma/ directory with operator guidance.

Purpose: When rules/sigma/ is absent or empty, the endpoint currently loads 0 rules, runs, and returns an empty detections list with no indication of the misconfiguration. Operators see "0 detections" and have no way to know if this reflects a genuine clean state or a broken rules path.

Output: Modified detect.py with HTTPException(422) guard, rules/sigma/README.md, and a unit test file confirming the guard behaviour.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/api/detect.py
@detections/matcher.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing test for 0-rule guard</name>
  <files>tests/unit/test_sigma_guard.py</files>
  <behavior>
    - test_run_detection_no_rules_raises_422: mock SigmaMatcher.load_rules_dir to return 0 for all dirs; call run_detection via FastAPI TestClient; assert response.status_code == 422; assert "No Sigma rules loaded" in response.json()["detail"]
    - test_run_detection_with_rules_proceeds: mock load_rules_dir to return 3 (rules loaded); mock matcher.run_all to return []; assert response.status_code == 200; assert response.json()["count"] == 0 (clean result, not an error)
  </behavior>
  <action>
Create `tests/unit/test_sigma_guard.py`. Use pytest-asyncio (auto mode — no explicit asyncio_mode decorator needed per pyproject.toml). Use FastAPI TestClient with `from backend.main import create_app`. Mock `detections.matcher.SigmaMatcher.load_rules_dir` using `unittest.mock.patch`. The tests must FAIL before Task 2 modifies detect.py (RED phase). Run `uv run pytest tests/unit/test_sigma_guard.py -v` and confirm at least one test fails before proceeding.

Import pattern:
```python
from unittest.mock import AsyncMock, MagicMock, patch
from fastapi.testclient import TestClient
from backend.main import create_app
```

For the no-rules test, patch `backend.api.detect.SigmaMatcher` so `load_rules_dir` returns 0 every call. For the with-rules test, patch it so `load_rules_dir` returns 3 and `run_all` returns an AsyncMock returning [].
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_sigma_guard.py -v 2>&1 | head -40</automated>
  </verify>
  <done>test_sigma_guard.py exists; at least one test fails (confirming the guard does not yet exist — RED state confirmed)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add 0-rule guard to run_detection and create rules/sigma/README.md</name>
  <files>backend/api/detect.py, rules/sigma/README.md</files>
  <behavior>
    - After the rules-loading loop in run_detection(), if loaded == 0: raise HTTPException(status_code=422, detail="No Sigma rules loaded — rules/sigma/ is empty or missing. Add .yml rule files to rules/sigma/ and retry.")
    - Log a warning before raising: log.warning("run_detection: 0 Sigma rules loaded — returning 422")
    - Normal flow (loaded > 0) is unchanged
  </behavior>
  <action>
Modify `backend/api/detect.py` `run_detection()` function.

After this existing block (lines 124-129):
```python
    rules_dirs = [_Path("fixtures/sigma"), _Path("rules/sigma")]
    loaded = 0
    for d in rules_dirs:
        if d.exists():
            loaded += matcher.load_rules_dir(str(d))
```

Insert immediately after:
```python
    if loaded == 0:
        log.warning(
            "run_detection: no Sigma rules found in any rules directory",
            checked=["fixtures/sigma", "rules/sigma"],
        )
        raise HTTPException(
            status_code=422,
            detail=(
                "No Sigma rules loaded — rules/sigma/ is empty or missing. "
                "Add Sigma YAML rule files to rules/sigma/ and retry."
            ),
        )
```

Then create the directory and README. The README must be placed at `rules/sigma/README.md`:

Content: Explain that `.yml` Sigma rule files placed in this directory are loaded automatically by POST /api/detect/run. Include a minimal example rule skeleton. Note that `fixtures/sigma/` is for development/test fixtures only and should not be used in production.

Run `uv run pytest tests/unit/test_sigma_guard.py -v` after the edit — both tests must pass (GREEN state).
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_sigma_guard.py -v</automated>
  </verify>
  <done>Both test_run_detection_no_rules_raises_422 and test_run_detection_with_rules_proceeds pass; rules/sigma/README.md exists; full unit suite still green: uv run pytest tests/unit/ -q passes</done>
</task>

<task type="auto">
  <name>Task 3: Full unit suite regression check</name>
  <files></files>
  <action>
Run the full unit test suite to confirm no regressions from the detect.py change.

```bash
uv run pytest tests/unit/ -q --tb=short
```

If any tests fail, inspect and fix. The detect.py change only adds a guard before the existing `matcher.run_all()` call, so regressions should be limited to tests that mock the full endpoint flow without providing rules.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/ -q --tb=short</automated>
  </verify>
  <done>All unit tests pass (or any pre-existing failures are unchanged from baseline). 0 new failures introduced.</done>
</task>

</tasks>

<verification>
- POST /api/detect/run with empty rules dirs returns HTTP 422 with detail containing "No Sigma rules loaded"
- POST /api/detect/run with rules present returns HTTP 200
- rules/sigma/README.md exists and contains operator guidance
- uv run pytest tests/unit/ -q passes
</verification>

<success_criteria>
- 0-rule guard in backend/api/detect.py raises HTTPException(422) when loaded == 0
- rules/sigma/README.md created with example rule skeleton
- tests/unit/test_sigma_guard.py contains at least 2 passing tests
- No regressions in unit suite
</success_criteria>

<output>
After completion, create `.planning/phases/30-final-security-and-human-sign-off/30-01-SUMMARY.md`
</output>
